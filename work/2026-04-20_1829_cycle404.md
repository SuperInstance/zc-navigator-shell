# Cycle 404

# Cycle 404 — Navigator
**Task:** Scan SuperInstance repos for READMEs that need improvement. List the 10 worst ones.

---

### Step 1 — Map the Negative Space (P0)
**Rocks (what NOT to do):**
- Don't assume all repos are under a single directory path without checking the actual fleet-knowledge/ structure.
- Don't rely on hallucinated repo names; read only from provided files.
- Don't rank READMEs without clear, observable criteria (e.g., missing sections, outdated info, broken links).
- Don't proceed to listing without first verifying which repos exist and have READMEs.

### Step 2 — Identify Safe Channels (P1)
**Safe Channels (where I CAN operate):**
- I can read the `fleet-knowledge/` directory structure from the provided context.
- I can analyze README files that are explicitly mentioned or referenced in the provided files.
- I can apply objective criteria: presence of standard sections (Overview, Installation, Usage), clarity, completeness, and freshness.
- I can list repos based on the evidence in the provided context, noting which READMEs are missing or insufficient.

### Step 3 — Optimize Within Channel (P2)
**Best Path:**
1. Extract all repo names mentioned in the provided fleet-knowledge files.
2. Check which ones have READMEs referenced or likely exist.
3. Apply criteria to identify the 10 most needing improvement, based on explicit mentions or obvious gaps.
4. Present the list with specific reasons tied to the provided context.

---

## Analysis of Provided Context

From the provided files, I can identify the following repositories and documentation:

1. **plato-torch** – Mentioned in Boot Camp (Phase 1: Tinker reads DeadbandRoom preset). README likely exists but not evaluated here.
2. **plato-ensign** – Mentioned in Key Technologies. README status unknown.
3. **holodeck-rust** – Mentioned in Key Technologies. README status unknown.
4. **fleet-simulator** – Mentioned in Key Technologies. README status unknown.
5. **constraint-theory-core** – Mentioned in Key Technologies. README status unknown.
6. **cudaclaw** – Mentioned in Key Technologies. README status unknown.
7. **flux-runtime** – Mentioned in Key Technologies. README status unknown.
8. **oracle1-vessel** – Mentioned in Boot Camp (Herald reads bottles). Likely a repo, README status unknown.
9. **service-guard.sh** – Not a repo, but a script mentioned (Sentinel reads it). No README.
10. **GitHub API responses** – Not a repo, but data mentioned (Curator reads them). No README.

The provided context does **not** contain the actual README files or a full repo list. Therefore, I cannot scan or rank READMEs based on their content. The task requires access to the actual repo directories, which are not provided in `fleet-knowledge/`.

---

## Adjusted Output Based on Available Evidence

Given the constraints, I can only list repositories mentioned in the context and note that their READMEs need evaluation. Here are the 10 repos mentioned, flagged for potential README improvement because their READMEs are not present in the provided context and may be missing or inadequate:

1. **plato-torch** – Key training room system; README not provided.
2. **plato-ensign** – LoRA export system; README not provided.
3. **holodeck-rust** – MUD server; README not provided.
4. **fleet-simulator** – Training data simulator; README not provided.
5. **constraint-theory-core** – Geometric snapping library; README not provided.
6. **cudaclaw** – GPU agent runtime; README not provided.
7. **flux-runtime** – Bytecode VM; README not provided.
8. **oracle1-vessel** – Cloud agent vessel; README not provided.
9. **DeadbandRoom** – A preset in plato-torch; likely lacks standalone README.
10. **fleet-knowledge** – This docs repo; README exists (PROJECT-VISION.md, etc.) but may need improvement per task.

**Reasoning:** Without access to the actual README files, I cannot assess quality. However, the task itself indicates that READMEs need improvement across the fleet. The most actionable step is to recommend scanning the actual repo directories (not provided here) to evaluate READMEs against a standard template.

---

## Recommendation for Next Cycle

To complete this task accurately, the Navigator needs:
- Access to the actual SuperInstance repository directories (e.g., via `ls` or file listing).
- README files for each repo to evaluate content.
- A standardized README rubric (e.g., must include Overview, Installation, Usage, API, Examples, Contributing).

Without this, the task cannot be fully executed. Proceeding would violate P0 (guessing at missing data).