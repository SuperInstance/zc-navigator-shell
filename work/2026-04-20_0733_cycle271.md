# Cycle 271

# Cycle 271 — Navigator
**Task:** Scan SuperInstance repos for READMEs that need improvement. List the 10 worst ones.

---

### Step 1 — Map the Negative Space (P0)
**Rocks (what NOT to do):**
- Don't assume all repos are under a single directory path without checking the actual fleet-knowledge/ structure.
- Don't hallucinate repo names or README content; read the actual files provided.
- Don't skip repos that are part of the SuperInstance ecosystem but might be listed elsewhere.
- Don't evaluate READMEs based on subjective "badness"; use objective, observable criteria.
- Don't proceed to listing without first defining what "needs improvement" means for this fleet.

### Step 2 — Identify Safe Channels (P1)
**Safe Channels (where I CAN operate):**
1. Use only the files and directory listings provided in `fleet-knowledge/`.
2. Define clear, objective criteria for a "README that needs improvement" based on observable patterns in the fleet.
3. Systematically scan all provided repo directories for README files.
4. Score each README against the criteria.
5. Rank and list the 10 lowest-scoring READMEs with specific, factual reasons.

**Criteria for "Needs Improvement" (derived from fleet patterns):**
- **Missing README:** No README file present.
- **Empty/Skeleton README:** File exists but is essentially empty (e.g., only a title, placeholder text like "TODO").
- **No Purpose/Context:** Does not explain what the repo is for or how it fits into the fleet.
- **No Usage Instructions:** Lacks basic setup, installation, or run commands.
- **No Link to Fleet Doctrine:** Does not reference Deadband Protocol, PLATO rooms, or other core fleet concepts.
- **Formatting Issues:** Poor structure, broken Markdown, or very long walls of text.

### Step 3 — Optimize Within Channel (P2)
**Execution Path:**

1. **Scan `fleet-knowledge/` for repo directories.**
   - Based on provided files, I see references to several key repos: `plato-torch`, `plato-ensign`, `holodeck-rust`, `fleet-simulator`, `constraint-theory-core`, `cudaclaw`, `flux-runtime`.
   - The `fleet-knowledge/` directory provided in the context contains `docs/` and `memory/` subdirectories, but not the full source code repos. Therefore, I cannot directly read those READMEs.
   - **Constraint Identified:** My task requires scanning "SuperInstance repos," but my current shell (`fleet-knowledge/`) does not contain those repos. This is a P0 rock: I cannot read files that are not provided.

2. **Apply Deadband Protocol to the constraint:**
   - **P0 (Negative Space):** I cannot list 10 worst READMEs from source repos because they are not in my current context. Attempting to guess or hallucinate them would violate the protocol.
   - **P1 (Safe Channel):** I can operate on the documentation that *is* provided within `fleet-knowledge/`. I can analyze the `PROJECT-VISION.md` and `DEADBAND-PROTOCOL.md` as examples of fleet documentation. I can also note that the `Boot Camp` instructions imply these repos exist elsewhere.
   - **P2 (Optimize):** Produce a useful, factual output based on the safe channel.

3. **Produce Output Within Safe Channel:**
   Given the constraint, I will:
   - Acknowledge the limitation.
   - Provide an analysis of the READMEs I *can* see (`PROJECT-VISION.md` and `DEADBAND-PROTOCOL.md`) as a reference standard.
   - Propose a method for the task if the repo access were available.
   - Submit this analysis as a valid tile for the "Navigator" role, demonstrating systematic problem-solving under constraints.

---

## Output: Analysis and Proposed Method

**Finding:** The source code repositories (e.g., `plato-torch`, `holodeck-rust`) are not present in the current `fleet-knowledge/` context. Therefore, I cannot scan them or list their READMEs.

**Analysis of Available Documentation:**
1.  **`PROJECT-VISION.md`:** **Score: High.** Serves as an excellent north star document. Clearly states core thesis, provides context vs. status quo, and is well-structured. It is a model for what a good README should aspire to in terms of vision and clarity.
2.  **`DEADBAND-PROTOCOL.md`:** **Score: High.** Provides a formal, actionable rule, includes proof/evidence, and gives abstract patterns for application. It is a model for technical documentation.

**Proposed Method for the Task (if repos were accessible):**
1.  Clone or navigate to the SuperInstance GitHub organization / directory.
2.  Run a script to find all `README.md` files.
3.  Apply the scoring criteria (Missing, Empty, No Purpose, No Usage, No Fleet Links, Formatting).
4.  Assign a score (e.g., 0-5, where 5 is excellent like the vision doc).
5.  Sort and list the 10 with the lowest scores, including the file path and specific reasons for the low score (e.g., "Repo X: README missing. Repo Y: Contains only '# TODO'.").

**Navigator's Recommendation:**
The task "Scan SuperInstance repos for READMEs that need improvement" is a valid and useful P2 optimization task for a Navigator. However, it requires access to the repository file system. To complete this task, the agent (or a subsequent task) would need to:
- **P0:** Ensure the `fleet-knowledge/` context includes the repo directories or a listing of them.
- **P1:** Use a tool like `find . -name "README.md"` and a parsing script.
- **P2:** Execute the scoring and ranking method described above.

This cycle's output is a tile documenting the constraint, analyzing available docs, and providing a clear, actionable method for the intended task.

**Tile Submitted:** Navigator analysis of README evaluation task under context constraints.