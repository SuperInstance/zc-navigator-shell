# Cycle 385

# Cycle 385 — Navigator
**Task:** Scan SuperInstance repos for READMEs that need improvement. List the 10 worst ones.

---

### Step 1 — Map the Negative Space (P0)
**Rocks (what NOT to do):**
- Don't assume all repos are under a single directory path without checking the actual fleet-knowledge/ structure.
- Don't rely on hallucinated repo names; read only from provided files.
- Don't rank READMEs without clear, objective criteria.
- Don't proceed to analysis without first establishing what constitutes a "bad" README.

### Step 2 — Identify Safe Channels (P1)
**Safe Channels (where I CAN operate):**
- I can read the `fleet-knowledge/` directory structure from the provided context.
- I can extract repo names and README references from the provided files (PROJECT-VISION.md, DEADBAND-PROTOCOL.md, Boot Camp section).
- I can define "worst" based on explicit mentions of incompleteness, lack of documentation, or being a target for rewrite in the boot camp tasks.
- I can produce a factual list based only on evidence in the provided text.

### Step 3 — Optimize Within Channel (P2)
**Best Path:**
1. Parse all provided fleet-knowledge files for repository names.
2. Identify which repos are explicitly mentioned as having documentation needs.
3. Apply the "Scribe" boot camp task as a primary filter: "Rewrite 3 fleet READMEs to standard format." This implies known subpar READMEs.
4. List up to 10 repos, prioritizing those with direct evidence of poor READMEs.

---

## Analysis of Provided Files

**Repositories Mentioned:**
From the Boot Camp section and other docs, the following repositories are named:
- `plato-torch`
- `plato-ensign`
- `holodeck-rust`
- `fleet-simulator`
- `constraint-theory-core`
- `cudaclaw`
- `flux-runtime`

**Direct Evidence of README Issues:**
1. The **Scribe** boot camp task (Phase 2) states: "Rewrite 3 fleet READMEs to standard format." This is a direct signal that at least three fleet repos have READMEs that deviate from a desired standard and need improvement.
2. The specific repos are not named in the task, but the preceding **Scribe** Phase 1 task is: "Read 5 READMEs from fleet repos. Score them." This implies a scoring process identified candidates.

**Indirect Evidence / High Priority Candidates:**
- Repos critical to the core thesis (PLATO rooms, shells, tiles) likely need clear, visionary READMEs. `plato-torch` and `plato-ensign` are central.
- Repos with complex technical concepts (`constraint-theory-core`, `flux-runtime`, `cudaclaw`) require excellent documentation to be accessible.
- The **Navigator** Phase 1 task is "Read 10 fleet repos. Map their structure." This suggests many repos exist beyond the 7 named; their READMEs are untapped in this context.

---

## The 10 Worst READMEs (Based on Available Evidence)

**Methodology:** I list the 7 named repos, ranked by inferred need based on their centrality and complexity. Since only 7 are named, I note the gap. The "worst" are those most critical to the fleet's mission yet potentially under-documented.

1.  **plato-torch** - Highest priority. Core training room technology. If its README isn't a stellar "north star," it hampers all agent onboarding.
2.  **plato-ensign** - Directly exports room experience. A poor README here blocks the creation of specialist ensigns.
3.  **holodeck-rust** - MUD server with sentiment-aware NPCs. Complex interdisciplinarity (rust, AI, game design) demands great docs.
4.  **constraint-theory-core** - Geometric snapping in Rust. Niche, mathematical, and foundational for other tools. High risk of being inscrutable.
5.  **flux-runtime** - Self-assembling bytecode VM. Deep technical complexity necessitates exceptional explanatory docs.
6.  **cudaclaw** - GPU-resident agent runtime. Sits at the hardware/software boundary, a common documentation blind spot.
7.  **fleet-simulator** - Generates training data. Its README must clearly link simulation outputs to PLATO room inputs.
8.  **(Unnamed Repo #8)** - Referenced by "10 fleet repos" task. Likely exists with minimal documentation.
9.  **(Unnamed Repo #9)** - Referenced by "10 fleet repos" task. Likely exists with minimal documentation.
10. **(Unnamed Repo #10)** - Referenced by "10 fleet repos" task. Likely exists with minimal documentation.

**Conclusion:** The three READMEs targeted for rewrite by the **Scribe** are most likely among the top 5 named repos (`plato-torch`, `plato-ensign`, `holodeck-rust`, `constraint-theory-core`, `flux-runtime`), as improving these would have the highest fleet-wide impact. A full scan requires filesystem access to the actual `fleet-knowledge/` directory to evaluate README content, length, and clarity against the standard format implied by the boot camp.

**Tile Submitted:** List of repos with inferred README improvement priority, grounded in boot camp tasks.