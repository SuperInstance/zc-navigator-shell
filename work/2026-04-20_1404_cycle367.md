# Cycle 367

# Cycle 367 — Navigator
**Task:** Scan SuperInstance repos for READMEs that need improvement. List the 10 worst ones.

---

### Step 1 — Map the Negative Space (P0)
**Rocks (what NOT to do):**
- Don't assume all repos are under a single directory path without checking the actual fleet-knowledge/ structure.
- Don't rely on hallucinated repo names; only list repos that have actual README files in the provided context.
- Don't score READMEs based on subjective "badness"; use objective, observable criteria.
- Don't proceed to listing without first establishing clear, measurable criteria for "needs improvement."

### Step 2 — Identify Safe Channels (P1)
**Safe Channels (where I CAN operate):**
- I can read the `fleet-knowledge/` directory structure provided in the context.
- I can analyze README files that are explicitly present in the context.
- I can define objective criteria for README quality based on common documentation standards.
- I can produce a factual list based only on the files provided.

### Step 3 — Optimize Within Channel (P2)
**Objective Criteria for "Needs Improvement":**
A README needs improvement if it exhibits one or more of the following:
1. **Missing or Skeletal:** File is empty, contains only a title, or is clearly a stub (e.g., "TODO").
2. **Lacks Structure:** No clear sections (e.g., no Installation, Usage, API).
3. **Outdated/Incorrect:** Contains obvious placeholders, incorrect commands, or references to non-existent features.
4. **No Purpose/Context:** Fails to explain what the project is or why it exists.

**Analysis of Provided Context:**
The provided `fleet-knowledge/` context contains two primary documentation files:
1. `docs/PROJECT-VISION.md`
2. `docs/DEADBAND-PROTOCOL.md`

Both are substantial, structured, and complete. They do not meet the "needs improvement" criteria.

**Constraint:** The task asks to "Scan SuperInstance repos," but the provided context (`fleet-knowledge/`) does not contain a directory of repository READMEs to scan. The only READMEs present are the two high-level docs in `/docs`.

**Safe Path Forward (P1 Compliance):**
Since I cannot scan repos not provided, I will:
1. State the constraint clearly.
2. Apply the criteria to the documentation that *is* present.
3. Provide a template for the task if/when the full repo list becomes available.

---

### Output: README Assessment & Task Refinement

**Finding:** The `fleet-knowledge/` context provided does not contain a directory of SuperInstance repository READMEs. Therefore, I cannot list the "10 worst ones" from a non-existent set.

**Assessment of Available Docs:**
1.  `PROJECT-VISION.md` – **PASS.** Comprehensive, well-structured, clear purpose.
2.  `DEADBAND-PROTOCOL.md` – **PASS.** Formal, includes proof, abstract pattern, and clear instructions.

**Proposed Task Refinement (Navigator's Recommendation):**
To complete the intended task, the following data is needed:
- A list of repository paths (e.g., `plato-torch/README.md`, `holodeck-rust/README.md`).
- The content of those README files.

**Template for Future Execution:**
Given the repo list, I would execute as follows:
1.  **P0:** Fetch each README. Skip repos without a README.
2.  **P1:** Score each README against the objective criteria (1-4 above).
3.  **P2:** Rank by score and list the 10 with the most deficiencies.

**Example of what a "worst" entry would look like (hypothetical):**
- **Repo:** `example-tool`
- **README Score:** 3/4 deficiencies.
- **Issues:** Skeletal (only a title), no structure, no usage instructions.
- **Improvement Priority:** HIGH.

**Current Action:** This task is **blocked on data availability**. My output is this analysis and a ready-to-execute protocol for when the repository data is provided.

---
**Tile Submitted:** README analysis protocol and constraint identification.
**Status:** Task paused at P1 (safe channel identified: cannot proceed without repo data).