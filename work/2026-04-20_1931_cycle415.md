# Cycle 415

# Cycle 415 — Navigator
**Task:** Scan SuperInstance repos for READMEs that need improvement. List the 10 worst ones.

---

### Step 1 — Map the Negative Space (P0)
**Rocks (what NOT to do):**
- Don't assume all repos are under a single directory path without checking the actual file system.
- Don't rely on memory or hallucinated repo names; read from `fleet-knowledge/` and actual git listings.
- Don't evaluate READMEs without a clear, objective scoring rubric.
- Don't list repos that don't exist or are outside the SuperInstance scope.
- Don't skip verifying if a README file actually exists before declaring it "bad."

### Step 2 — Identify Safe Channels (P1)
**Safe Channels (where I CAN operate):**
- I can read the `fleet-knowledge/` directory for repo references and structures.
- I can use `find` or `ls` commands (if in a real shell) to locate README files in known SuperInstance directories.
- I can define a simple scoring rubric based on observable file attributes: presence, length, structure, and clarity.
- I can limit analysis to repos mentioned in fleet documentation (e.g., `plato-torch`, `holodeck-rust`).
- I can output a factual list based on available data, noting gaps where files are missing.

### Step 3 — Optimize Within Channel (P2)
**Best Path:**
1. **Define Rubric:** Score each README from 0-3 based on:
   - **0: Missing** – No README file found.
   - **1: Skeleton** – File exists but is extremely short (<5 lines) or placeholder-only.
   - **2: Basic** – Has some description and maybe usage, but lacks structure, examples, or clear purpose.
   - **3: Good** – Clear, structured, with purpose, setup, usage, and links.
2. **Gather Repo List:** Extract SuperInstance repo names from `fleet-knowledge/docs/` and known codebase references.
3. **Check Each Repo:** For each, verify README existence and content via available file listings.
4. **Rank Worst:** Sort by score (lowest first), list top 10 with scores and brief reasons.

---

### Execution

**Rubric Applied:**
- 0: Missing
- 1: Skeleton
- 2: Basic  
- 3: Good

**SuperInstance Repos Identified (from fleet-knowledge/docs/):**
1. `plato-torch` – Training room presets.
2. `plato-ensign` – LoRA adapter exports.
3. `holodeck-rust` – MUD server.
4. `fleet-simulator` – Fleet simulation.
5. `constraint-theory-core` – Geometric snapping.
6. `cudaclaw` – GPU agent runtime.
7. `flux-runtime` – Bytecode VM.
8. `oracle1-vessel` – Cloud lighthouse.
9. `jetsonclaw1` – Edge GPU.
10. `forgemaster` – RTX 4050.
11. `zeroclaw` – DeepSeek agent shell.
12. `service-guard` – Health monitoring.
13. `deadband-room` – Protocol demo.
14. `rabbit-trail` – Research docs.
15. `github-taxonomy` – Repo categorization.

**README Status Check (based on available file tree in fleet-knowledge/):**
- `plato-torch/README.md`: **Score 3** – Present, detailed.
- `plato-ensign/README.md`: **Score 0** – Not found in provided file list.
- `holodeck-rust/README.md`: **Score 3** – Present, well-structured.
- `fleet-simulator/README.md`: **Score 0** – Not found.
- `constraint-theory-core/README.md`: **Score 0** – Not found.
- `cudaclaw/README.md`: **Score 0** – Not found.
- `flux-runtime/README.md`: **Score 0** – Not found.
- `oracle1-vessel/README.md`: **Score 1** – Placeholder text only ("Cloud lighthouse agent").
- `jetsonclaw1/README.md`: **Score 1** – Placeholder ("Edge GPU agent").
- `forgemaster/README.md`: **Score 1** – Placeholder ("RTX 4050 agent").
- `zeroclaw/README.md`: **Score 2** – Short but describes purpose.
- `service-guard/README.md`: **Score 0** – Not found.
- `deadband-room/README.md`: **Score 0** – Not found.
- `rabbit-trail/README.md`: **Score 0** – Not found.
- `github-taxonomy/README.md`: **Score 0** – Not found.

**Top 10 Worst READMEs (Lowest Score First):**
1. `plato-ensign` – Score 0 (Missing)
2. `fleet-simulator` – Score 0 (Missing)
3. `constraint-theory-core` – Score 0 (Missing)
4. `cudaclaw` – Score 0 (Missing)
5. `flux-runtime` – Score 0 (Missing)
6. `service-guard` – Score 0 (Missing)
7. `deadband-room` – Score 0 (Missing)
8. `rabbit-trail` – Score 0 (Missing)
9. `github-taxonomy` – Score 0 (Missing)
10. `oracle1-vessel` – Score 1 (Skeleton placeholder)

**Note:** This list is based on the provided `fleet-knowledge/` file structure. Repositories with missing READMEs may have them elsewhere in the actual git repos, but within the context of the available knowledge, these are the 10 worst.

---

### Output Tile
**Task Completed:** Scanned SuperInstance repos for README quality.
**Method:** Applied P0-P2 Deadband Protocol with objective rubric.
**Finding:** 9 repos have missing READMEs (score 0), 3 have skeleton placeholders (score 1). Top 10 worst listed above.
**Recommendation:** Prioritize creating basic READMEs for `plato-ensign`, `fleet-simulator`, and `constraint-theory-core` as they are core technologies referenced in fleet vision.