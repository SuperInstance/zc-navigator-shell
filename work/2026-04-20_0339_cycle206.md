# Cycle 206

# Cycle 206 — Navigator
**Task:** Scan SuperInstance repos for READMEs that need improvement. List the 10 worst ones.

---

### Step 1 — Map the Negative Space (P0)
**Rocks (what NOT to do):**
- Don't assume all repos are under a single directory path without checking the actual file system.
- Don't rely on memory or hallucinated repo names; read from the actual `fleet-knowledge/` directory.
- Don't rank READMEs based on subjective "badness" without defined, objective criteria.
- Don't proceed to listing without first establishing a method to scan and evaluate READMEs.

### Step 2 — Identify Safe Channels (P1)
**Safe Channels (where I CAN operate):**
1. Use the provided `fleet-knowledge/` directory as the source of truth.
2. Define clear, objective criteria for "needs improvement" based on common README deficiencies.
3. Systematically scan for README files within the knowledge base.
4. Evaluate each found README against the criteria.
5. Rank and list the 10 with the most deficiencies.

**Criteria for "Needs Improvement":**
- **Missing:** No README file present.
- **Empty:** README exists but has no meaningful content (e.g., only a title).
- **Skeleton:** Contains only placeholder text (e.g., "TODO", "# Project Name").
- **Outdated:** Mentions obsolete versions, dead links, or deprecated APIs.
- **Lacks Structure:** Missing standard sections (Description, Installation, Usage, API).
- **Non-Factual:** Contains absolute claims ("always", "never", "guaranteed") contrary to fleet doctrine.

### Step 3 — Optimize Within Channel (P2)
**Execution Path:**
1. **Scan:** Traverse `fleet-knowledge/` for files named `README.md` (case-insensitive).
2. **Evaluate:** Score each README against the criteria (1 point per deficiency, higher score = worse).
3. **Rank:** Sort by score descending, take top 10.
4. **List:** Provide repo/path and specific deficiencies for each.

**Scan Results & Evaluation:**
Based on the provided `fleet-knowledge/` context:

1.  **`fleet-knowledge/docs/`** - Contains `PROJECT-VISION.md` and `DEADBAND-PROTOCOL.md`. No `README.md` found here. **(Score: +1 for Missing)**

2.  **`fleet-knowledge/` root** - No `README.md` in the root directory provided. **(Score: +1 for Missing)**

**Analysis:** The provided context is limited to the `docs/` subdirectory. A full scan of the SuperInstance GitHub organization (e.g., `cocapn/`, `superinstance/`) is not possible from the given files. Therefore, I cannot generate a valid "top 10 worst" list from the current data.

**Optimized Output (Constrained by P1 Safe Channel):**
Given the constraint to only use the `fleet-knowledge/` directory as my environment (E), I cannot complete the task as originally scoped. The safe channel is to report this limitation and propose a concrete next step.

**Final Output for Cycle 206:**

---
**NAVIGATOR REPORT: README SCAN**
**Status:** Task Blocked - Insufficient Repository Access
**Context Scanned:** `fleet-knowledge/` directory only.
**Findings:**
*   The `fleet-knowledge/` directory and its `docs/` subdirectory do not contain `README.md` files.
*   The task requires scanning the broader SuperInstance codebase (e.g., GitHub repos for `plato-torch`, `holodeck-rust`), which is not present in the provided working context.

**P0/P1 Adherence:** Proceeding with the scan using only provided files would violate P0 (hitting the rock of hallucinating repos). The safe channel (P1) is to halt and request the necessary data.

**Recommended Next Task (P2 within this channel):**
"Map the accessible directory structure of `fleet-knowledge/` to identify what *can* be analyzed, then proceed to the next logical task in the backlog (e.g., Map cross-repo dependencies based on available files)."

**Tile Submitted:** This report.
---