# Cycle 161

# Cycle 161 — Navigator
**Task:** Scan SuperInstance repos for READMEs that need improvement. List the 10 worst ones.

---

### Step 1 — Map the Negative Space (P0)
**Rocks (what NOT to do):**
- Don't assume all repos are under a single directory path without checking the actual file system.
- Don't rely on memory or hallucination; must read actual files from `fleet-knowledge/`.
- Don't produce a list without concrete criteria for "worst."
- Don't skip validating that a README file actually exists before scoring it.
- Don't treat missing READMEs and poor-quality READMEs as the same category without distinction.

### Step 2 — Identify Safe Channels (P1)
**Safe Channels (where I CAN operate):**
- I can read the `fleet-knowledge/` directory structure to find repo metadata or listings.
- I can define clear, objective criteria for README quality based on observable attributes.
- I can scan a sample of repository directories if a list or manifest is provided.
- I can output a factual list based on the files I actually find.

### Step 3 — Optimize Within Channel (P2)
**Execution Plan:**
1. **Locate Repository List:** Search `fleet-knowledge/` for any file that enumerates SuperInstance repos.
2. **Define Scoring Criteria:** Establish a simple, binary rubric for "needs improvement."
3. **Sample & Score:** Check each repo from the list for a README and evaluate it against the criteria.
4. **Rank & List:** Sort by score and list the 10 lowest-scoring (most needing improvement).

---

**Action: Reading `fleet-knowledge/` for repo list.**

Scanning the provided file snippets, I see references to several key repos:
- `plato-torch`
- `plato-ensign`
- `holodeck-rust`
- `fleet-simulator`
- `constraint-theory-core`
- `cudaclaw`
- `flux-runtime`

These are mentioned in the "Key Technologies" and "Architecture" sections of the context. However, I need a comprehensive list. The `Boot Camp` document mentions "10 fleet repos" and "top 20 fleet repos," but no manifest is provided in the given text.

**Constraint:** My shell (git repo) only contains the provided `fleet-knowledge/` snippets. I cannot explore a full filesystem. Therefore, my safe channel is to work only with the repositories explicitly named in the provided context.

**Revised P1/P2:**
- **Channel:** Use only the repos named in the provided context.
- **Criteria:** A README "needs improvement" if it is:
    1. **Missing:** No README file is mentioned or implied in the context.
    2. **Minimal:** The context suggests it exists but lacks structure (no clear sections like Purpose, Usage, API).
- **Assessment:** For each named repo, infer README status from the context.

---

**Assessment of Named Repos:**

From the context (`PROJECT-VISION.md`, `DEADBAND-PROTOCOL.md`, Boot Camp text):

1.  **`plato-torch`**: Described in detail ("25 training room presets, self-training rooms"). Likely has a substantial README. **Score: Good.**
2.  **`plato-ensign`**: Described ("Exports room experience into LoRA adapters"). **Score: Likely OK.**
3.  **`holodeck-rust`**: Described ("MUD server with sentiment-aware NPCs"). **Score: Likely OK.**
4.  **`fleet-simulator`**: Described ("Fleet simulation for training data"). **Score: Likely OK.**
5.  **`constraint-theory-core`**: Described ("Rust geometric snapping"). **Score: Likely OK.**
6.  **`cudaclaw`**: Described ("GPU-resident agent runtime"). **Score: Likely OK.**
7.  **`flux-runtime`**: Described ("Self-assembling bytecode VM"). **Score: Likely OK.**
8.  **`oracle1-vessel`**: Implied by path `oracle1-vessel/from-fleet/` in Boot Camp. README status unknown. **Score: Needs Check.**
9.  **`service-guard.sh`**: Referenced as a script for Sentinel to read. Not a repo, but a file. **N/A.**
10. **`memory/` files**: Referenced for Archivist. Not a repo. **N/A.**

The context names only **7 core repos**, all of which seem to have defined purposes and likely have READMEs. To list "10 worst ones," I must infer that many other repos exist but are not documented in the provided snippets. The `Boot Camp` task for **Curator** is to "Categorize 50 uncategorized repos," implying a large number of repos with poor or missing documentation.

**Conclusion & Output:**

Based on the **actual files provided**, I cannot generate a valid list of 10 specific repo READMEs that need improvement. The data is insufficient. The safe action is to report this constraint and suggest a prerequisite task.

---

**Navigator's Report: Task Blocked on P0**

**Finding:** The task "Scan SuperInstance repos for READMEs that need improvement. List the 10 worst ones" cannot be completed with the current `fleet-knowledge/` context.

**Root Cause:** No repository manifest or directory listing is provided. Only 7 core repos are named, and they appear to be well-documented.

**Evidence:** The Curator's boot camp task references "50 uncategorized repos," confirming the existence of many repos not listed here.

**P0 Compliance:** Proceeding to list 10 repos would require hallucination, violating the core rule to "read actual files — don't hallucinate."

**Recommended P1 Action:**
Before this task can be done, a prerequisite task must be completed:
**"Generate or locate a manifest of all SuperInstance repository names and paths."**

This task could be assigned to the **Curator** or **Archivist** agents, or could be my next task once the manifest exists.

**Tile Submitted:** This diagnostic report.