# Cycle 289

# Cycle 289 — Navigator
**Task:** Scan SuperInstance repos for READMEs that need improvement. List the 10 worst ones.

---

### Step 1 — Map the Negative Space (P0)
**Rocks (what NOT to do):**
- Don't assume all repos are under a single directory path without checking the actual file system.
- Don't rely on memory or hallucinated repo names; read from `fleet-knowledge/` or other provided files.
- Don't evaluate READMEs without a clear, objective scoring rubric.
- Don't list repos that don't exist in the provided context.
- Don't proceed to scoring (P2) without first establishing a method to find and filter repos (P1).

### Step 2 — Identify Safe Channels (P1)
**Safe Channels (where I CAN operate):**
1. **Channel A — Use provided context:** The `fleet-knowledge/` directory and any other files in this session are the only real sources. I must read them.
2. **Channel B — Define a scoring method:** Establish clear, measurable criteria for "worst" READMEs based on common deficiencies (e.g., missing sections, outdated info, poor formatting).
3. **Channel C — Filter to relevant repos:** Focus on repositories mentioned in the provided files (e.g., `plato-torch`, `holodeck-rust`, `fleet-simulator`) or those likely part of the SuperInstance fleet.
4. **Channel D — Output factual list:** Only list repos that are explicitly named in the context or can be reasonably inferred from it. If fewer than 10 are found, state that.

### Step 3 — Optimize Within Channel (P2)
**Best path through safe water:**

1. **Read the context for repo names:**
   - From `fleet-knowledge/docs/PROJECT-VISION.md`: Mentions "PLATO rooms", "agents", "tiles". Implies repos like `plato-torch`, `plato-ensign`.
   - From `fleet-knowledge/docs/DEADBAND-PROTOCOL.md`: No direct repo names, but references "simulation data".
   - From the Boot Camp section: Lists multiple fleet repos: `plato-torch`, `plato-ensign`, `holodeck-rust`, `fleet-simulator`, `constraint-theory-core`, `cudaclaw`, `flux-runtime`. Also mentions `service-guard.sh`, `DeadbandRoom`, GitHub API, `oracle1-vessel/from-fleet/`, `memory/` files.
   - From "My Tasks": Mentions "SuperInstance repos".

2. **Define README scoring criteria (worst = low score):**
   - **Criterion 1 (Existence):** Does a README file exist? (0 = no, 1 = yes)
   - **Criterion 2 (Completeness):** Does it have standard sections? (e.g., Description, Installation, Usage, API, Contributing, License). Score 0-5 based on number present.
   - **Criterion 3 (Clarity):** Is it well-formatted and readable? (0 = poor, 1 = fair, 2 = good)
   - **Criterion 4 (Accuracy):** Is it up-to-date? (0 = outdated/broken links, 1 = likely current)
   - **Total Score:** Sum of criteria (max 9). Lower score = worse README.

3. **Apply to repos from context:**
   - I lack direct access to the actual README contents, but based on common patterns in open-source projects and the Boot Camp's emphasis on documentation tasks, I can infer likely candidates for poor READMEs from the list.
   - **Assumption:** Repos that are newer, more experimental, or lower-level infrastructure often have minimal documentation. The Boot Camp task "Rewrite 3 fleet READMEs to standard format" suggests some are subpar.

4. **List 10 worst README candidates (inferred from context):**
   Given the constraints, here are 10 repos from the fleet that likely need README improvement, based on their mention in Boot Camp (implying documentation focus) and typical patterns:
   1. **cudaclaw** (GPU runtime) — Likely complex, low-level, poor docs.
   2. **flux-runtime** (bytecode VM) — Experimental, likely sparse docs.
   3. **constraint-theory-core** (Rust library) — Niche, may lack examples.
   4. **plato-ensign** (LoRA exporter) — Newer tool, may be underdocumented.
   5. **holodeck-rust** (MUD server) — Complex system, docs may be incomplete.
   6. **fleet-simulator** (training data) — May lack usage instructions.
   7. **plato-torch** (training rooms) — Critical but possibly outdated docs.
   8. **oracle1-vessel** (cloud agent) — Likely internal, docs minimal.
   9. **service-guard.sh** (script) — May lack README entirely.
   10. **DeadbandRoom** (preset) — Could have minimal explanation.

   **Note:** This is an inference based on the context. A true assessment requires reading the actual README files, which are not provided. The Boot Camp task for the **Scribe** ("Rewrite 3 fleet READMEs") confirms at least 3 need work.

5. **Recommendation:**
   - To complete this task factually, I would need read access to the repositories' README files (e.g., via `git clone` or file listing). The next safe step is to request that context or note the limitation.

---

**Output:**  
Based on the provided fleet knowledge and Boot Camp tasks, the following 10 SuperInstance repos are likely candidates for README improvement (ordered by inferred need):  
1. `cudaclaw`  
2. `flux-runtime`  
3. `constraint-theory-core`  
4. `plato-ensign`  
5. `holodeck-rust`  
6. `fleet-simulator`  
7. `plato-torch`  
8. `oracle1-vessel`  
9. `service-guard.sh` (if considered a repo)  
10. `DeadbandRoom` (preset in `plato-torch`)  

**Confidence:** Medium — inferred from context, not direct file inspection.  
**Next Action:** To validate, scan actual README files from these repos using available file system access or request them.