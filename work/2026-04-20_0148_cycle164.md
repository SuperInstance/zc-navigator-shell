# Cycle 164

# Cycle 164 — Navigator
**Task:** Scan SuperInstance repos for READMEs that need improvement. List the 10 worst ones.

---

### Step 1 — Map the Negative Space (P0)
**Rocks (what NOT to do):**
- Don't assume all repos are under a single directory path without checking the actual file system.
- Don't rely on memory or hallucinated repo names; read from the actual `fleet-knowledge/` index or git structure.
- Don't evaluate READMEs based on subjective "badness"; need objective criteria (missing sections, outdated info, broken links, no examples).
- Don't list repos that don't exist in the current fleet.
- Don't proceed to listing without first verifying access to repo contents.

### Step 2 — Identify Safe Channels (P1)
**Safe Channels (where I CAN operate):**
1. Use the existing `fleet-knowledge/` directory tree to locate repository references.
2. Check for a manifest or index file that lists fleet repos (e.g., `docs/`, `memory/`, `repos/`).
3. If direct README files are present in `fleet-knowledge/`, evaluate those.
4. If only metadata exists, note repos that lack READMEs entirely as candidates.
5. Apply consistent criteria: README must have at least: Title, Description, Installation/Usage, clear Purpose. Missing >2 sections qualifies as "needs improvement".
6. Limit to repositories mentioned in the provided context (PLATO, holodeck, fleet-simulator, etc.) to stay factual.

### Step 3 — Optimize Within Channel (P2)
**Action Plan:**
1. Scan `fleet-knowledge/` for any README.md files or references to repositories.
2. From the context docs, extract repo names: `plato-torch`, `plato-ensign`, `holodeck-rust`, `fleet-simulator`, `constraint-theory-core`, `cudaclaw`, `flux-runtime`.
3. Assess each based on available data in the knowledge base.
4. Rank by completeness against criteria.
5. Output list of 10 worst, or all that qualify if fewer than 10.

---

### Execution

**Available READMEs in fleet-knowledge/ context:**
- `fleet-knowledge/docs/PROJECT-VISION.md` – Not a repo README.
- `fleet-knowledge/docs/DEADBAND-PROTOCOL.md` – Not a repo README.
- `fleet-knowledge/docs/` contains no other README files.
- No `repos/` subdirectory is present in the provided file tree.

**Repo names inferred from context:**
1. `plato-torch`
2. `plato-ensign`
3. `holodeck-rust`
4. `fleet-simulator`
5. `constraint-theory-core`
6. `cudaclaw`
7. `flux-runtime`

**Assessment:**
Since README files are not present in the provided knowledge slice, I cannot evaluate their content. However, based on the **Boot Camp** instructions, several repos have associated tasks:
- **plato-torch**: Phase 1: "Read DeadbandRoom preset." Phase 3: "Write 10 edge-case tests for DeadbandRoom." Implies a README exists but may be minimal.
- **holodeck-rust**: Phase 1: "Read 3 rabbit-trail research docs." Not directly about README.
- **fleet-simulator**: Phase 1: "Read fleet-simulator README." Explicitly mentions a README.

Given the task is to "list the 10 worst ones," but only 7 repos are named and no READMEs are accessible, the **safe channel** is to report the factual state:

**Finding:** README files are not present in the provided `fleet-knowledge/` directory. Therefore, I cannot scan or rank them. The task cannot be completed with the current information.

**Alternative Safe Output:**
List repositories that are most likely to need README improvement based on context clues:

1. **plato-ensign** – Newer component, may lack documentation.
2. **constraint-theory-core** – Niche technical library, often under-documented.
3. **cudaclaw** – GPU runtime, complex setup likely poorly documented.
4. **flux-runtime** – Experimental bytecode VM, README may be sparse.
5. **fleet-simulator** – README exists but may be outdated (referenced in boot camp).
6. **holodeck-rust** – MUD server, may have incomplete usage examples.
7. **plato-torch** – Has presets but README may lack examples.

*(List truncated to 7 due to available repo names.)*

**Recommendation:** To complete this task accurately, the Navigator requires access to the actual repository directories or a `repos/` subfolder within `fleet-knowledge/` containing README files.

---

**Tile Submitted:** Factual status report with constrained assessment based on available data.