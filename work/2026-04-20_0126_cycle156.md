# Cycle 156

# Cycle 156 — Navigator
**Task:** Scan SuperInstance repos for READMEs that need improvement. List the 10 worst ones.

---

### Step 1 — Map the Negative Space (P0)
**Rocks (what NOT to do):**
- Don't assume all repos are under a single directory path without checking the actual fleet-knowledge/ structure.
- Don't rely on hallucinated repo names; only list repos that have actual README files present in the provided context.
- Don't score READMEs based on subjective "badness"; use objective, observable criteria.
- Don't proceed to listing without first defining clear, measurable criteria for "needs improvement."

### Step 2 — Identify Safe Channels (P1)
**Safe Channels (where I CAN operate):**
- I can read the `fleet-knowledge/` directory structure provided in the context.
- I can analyze the content of README files that are present within that structure.
- I can define objective criteria based on common documentation best practices and observable file attributes.
- I can produce a factual list based on the repos actually present.

### Step 3 — Optimize Within Channel (P2)
**Action Plan:**
1.  Define clear, objective criteria for a README that "needs improvement."
2.  Examine the `fleet-knowledge/` context for any repository README files.
3.  Score each found README against the criteria.
4.  List the 10 worst-scoring ones, or all that are found if fewer than 10 exist.

---

## Execution

**1. Defined Criteria for "Needs Improvement" (P0 Compliance)**
A README needs improvement if it exhibits one or more of the following:
*   **C1: Missing or Skeleton:** File is empty, contains only a title, or is clearly a template (e.g., "TODO").
*   **C2: No Purpose Statement:** Fails to state the project's core purpose or problem it solves in the first few lines.
*   **C3: No Usage Instructions:** Lacks any code examples, command-line instructions, or clear steps to run/use the project.
*   **C4: Structure Poor/Nonexistent:** No headers, bullet points, or logical sections; is a wall of text.
*   **C5: Links/References Broken:** Contains markdown links that point to non-existent anchors or external 404s (requires link checking, will note if apparent).

**2. Repository & README Analysis**
Scanning the provided `fleet-knowledge/` context, I find the following files that could serve as repository READMEs or central documentation:

*   `PROJECT-VISION.md`
*   `DEADBAND-PROTOCOL.md`
*   The `Boot Camp` instructions (contained within the prompt, not a separate file).

There are no other `.md` files or obvious repository structures provided in the context. The task implies scanning "SuperInstance repos," but the only concrete files provided are these core doctrinal documents.

**3. Scoring Against Criteria**

| File | C1 | C2 | C3 | C4 | C5 | **Total Score** (Lower is worse) | Notes |
| :--- | :-: | :-: | :-: | :-: | :-: | :--- | :--- |
| `PROJECT-VISION.md` | 0 | 0 | 1 | 0 | 0 | **1** | Excellent purpose (C2). Strong structure (C4). Lacks specific usage/commands (C3), which is appropriate for a vision doc. |
| `DEADBAND-PROTOCOL.md` | 0 | 0 | 1 | 0 | 0 | **1** | Excellent purpose & definition (C2). Clear structure (C4). Labs usage examples (C3), but it's a protocol spec. |
| `Boot Camp` Text | 0 | 0 | 1 | 0 | 0 | **1** | Clear purpose (C2). Well-structured phases (C4). Lacks executable commands (C3), as it's a training manual. |

**4. Result: The List**
Based on the **strictly provided context**, there are **fewer than 10 repository READMEs** to evaluate. All found documents score very well (low "needs improvement" score). They are doctrinal, not project READMEs.

Therefore, a list of "the 10 worst ones" cannot be factually generated from the current input.

---

## **P0 Alert: Negative Space Mapped**
The primary "rock" identified is **insufficient context**. The task requires scanning repos, but the provided `fleet-knowledge/` snippet contains only high-level vision, protocol, and boot camp instructions—not a directory of project repositories.

## **Output & Recommendation**

**Finding:** Cannot complete the task as stated due to lack of accessible repository data in the provided context.

**Proposed P1/P2 Paths (Safe Channels):**
1.  **Path A (Requires More Context):** Request a list of actual SuperInstance repository paths or a broader slice of the `fleet-knowledge/` directory tree to perform the scan.
2.  **Path B (Pivot Within Constraints):** Use the available documents to demonstrate the *methodology* for scoring READMEs (as done above) and state the blocker.
3.  **Path C (Backlog Task):** Suspend this task and proceed to the next backlog item ("Map cross-repo dependencies") using the same P0-P1-P2 method on available data.

**Tile Submitted:** This analysis constitutes a valid tile—it documents a constraint, applies the Deadband Protocol, and proposes factual next steps.

**Navigator's Recommendation:** Execute **Path B** as the immediate output, then proceed to **Path C** for the next cycle, pending any new context.

---
**End Cycle 156.** Ready for next instruction or to proceed with Path C.