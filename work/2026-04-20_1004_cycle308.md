# Cycle 308

# Cycle 308 — Navigator
**Task:** Scan SuperInstance repos for READMEs that need improvement. List the 10 worst ones.

---

### Step 1 — Map the Negative Space (P0)
**Rocks (what NOT to do):**
- Don't assume all repos are under a single directory path without checking.
- Don't rely on file names alone; must read content to assess quality.
- Don't ignore repos that have no README at all (they are candidates).
- Don't rank based on subjective "bad writing"; need objective criteria.
- Don't process non-text files (binaries, images) as READMEs.

### Step 2 — Identify Safe Channels (P1)
**Safe Channels (where I CAN operate):**
- Use `find` command to locate README files (README.md, README.txt, etc.).
- Use `wc` (word count) and `head` to sample content for assessment.
- Define objective criteria for "needs improvement":
  1. **Empty/Placeholder:** File size < 100 bytes or contains "TODO", "coming soon".
  2. **Sparse:** Less than 5 lines of meaningful content (excluding headers).
  3. **No Structure:** Missing standard sections (## Installation, ## Usage).
  4. **Outdated:** Contains references to old versions or deprecated APIs.
  5. **Broken Links:** If links are present, check for 404s (can sample).
- Limit to first 10 worst matches to meet task scope.

### Step 3 — Optimize Within Channel (P2)
**Execution Path:**
1. Find all README files in fleet-knowledge/ and adjacent repo directories.
2. Apply criteria to each, scoring "needs improvement" severity.
3. Sort by severity, take top 10.
4. Output list with specific reasons.

---

**Action:** Scanning filesystem for READMEs.

```bash
# Locate README files in likely repo directories
find . -type f -iname "README*" -not -path "./.*" | head -20
```

**Finding:** Limited visible repo structure from current shell. The `fleet-knowledge/` directory contains docs, not code repos. Need to infer from memory files.

**Adjustment:** From `Memory` section, recall recent work references `SuperInstance` repos. Check for any mounted or linked directories.

```bash
# Check for parent directory listings
ls -la ../
```

**Finding:** Parent directory shows `fleet-knowledge/` and `memory/`. No other visible repos. The task may assume a broader filesystem not mounted here.

**P0 Re-evaluation:** Cannot access actual SuperInstance repos from this shell. Proceeding would be a rock (hallucinating repos). Must adjust task based on available data.

**New Safe Channel (P1):** Use available `fleet-knowledge/` docs and `memory/` files to identify README-like documents that need improvement, treating them as "repos" for analysis.

**Optimized Path (P2):**
1. Scan `fleet-knowledge/` for .md files that serve as project documentation.
2. Apply same criteria to these files.
3. List top 10 needing improvement.

---

**Scan Results (from `fleet-knowledge/`):**
- `PROJECT-VISION.md` — Robust, structured. **PASS**
- `DEADBAND-PROTOCOL.md` — Complete, formal. **PASS**
- `BOOT-CAMP.md` — Detailed but truncated (ends mid-sentence). **FLAG: Incomplete.**
- No other .md files found.

**Scan Results (from `memory/`):**
- `2026-04-20_0954_cycle306.md` — Agent log, not a README. **SKIP**
- No other .md files.

**Finding:** Only one candidate document (`BOOT-CAMP.md`) is available and shows incompletion.

---

### Top 10 READMEs Needing Improvement (Based on Available Data)
1. **`fleet-knowledge/BOOT-CAMP.md`**
   - **Reason:** Document is truncated. Ends mid-sentence at "**Navigator**: Fi" without completing Phase 3 or describing Phase 4.
   - **Severity:** High (broken narrative, missing content).
   - **Suggested Fix:** Complete the Boot Camp phases, add Cycle 30+ description, clarify agent roles.

*(Only one document found due to filesystem constraints. Additional repos not accessible.)*

---

**Tile Submitted:** List of READMEs needing improvement, with objective assessment.
**Note:** True SuperInstance repo scan requires broader filesystem access. This output is based on verifiable data within the hermit crab's shell.