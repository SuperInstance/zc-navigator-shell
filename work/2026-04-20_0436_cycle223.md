# Cycle 223

# Cycle 223 — Navigator
**Task:** Scan SuperInstance repos for READMEs that need improvement. List the 10 worst ones.

---

### Step 1 — Map the Negative Space (P0)
**Rocks (what NOT to do):**
- Don't assume all repos are under a single directory path without checking the actual file system.
- Don't rely on memory or hallucinated repo names; read from the actual `fleet-knowledge/` directory.
- Don't evaluate READMEs based on subjective "badness"; define objective, measurable criteria first.
- Don't skip repos that are part of the SuperInstance ecosystem but might be in nested or unexpected locations.
- Don't produce a list without providing actionable, specific reasons for each selection.

### Step 2 — Identify Safe Channels (P1)
**Safe Channels (where I CAN operate):**
- I can read the real file system from the provided `fleet-knowledge/` context.
- I can define clear, objective criteria for a "README that needs improvement" based on common documentation standards and fleet patterns.
- I can systematically examine repository directories and their README files.
- I can produce a ranked list based on the defined criteria, with explicit justifications.
- I can limit the scope to repositories directly referenced in the fleet knowledge (e.g., `plato-torch`, `holodeck-rust`) to ensure factual accuracy.

### Step 3 — Optimize Within Channel (P2)
**Optimal Path:**
1.  **Define Criteria:** Establish a simple scoring system (0-2 points per criterion, lower score = worse).
    *   **Existence (2 pts):** Does a README.md file exist in the repo root? (No=0, Yes=2)
    *   **Content Minimum (2 pts):** Does it have at least a project description and basic usage/installation instructions? (Minimal/None=0, Partial=1, Good=2)
    *   **Structure (2 pts):** Is it formatted with clear headers (e.g., `##`, `###`)? (Poor/None=0, Basic=1, Good=2)
    *   **Links (1 pt):** Does it contain links to related repos, documentation, or issues? (No=0, Yes=1)
    *   **Max Score:** 7 points.
2.  **Survey Repositories:** Examine the READMEs for key fleet repos mentioned in the context.
3.  **Score & Rank:** Apply the criteria, calculate scores, and list the 10 lowest-scoring repos.
4.  **Justify:** Provide a brief, specific reason for each repo's low score.

---

### Execution & Output

**Surveyed Repositories (from fleet-knowledge context):**
1.  `plato-torch`
2.  `plato-ensign`
3.  `holodeck-rust`
4.  `fleet-simulator`
5.  `constraint-theory-core`
6.  `cudaclaw`
7.  `flux-runtime`

*(Note: The `fleet-knowledge/` directory provided in the context does not contain the full file trees for these repos, only documentation snippets. Therefore, I cannot read the actual README files. This is a P0 rock: I cannot factually score files that are not present. I must operate within the safe channel of what IS provided.)*

**Safe Channel Adjustment:** I will analyze the **documentation quality as presented in the context snippets** for these repos, treating the provided snippets as indicative of their documentation state. I will also note the absence of a visible README as a major negative factor.

**Ranking of Repositories Needing README Improvement (Based on Available Evidence):**

1.  **`cudaclaw`** (Score: ~0/7)
    *   **Reason:** Only mentioned by name ("GPU-resident agent runtime"). No README snippet or details provided in context. High-priority core technology with no visible documentation.

2.  **`flux-runtime`** (Score: ~0/7)
    *   **Reason:** Only mentioned by name ("Self-assembling bytecode VM"). No README snippet or details provided. Critical low-level component without explained purpose.

3.  **`plato-ensign`** (Score: ~1/7)
    *   **Reason:** Defined only as "Exports room experience into LoRA adapters." Lacks installation, usage, or integration details with `plato-torch`.

4.  **`constraint-theory-core`** (Score: ~1/7)
    *   **Reason:** Defined only as "Rust geometric snapping." No explanation of the theory, API, or problem it solves.

5.  **`fleet-simulator`** (Score: ~2/7)
    *   **Reason:** Mentioned as "Fleet simulation for training data." Lacks purpose, architecture, or how it connects to PLATO rooms.

6.  **`holodeck-rust`** (Score: ~3/7)
    *   **Reason:** Slightly better ("MUD server with sentiment-aware NPCs"). Gives a high-concept purpose but no technical details, setup, or protocol.

7.  **`plato-torch`** (Score: ~4/7)
    *   **Reason:** Most frequently referenced ("25 training room presets, self-training rooms"). Implies substance, but the provided context lacks a structured README explaining how to use the presets or run a room.

**(List ends at 7)** The context only provides definitive information on 7 core repos. To reach a "top 10," I would need to scan the actual SuperInstance GitHub organization, which is not within the current safe channel of provided files.

### Recommendation (P2 Optimization within Constraint)
The most useful output within the given constraints is to **flag the 7 core repos and recommend a systematic documentation audit**. The worst are `cudaclaw` and `flux-runtime` due to complete documentation absence in the context. The immediate next action (a new tile) should be to clone these repos and perform a real README analysis using the defined scoring system.

**Tile Submitted:** Analysis of documentation gaps for 7 core fleet repos, with a methodology for scoring README quality.