# Cycle 403

# Cycle 403 — Navigator
**Task:** Scan SuperInstance repos for READMEs that need improvement. List the 10 worst ones.

---

### Step 1 — Map the Negative Space (P0)
**Rocks (what NOT to do):**
- Don't assume all repos are under a single directory path without checking the actual fleet-knowledge/ file structure.
- Don't hallucinate repo names or README content; read only from provided files.
- Don't rank based on subjective "badness" without defined, objective criteria.
- Don't proceed to listing without first establishing a method to scan and evaluate READMEs from the available data.

### Step 2 — Identify Safe Channels (P1)
**Safe Channels (where I CAN operate):**
1. Use the `fleet-knowledge/` file tree provided in the context to locate repository references.
2. Extract README filenames or references from those files.
3. Define objective criteria for "needs improvement" based on common README anti-patterns observable in the fleet's own documentation standards.
4. Evaluate only the READMEs I can actually see content for in the context.
5. Output a factual list based on the evaluation.

### Step 3 — Optimize Within Channel (P2)
**Action Plan:**
1. **Scan for Repos & READMEs:** Parse the provided fleet-knowledge files for repository names and README mentions.
2. **Define Criteria:** Establish clear, binary flags for a "poor" README.
3. **Evaluate Available READMEs:** Apply criteria to the README content I can access.
4. **Rank & List:** Sort by number of criteria failed and list the top 10.

---

### Execution

#### 1. Scan for Repos & READMEs
From the provided `fleet-knowledge/docs/BOOT-CAMP.md`, I see references to the following fleet repos:
- `plato-torch`
- `plato-ensign`
- `holodeck-rust`
- `fleet-simulator`
- `constraint-theory-core`
- `cudaclaw`
- `flux-runtime`

The `BOOT-CAMP.md` also references `service-guard.sh`, `TOOLS.md`, and files in `oracle1-vessel/from-fleet/` and `memory/`, but these are not necessarily repos.

The `PROJECT-VISION.md` and `DEADBAND-PROTOCOL.md` are core docs, not repo READMEs.

**Available README Content in Context:**
I have **no direct file content** for any of the listed repository READMEs in the provided context. The context only includes the three documentation files (`PROJECT-VISION.md`, `DEADBAND-PROTOCOL.md`, `BOOT-CAMP.md`) and my previous cycle log.

#### 2. Define "Needs Improvement" Criteria
Based on the fleet's emphasis on clear documentation (e.g., the Scribe's boot camp task to rewrite READMEs to a standard format), I'll define a poor README as one likely missing:
- **C1: Clear Purpose Statement** (What is this repo for?)
- **C2: Installation/Setup Instructions**
- **C3: Usage Examples**
- **C4: Link to Main Project Documentation** (e.g., linking to `PROJECT-VISION.md`)
- **C5: Link to Other Fleet Repos** (showing integration awareness)

Since I cannot read the actual READMEs, I **cannot evaluate them**. The safe channel (P1) dictates I must work with available data.

#### 3. Pivot Based on Available Data
The only README-like content I have is for the **docs themselves**. I can evaluate the `BOOT-CAMP.md` file as if it were a README for the boot camp process.

**Evaluation of `BOOT-CAMP.md`:**
- **C1 (Purpose):** ✅ Clear. It's a boot camp guide.
- **C2 (Setup):** ❌ No setup instructions. It assumes an agent is already in the system.
- **C3 (Usage):** ✅ Has clear phases and tasks.
- **C4 (Link to Main Docs):** ❌ Does not link to `PROJECT-VISION.md` or `DEADBAND-PROTOCOL.md`.
- **C5 (Link to Other Repos):** ✅ Mentions multiple fleet repos by name.

**Score:** 2 out of 5 criteria failed.

---

### Output: List of READMEs That Need Improvement

**Finding:** Based **solely on the data provided in this session**, I cannot scan or list the READMEs for the SuperInstance repos. The context does not contain those repository files.

**However, following the Deadband Protocol (P0), I have identified the "rock":** Attempting to evaluate files that are not present.

**Safe Channel (P1) Output:** I can report the **methodology** that *would* be used and state the **blocking dependency**.

1.  **Methodology:** To complete this task, a Navigator agent requires read access to the `README.md` (or similar) file at the root of each repository in the fleet (e.g., `plato-torch/README.md`, `holodeck-rust/README.md`).
2.  **Blocking Dependency:** The task cannot be completed with the current context. The necessary files are not in the `fleet-knowledge/` snippet provided.

**Recommendation (P2 within the safe channel):** The task should be updated to reflect available data, or the context should be expanded to include the repository README files. As a fallback, I have evaluated the one instructional document (`BOOT-CAMP.md`) against the criteria, finding it lacks setup instructions and links to core doctrine.

**Tile Generated:** This analysis itself is a valid tile—it maps a constraint (missing data) and defines the safe path for obtaining the necessary information.