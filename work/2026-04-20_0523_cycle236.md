# Cycle 236

# Cycle 236 — Navigator
**Task:** Scan SuperInstance repos for READMEs that need improvement. List the 10 worst ones.

---

### Step 1 — Map the Negative Space (P0)
**Rocks (what NOT to do):**
- Don't assume all repos are under a single directory path without checking the actual fleet-knowledge/ structure.
- Don't rely on memory or hallucination; must read actual files.
- Don't evaluate READMEs without a clear, objective scoring rubric.
- Don't list repos that don't exist in the provided context.
- Don't proceed to scoring (P2) without first establishing a method to find repos (P1).

### Step 2 — Identify Safe Channels (P1)
**Safe Channels (where I CAN operate):**
1. Use the `fleet-knowledge/` directory as the sole source of truth.
2. Develop a simple, repeatable method to locate README files.
3. Define clear, objective criteria for "needs improvement" based on common deficiencies.
4. Only analyze repos/files that are present and readable within the provided context.

**Method Established:**
1. **Locate Repos:** Scan the provided context for any file paths or references to repositories. The primary source is the `fleet-knowledge/` docs.
2. **Extract READMEs:** Identify files named `README.md` or equivalent documentation.
3. **Scoring Rubric:** A README "needs improvement" if it exhibits one or more of the following:
    - **Missing:** No README file is present for a core project.
    - **Skeleton:** Contains only a title or placeholder text (e.g., "# Project Name").
    - **Incomplete:** Lacks essential sections (Purpose, Installation, Usage, API).
    - **Outdated:** Contains references to broken links, deprecated APIs, or old version numbers.
    - **Unclear:** Fails to explain what the project does or how to use it in plain terms.
4. **Rank:** Sort by severity (Missing > Skeleton > Incomplete > Outdated > Unclear).

### Step 3 — Optimize Within Channel (P2)
**Execution:**
I will now scan the provided `fleet-knowledge/` context for repository references and READMEs.

**Findings from Context:**
The provided context includes several files from `fleet-knowledge/docs/` and a state file. It references multiple project names but does not contain the full file trees for those repos. Therefore, I cannot directly read their READMEs. However, I can infer from the context which projects are mentioned and note which ones lack *visible* documentation.

**List of Projects Mentioned in Context:**
1.  **plato-torch** (Mentioned in Key Technologies, Architecture, Boot Camp)
2.  **plato-ensign** (Mentioned in Key Technologies, Architecture)
3.  **holodeck-rust** (Mentioned in Key Technologies, Boot Camp)
4.  **fleet-simulator** (Mentioned in Key Technologies, Boot Camp)
5.  **constraint-theory-core** (Mentioned in Key Technologies)
6.  **cudaclaw** (Mentioned in Key Technologies)
7.  **flux-runtime** (Mentioned in Key Technologies)
8.  **oracle1-vessel** (Mentioned in Boot Camp - Herald's task)
9.  **SuperInstance** (The overarching project in PROJECT-VISION.md)

**Analysis of Visible Documentation:**
*   `PROJECT-VISION.md`: Exists and is detailed.
*   `DEADBAND-PROTOCOL.md`: Exists and is detailed.
*   The `Boot Camp` instructions are present.
*   **No README.md files for the individual technical repos (1-8) are provided in the context.**

**Conclusion & List:**
Based on the **"Missing"** criterion in my rubric, the following projects are the "worst" in terms of README improvement because their documentation is not visible in the provided knowledge base. This list is constrained by the safe channel of available data.

**The 10 Worst READMEs (by inference of absence):**
1.  **plato-torch** - Core training system. No README visible.
2.  **plato-ensign** - LoRA export system. No README visible.
3.  **holodeck-rust** - MUD server. No README visible.
4.  **fleet-simulator** - Training data generator. No README visible.
5.  **constraint-theory-core** - Geometric snapping library. No README visible.
6.  **cudaclaw** - GPU runtime. No README visible.
7.  **flux-runtime** - Bytecode VM. No README visible.
8.  **oracle1-vessel** - Cloud agent vessel. No README visible.
9.  *(Reserved for a 9th repo not mentioned in context)*
10. *(Reserved for a 10th repo not mentioned in context)*

**Recommendation (P2 Optimization):**
To complete this task accurately, the Navigator agent requires read access to the actual repository directories (e.g., `../plato-torch/README.md`). The next safe action is to request this access or clarify the scope. The most useful output with current constraints is this analysis and the specific list of core projects whose documentation is not immediately available for review.

**Tile Submitted:** Analysis of README visibility for core fleet projects.