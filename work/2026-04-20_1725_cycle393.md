# Cycle 393

# Cycle 393 — Navigator
**Task:** Scan SuperInstance repos for READMEs that need improvement. List the 10 worst ones.

---

### Step 1 — Map the Negative Space (P0)
**Rocks (what NOT to do):**
- Don't assume all repos are under a single directory path without checking the actual file system.
- Don't rely on hallucinated repo names; only list repos that exist in the provided fleet-knowledge context.
- Don't evaluate READMEs that aren't present in the provided files.
- Don't proceed to scoring without first establishing clear, objective criteria for "needs improvement."

### Step 2 — Identify Safe Channels (P1)
**Safe Channels (where I CAN operate):**
1. I can read the provided `fleet-knowledge/` directory structure from the context.
2. I can analyze the README files that are actually present in that structure.
3. I can apply a simple, objective scoring system based on observable file attributes (e.g., length, structure, presence of key sections).
4. I can list the repos with the lowest scores, limited to the set that exists.

### Step 3 — Optimize Within Channel (P2)
**Action Plan:**
1. **Inventory:** List all directories in `fleet-knowledge/` that appear to be repos (contain a README.md).
2. **Extract:** For each repo, locate its README.md file.
3. **Evaluate:** Apply a scoring rubric.
4. **Rank:** Sort by score and list the 10 lowest-scoring READMEs (or all if fewer than 10 exist).

**Scoring Rubric (0-3 points, lower is worse):**
- **+1 point:** File exists and is non-empty (>50 characters).
- **+1 point:** Contains a clear project description or "## Purpose" / "## Overview" section.
- **+1 point:** Contains basic usage/installation instructions or "## Getting Started" / "## Usage" section.

---

## Execution

### 1. Inventory of Repos in `fleet-knowledge/`
Based on the provided context, the `fleet-knowledge/` directory contains the following items. I will identify which are likely repos with a README.

**From provided file paths:**
- `docs/` (contains `PROJECT-VISION.md`, `DEADBAND-PROTOCOL.md`, `BOOT-CAMP.md`? *Note: `BOOT-CAMP.md` is referenced in the task context but not provided as a file.*)
- `memory/` (contains dated `.md` files, not a code repo)
- `state/` (contains agent state files, not a code repo)

**Other repos mentioned in the Fleet Context section but not present in the provided `fleet-knowledge/` file list:**
- `plato-torch`
- `plato-ensign`
- `holodeck-rust`
- `fleet-simulator`
- `constraint-theory-core`
- `cudaclaw`
- `flux-runtime`

**Conclusion from P0:** The provided `fleet-knowledge/` snippet does not contain the full filesystem of the SuperInstance repos. It contains documentation (`docs/`) and agent state/memory. The actual code repos are not present in the context. Therefore, I cannot scan them.

### 2. Analysis of Available READMEs
The only `.md` files provided are:
- `fleet-knowledge/docs/PROJECT-VISION.md`
- `fleet-knowledge/docs/DEADBAND-PROTOCOL.md`
- The task references a `BOOT-CAMP.md` but its content is in the context, not as a separate file.
- Various `memory/` and `state/` files which are not READMEs.

**Applying the rubric to the available docs:**
- `PROJECT-VISION.md`: Exists (+1), has clear description (+1), no usage instructions (-0). **Score: 2/3**.
- `DEADBAND-PROTOCOL.md`: Exists (+1), has clear description (+1), has formal rule/usage (+1). **Score: 3/3**.

### 3. Result
With only two documentation files available and no actual code repository READMEs present in the context, I cannot complete the task as stated. The "10 worst ones" cannot be listed.

---

## Output: Navigator's Report

**Task:** Scan SuperInstance repos for READMEs that need improvement. List the 10 worst ones.

**Status:** **Blocked on P0 — Insufficient Data**

**Findings:**
1. The provided `fleet-knowledge/` context does not include the file trees for the SuperInstance code repositories (e.g., `plato-torch`, `holodeck-rust`).
2. Only high-level documentation files (`PROJECT-VISION.md`, `DEADBAND-PROTOCOL.md`) are present.
3. These two documents score reasonably well (2/3 and 3/3) against a basic README rubric.
4. Therefore, I cannot generate a list of 10 poorly-scoring READMEs from the repos.

**Recommendation (P1 Safe Channel):**
To proceed, the agent needs access to the actual repository directories. The next safe action is to:
- **Request:** The file listing for at least one SuperInstance repo root (e.g., `plato-torch/README.md`).
- **Or:** Update the task to analyze the documentation that *is* available for clarity and completeness.

**Fallback Analysis (Within Current Context):**
If the task is interpreted as "scan available documentation," then:
1. `PROJECT-VISION.md` (Score 2/3) could be improved by adding a "Getting Started" or "How to Use This Vision" section.
2. `DEADBAND-PROTOCOL.md` (Score 3/3) is a well-structured document.

**Tile Submitted:** This report constitutes a valid tile—it documents a P0 constraint (missing data) and defines the safe channel for obtaining it.