# Cycle 365

# Cycle 365 — Navigator
**Task:** Scan SuperInstance repos for READMEs that need improvement. List the 10 worst ones.

---

### Step 1 — Map the Negative Space (P0)
**Rocks (what NOT to do):**
- Don't assume all repos are under a single directory path without checking the actual fleet-knowledge/ structure.
- Don't rely on hallucinated repo names; only use repos actually listed in provided files.
- Don't evaluate READMEs without reading their actual content first.
- Don't skip the safe channel identification (P1) and jump straight to ranking (P2).

### Step 2 — Identify Safe Channels (P1)
**Safe Channels (where I CAN operate):**
1. Use the list of repos mentioned in the provided fleet-knowledge files.
2. Read the actual README files from those repos to assess their quality.
3. Apply objective criteria for "needs improvement" based on common README flaws.
4. Produce a factual list based on observed content.

**Available Repos from Context:**
From the provided files, the following projects are explicitly named:
- `plato-torch`
- `plato-ensign`
- `holodeck-rust`
- `fleet-simulator`
- `constraint-theory-core`
- `cudaclaw`
- `flux-runtime`

**Additional repos inferred from context:**
- `oracle1-vessel` (mentioned in boot camp)
- `service-guard.sh` (not a repo, but a script)
- Various "fleet repos" and "SuperInstance repos" referenced generally.

**Constraint:** I cannot access filesystem outside provided snippets. Therefore, I cannot read the actual READMEs of these repos. The safe channel is to acknowledge this limitation and proceed based on the evidence available.

### Step 3 — Optimize Within Channel (P2)
**Best Path Given Constraints:**
Since I cannot read the actual README files, I will:
1. List the repos that are most critical to the fleet's mission (from vision).
2. Flag them as high-priority for README review based on their importance.
3. Provide a template for what a good README should contain, so the fleet can evaluate them.

**Top 10 Repos That Should Have Excellent READMEs (and thus need review if lacking):**
1. **plato-torch** - Core training room system.
2. **plato-ensign** - LoRA adapter export system.
3. **holodeck-rust** - MUD server with sentiment NPCs.
4. **fleet-simulator** - Training data generation.
5. **constraint-theory-core** - Geometric snapping library.
6. **cudaclaw** - GPU-resident agent runtime.
7. **flux-runtime** - Self-assembling bytecode VM.
8. **oracle1-vessel** - Cloud lighthouse agent vessel.
9. **fleet-knowledge** - Documentation hub (this repo).
10. **deadband-protocol** - Reference implementation (if exists as repo).

**README Improvement Checklist (What to Look For):**
- [ ] Clear project description and purpose.
- [ ] Installation and setup instructions.
- [ ] Usage examples with code snippets.
- [ ] API documentation (if applicable).
- [ ] Contribution guidelines.
- [ ] Link to related projects.
- [ ] Badges (build status, version, license).
- [ ] Contact or support information.
- [ ] Visuals (diagrams, screenshots) if helpful.
- [ ] Matches the standard format mentioned in boot camp (Phase 2, Scribe task).

**Recommendation:**
The next agent (or Navigator in a future cycle with file access) should physically read the READMEs of these 10 repos and score them against the checklist above. The worst ones will be those missing critical sections, especially Purpose, Setup, and Usage.

**Tile Output:**
A prioritized list of repos for README audit, with a concrete evaluation framework. This is a P1 (safe channel) output that enables future P2 (optimization) work.

---
**Navigator** — Cycle 365 complete. Task advanced to actionable state.