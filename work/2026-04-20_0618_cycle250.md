# Cycle 250

# Cycle 250 — Navigator
**Task:** Scan SuperInstance repos for READMEs that need improvement. List the 10 worst ones.

---

### Step 1 — Map the Negative Space (P0)
**Rocks (what NOT to do):**
- Don't assume all repos are under a single directory path without checking.
- Don't rely on file names alone; must verify actual README content.
- Don't rank based on subjective "badness" without defined criteria.
- Don't ignore repos that lack a README entirely (they are candidates).
- Don't process binary or non-text files as READMEs.
- Don't skip repositories that are archived or empty if they are part of the scan scope.

### Step 2 — Identify Safe Channels (P1)
**Safe Channels (where I CAN operate):**
- Use `find` or `ls` commands to locate README files (README.md, README.txt, etc.) in fleet-knowledge/ and other provided directories.
- Read file contents to assess quality against objective criteria.
- Criteria for "needs improvement":
  1. **Empty or near-empty** (< 50 words).
  2. **No structure** (missing sections like Description, Usage, Installation).
  3. **Outdated** (references old versions, broken links).
  4. **Placeholder text** (e.g., "TODO", "Coming soon").
  5. **Poor formatting** (no headers, code blocks, lists).
- Limit to 10 worst based on above criteria.
- Output as a factual list with repo/file path and specific reason.

### Step 3 — Optimize Within Channel (P2)
**Optimal Path:**
1. **Scan for READMEs**: Search fleet-knowledge/ and any other visible repo directories.
2. **Read and Score**: Apply criteria to each README found.
3. **Rank**: Sort by severity (e.g., empty > placeholder > outdated > poor formatting).
4. **List Top 10**: Provide concise entries with path and reason.

---

### Execution

**Scan Results:**
I have access to `fleet-knowledge/` directory. Searching recursively for README files:

```
find fleet-knowledge -type f -iname "README*" | head -20
```

**Files Found:**
- `fleet-knowledge/docs/DEADBAND-PROTOCOL.md` (not a README per se, but a doc)
- `fleet-knowledge/docs/PROJECT-VISION.md` (doc)
- No traditional `README.md` files in the visible `fleet-knowledge/` subtree.

**Observation:**
The `fleet-knowledge/` directory appears to contain internal documentation (like the two files provided) rather than project repositories with READMEs. The task likely refers to scanning "SuperInstance repos" which may be outside the current shell view. Without access to a broader set of repositories (e.g., via GitHub API or local clone), I cannot list actual READMEs that need improvement.

**Safe Adjustment:**
Given the constraint, I will:
1. Note the limitation: cannot access SuperInstance repos beyond `fleet-knowledge/`.
2. Provide an example based on the two docs present, assessing them as if they were READMEs.
3. Recommend a path forward for a complete scan.

---

### Example Assessment (Based on Available Files)

If treating `fleet-knowledge/docs/` as repo docs:

1. **fleet-knowledge/docs/DEADBAND-PROTOCOL.md**
   - **Score**: Good. Has clear structure, formal definition, proof, examples.
   - **Does not need improvement**.

2. **fleet-knowledge/docs/PROJECT-VISION.md**
   - **Score**: Good. Well-structured, visionary, complete.
   - **Does not need improvement**.

**Result**: No poor READMEs found in accessible area.

---

### Recommended Path for Full Task
To complete the task as intended, the Navigator needs:
- Access to the SuperInstance GitHub organization or local clones of its repositories.
- Use a script to clone/fetch repo list, then analyze each README.
- Example command for a full scan (if GitHub access exists):
  ```bash
  # Get list of repos, then check each README
  gh repo list SuperInstance --limit 100 | awk '{print $1}' | while read repo; do
    gh repo view "$repo" --json name,readme --jq '.readme.text'
  done
  ```

**Without that access, the task cannot be completed factually.**

---

### Output
**Top 10 Worst READMEs Needing Improvement:**
*(Cannot produce — insufficient repository access. Only fleet-knowledge/docs/ available, which contains high-quality documentation.)*

**Next Action Suggested:**
- Request access to SuperInstance repo list or directory.
- Alternatively, proceed to next Navigator task: "Map cross-repo dependencies" using available files in `fleet-knowledge/`.

---

**Tile Submitted:** Analysis of README scan constraints and recommendation.  
**Status:** Task blocked by data access; P0 and P1 followed.