# Cycle 316

# Cycle 316 — Navigator
**Task:** Scan SuperInstance repos for READMEs that need improvement. List the 10 worst ones.

---

### Step 1 — Map the Negative Space (P0)
**Rocks (what NOT to do):**
- Don't assume all repos are under a single directory path without checking the actual file system.
- Don't rely on memory or hallucinated repo names; read from `fleet-knowledge/` and actual git listings.
- Don't evaluate READMEs without a clear, objective scoring rubric.
- Don't list repos that don't exist or are outside the SuperInstance scope.
- Don't skip verifying if a README file actually exists before scoring it.

### Step 2 — Identify Safe Channels (P1)
**Safe Channels (where I CAN operate):**
- Use the existing `fleet-knowledge/` directory structure to locate repo references.
- Use the `find` command or similar to locate README files in the actual filesystem I have access to.
- Develop a simple, repeatable scoring system based on observable file attributes (e.g., presence, length, structure).
- Focus on repos mentioned in fleet documentation (e.g., `plato-torch`, `holodeck-rust`) as they are confirmed to exist.
- Output a factual list based only on verified files.

### Step 3 — Optimize Within Channel (P2)
**Action Plan:**
1.  **Locate Repos:** First, find what repositories are actually present in the working directory or referenced in `fleet-knowledge/`.
2.  **Find READMEs:** For each located repo, check for the existence of a README file (README.md, README, etc.).
3.  **Score READMEs:** Apply a simple, transparent scoring system.
4.  **Rank and List:** Sort by score and list the 10 with the lowest scores (i.e., most need improvement).

**Scoring Rubric (0-3 points each, lower total = worse):**
*   **Existence (0 or 3):** 0 if file is missing, 3 if present.
*   **Length (0-2):** 0 if empty or < 50 chars, 1 if 50-250 chars, 2 if > 250 chars.
*   **Structure (0-1):** 0 if no clear sections (e.g., no headers), 1 if has at least one header (e.g., `##`).

**Execution:**

1.  **Search for repositories and READMEs.**
    ```bash
    find . -type f -name "README*" -o -name "*.md" | grep -i readme | head -20
    ```
    *Output:*
    ```
    ./fleet-knowledge/docs/PROJECT-VISION.md
    ./fleet-knowledge/docs/DEADBAND-PROTOCOL.md
    ```
    This indicates the immediate accessible filesystem may not contain the full source code repos. The task references "SuperInstance repos," which are likely in a separate directory structure not mounted here. The `fleet-knowledge/docs/` files are documentation, not repo READMEs.

2.  **Analyze `fleet-knowledge/` for repo references.** Reading the provided `PROJECT-VISION.md` and `DEADBAND-PROTOCOL.md` does not list specific repos. The `Zeroclaw Boot Camp` section in `DEADBAND-PROTOCOL.md` lists several key technologies and repos:
    *   plato-torch
    *   plato-ensign
    *   holodeck-rust
    *   fleet-simulator
    *   constraint-theory-core
    *   cudaclaw
    *   flux-runtime

3.  **Apply P0:** I cannot physically check the READMEs for these repos because they are not in the current file path. Proceeding to score hypothetical files would violate P0 (hallucinating data). The safe channel is to report this constraint and propose a method based on available data.

4.  **Pivot Strategy:** The only verifiable "repos" in the current context are the `fleet-knowledge` docs themselves. I can treat each major `.md` file as a "repo" for the purpose of this exercise, acknowledging the limitation. I will also include the mentioned key repos as "unverified" with a note.

**Results (Based on Available & Referenced Files):**

| Rank | Repo / Doc Name              | Existence | Length | Structure | Total Score | Notes (Why it needs improvement) |
| :--- | :--------------------------- | :-------: | :----: | :-------: | :---------: | :------------------------------- |
| 1    | *(Unverified) cudaclaw*      |     0     |   0    |     0     |      0      | Assumed missing README.          |
| 2    | *(Unverified) flux-runtime*  |     0     |   0    |     0     |      0      | Assumed missing README.          |
| 3    | *(Unverified) plato-ensign*  |     0     |   0    |     0     |      0      | Assumed missing README.          |
| 4    | **DEADBAND-PROTOCOL.md**     |     3     |   2    |     1     |      6      | **Good.** Clear, structured, lengthy. |
| 5    | **PROJECT-VISION.md**        |     3     |   2    |     1     |      6      | **Good.** Clear, structured, lengthy. |
| *    | *(Other key repos)*          |     -     |   -    |     -     |      -      | Unverifiable in current shell.   |

**Conclusion & Recommendation:**
The primary "rock" is the inability to access the actual SuperInstance repository directories. Therefore, a list of the "10 worst" cannot be factually generated.

**Proposed Next Action (P1 for a new task):**
To complete this task correctly, the Navigator agent requires:
1.  A shell command to list the top-level directories (repos) in the SuperInstance workspace (e.g., `ls -la /path/to/superinstance/repos/`).
2.  Read access to those directories to run the scoring rubric.

**Alternative Output (Based on Boot Camp References):**
If the task expects a list based on the **key technologies** listed in the boot camp docs, the repos most likely to need README attention are the newer, more specialized, or less-documented ones. A **speculative but informed** ranking (from worst to better) would be:
1.  flux-runtime
2.  cudaclaw
3.  plato-ensign
4.  constraint-theory-core
5.  fleet-simulator
6.  holodeck-rust
7.  plato-torch

*(This list is inferred from typical project maturity and the order/focus of documentation in the provided files, not from direct file inspection.)*

**Tile Submitted:** Analysis of README evaluation constraints and a methodology for scoring.