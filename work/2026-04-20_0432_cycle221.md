# Cycle 221

# Cycle 221 — Navigator
**Task:** Scan SuperInstance repos for READMEs that need improvement. List the 10 worst ones.

---

### Step 1 — Map the Negative Space (P0)
**Rocks (what NOT to do):**
- Don't assume all repos are under a single directory path without checking the actual filesystem.
- Don't rely on memory or hallucinated repo names; read from the actual `fleet-knowledge/` context.
- Don't produce a list without clear, objective criteria for "worst."
- Don't skip verifying that a README file actually exists before scoring it.
- Don't treat a missing README as automatically "worst" without considering the repo's purpose and activity.

### Step 2 — Identify Safe Channels (P1)
**Safe Channels (where I CAN operate):**
- I can read the provided `fleet-knowledge/` files to understand the project structure and known repos.
- I can use the `Boot Camp` instructions as a guide: the Scribe's task is to "Read 5 READMEs from fleet repos. Score them." This implies a scoring rubric exists or can be inferred.
- I can define a simple, objective scoring system based on observable README attributes.
- I can list repos mentioned in the provided context and assess their README status based on that limited, verified information.
- I can note that my task is to "list the 10 worst ones," but I must first determine how many repos are actually known/available.

### Step 3 — Optimize Within Channel (P2)
**Best Path (execute the task within constraints):**

1. **Define Scoring Criteria:** Based on common README best practices and the fleet's emphasis on documentation, I'll score each known repo's README on a 0-5 scale (5=best) using:
   - **Existence (0 or 1):** Does a README.md file exist?
   - **Content (0-2):** 0=empty/missing, 1=minimal (title only), 2=has purpose, setup, or usage.
   - **Structure (0-1):** 0=wall of text, 1=has sections (e.g., ## Usage).
   - **Fleet Context (0-1):** 0=no mention of Cocapn/fleet, 1=links to or mentions fleet projects/vision.

2. **Identify Known Repos from Context:** From the provided `fleet-knowledge/` files, I can extract the following explicitly named repositories/projects:
   - `plato-torch`
   - `plato-ensign`
   - `holodeck-rust`
   - `fleet-simulator`
   - `constraint-theory-core`
   - `cudaclaw`
   - `flux-runtime`
   - `oracle1-vessel` (implied by path in Boot Camp)
   - `superinstance` (from PROJECT-VISION.md title)
   - `deadband-protocol` (implied by DEADBAND-PROTOCOL.md)

   **Note:** This is only 10 repos. The task asks for "the 10 worst ones," which suggests there are more than 10 total. However, my safe channel is the *verified context*. I cannot hallucinate additional repos. Therefore, I will assess these 10 and rank them, acknowledging the limited sample.

3. **Assess README Status (Based on Available Context):**
   - `plato-torch`: Mentioned in Boot Camp ("Read plato-torch test files"). Likely has a README. **Assumed score: 4** (exists, content, structure, fleet context).
   - `plato-ensign`: Mentioned in Key Technologies. **Assumed score: 3** (exists, content, fleet context).
   - `holodeck-rust`: Mentioned in Key Technologies. **Assumed score: 3**.
   - `fleet-simulator`: Mentioned in Key Technologies. **Assumed score: 3**.
   - `constraint-theory-core`: Mentioned in Key Technologies. **Assumed score: 3**.
   - `cudaclaw`: Mentioned in Key Technologies. **Assumed score: 3**.
   - `flux-runtime`: Mentioned in Key Technologies. **Assumed score: 3**.
   - `oracle1-vessel`: Path exists (`oracle1-vessel/from-fleet/`). README status unknown. **Assumed score: 2** (likely exists, minimal content).
   - `superinstance`: The vision doc title. May be a top-level meta-repo. README status unknown. **Assumed score: 1** (may exist, purpose unclear).
   - `deadband-protocol`: Likely a repo containing the protocol doc. README status unknown. **Assumed score: 1**.

   **Critical Observation:** This assessment is based on *assumptions*, not file reads. This violates P0 (don't assume). The only READMEs I can *actually* verify from the provided context are the two markdown files in `fleet-knowledge/docs/`. Those are not repo READMEs; they are project documentation.

4. **Execute Within Safe Channel:** Since I cannot access the actual repo filesystems, I cannot complete the task as intended. The safe channel is to report this constraint and propose a concrete next step.

---

### Output: Navigator's Report

**Task:** Scan SuperInstance repos for READMEs that need improvement. List the 10 worst ones.

**Status:** **Blocked by P0 Constraint**

**Findings:**
1. From the provided `fleet-knowledge/` context, I can identify **10 potential repos** by name.
2. I cannot verify the existence or content of their README.md files without filesystem access.
3. The two available documentation files (`PROJECT-VISION.md`, `DEADBAND-PROTOCOL.md`) are not repo READMEs.
4. Therefore, producing a ranked list of "worst" READMEs would be based on speculation, which violates the Deadband Protocol's P0 (Map Negative Space – do not assume).

**Proposed Next Step (P1 Safe Channel):**
To complete this task, I require one of the following:
- **A.** Direct read access to the repositories' root directories to scan for README.md files.
- **B.** A pre-generated list or index of fleet repositories with links or content snippets.
- **C.** Clarification that the task should be performed on the 10 repos named in the context, using a defined rubric, with the understanding that scores are estimates.

**Immediate Action (Within Current Constraints):**
I can create the **scoring rubric** (Step 3.1 above) as a useful artifact. This is a concrete tile that can be used by the Scribe or future agents to evaluate READMEs when access is available.

**Tile Produced: README Scoring Rubric for Fleet Repositories**

```markdown
# README Scoring Rubric (v1)
**Scale:** 0-5 (5 = best)

## Criteria
1.  **Existence (0 or 1):** Does a `README.md` file exist in the repo root?
2.  **Content (0-2):**
    - 0: File missing or completely empty.
    - 1: Minimal content (e.g., only a project title).
    - 2: Contains clear statements of purpose, setup/installation instructions, or basic usage examples.
3.  **Structure (0-1):**
    - 0: No clear structure (wall of text).
    - 1: Uses markdown sections (e.g., `## Installation`, `## Usage`).
4.  **Fleet Context (0-1):**
    - 0: No reference to Cocapn, SuperInstance, or related fleet projects.
    - 1: Includes links to, or mentions of, other fleet projects, the overarching vision, or key concepts (Deadband Protocol, PLATO rooms).

## Scoring Example
- **Score 5 (Exemplary):** README exists (1), has purpose & setup (2), is well-structured (1), and links to fleet docs (1).
- **Score 1 (Poor):** README exists (1), but has only a title (0 for content), no structure (0), no fleet context (0).
- **Score 0 (Missing):** No README file (0 for existence, all other criteria are 0).
```

**Conclusion:** The task to list the 10 worst READMEs cannot be completed accurately with current context. The primary blocker is lack of filesystem access to the repositories. The scoring rubric is submitted as a useful intermediate tile. Requesting guidance or access to proceed.