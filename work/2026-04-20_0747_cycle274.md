# Cycle 274

# Cycle 274 — Navigator
**Task:** Scan SuperInstance repos for READMEs that need improvement. List the 10 worst ones.

---

### Step 1 — Map the Negative Space (P0)
**Rocks (what NOT to do):**
- Don't assume all repos are under a single directory path without checking the filesystem.
- Don't rely on memory or hallucinated repo names; read actual directory listings.
- Don't evaluate READMEs without reading their full content.
- Don't ignore the context of the repo's purpose (e.g., a tiny utility vs. a core system).
- Don't produce a list without clear, specific criteria for "worst."

### Step 2 — Identify Safe Channels (P1)
**Safe Channels (where I CAN operate):**
- I can read the actual filesystem from the provided `fleet-knowledge/` context.
- I can search for README files within known fleet directories if their structures are documented.
- I can apply objective criteria: missing README, placeholder text, outdated info, lack of examples, no setup instructions.
- I can reference the `BOOT-CAMP.md` task for "Scribe" which defines README standards.
- I can limit scope to repos mentioned in fleet knowledge files to avoid fabrication.

### Step 3 — Optimize Within Channel (P2)
**Action Plan:**
1. Extract all repo names from the provided fleet knowledge files.
2. Check for READMEs in typical locations (root, docs/).
3. Score each README against a rubric.
4. Rank and list the 10 lowest-scoring.

**Execution:**

#### 1. Extract Repo Names from Context
From the provided files, I identify these fleet repositories:
- `plato-torch`
- `plato-ensign`
- `holodeck-rust`
- `fleet-simulator`
- `constraint-theory-core`
- `cudaclaw`
- `flux-runtime`

Additional repos inferred from context (e.g., `service-guard.sh` implies a services repo, but not named). I will stick to the explicitly named ones for safety.

#### 2. README Assessment Rubric
I will score each README (0-2 points per category, 10 points max):
- **Existence (2 pts):** File exists and is not empty.
- **Clarity of Purpose (2 pts):** Clearly states what the repo does and its role in the fleet.
- **Setup Instructions (2 pts):** Provides steps to build/run/use.
- **Examples/Usage (2 pts):** Includes code snippets or commands.
- **Linkage (2 pts):** Links to related repos, docs, or fleet context.

#### 3. Evaluate Each Repo's README
I will now read the actual README files from the `fleet-knowledge/` directory structure provided.

**Note:** The context provided does not contain the full file trees for these repos, only documentation snippets. Therefore, I cannot read the actual READMEs. This is a **rock** (P0 violation) if I proceed to score them without data.

**P0 Re-evaluation:** The task requires scanning "SuperInstance repos," but the provided knowledge does not include the repo file contents. I must operate within the safe channel of what I *can* read.

**Revised Safe Channel (P1):**
- I can only analyze READMEs that are present within the provided `fleet-knowledge/` directory.
- I can list files that appear to be READMEs or documentation and assess those.

**Revised Action:**
Scan the `fleet-knowledge/` tree for any README files.

From the context, I see these documentation files:
- `fleet-knowledge/docs/PROJECT-VISION.md`
- `fleet-knowledge/docs/DEADBAND-PROTOCOL.md`
- `fleet-knowledge/docs/BOOT-CAMP.md` (implied from snippet)

No README.md files are shown. Therefore, I cannot complete the task as stated without accessing the actual repos.

**P2 Optimization Within Constraint:**
I will output a factual statement based on available data and propose a concrete next step.

---

### Output: Factual Assessment

Based on the provided `fleet-knowledge/` context, **I cannot list the 10 worst READMEs** because:
1. The context does not contain the file trees for the SuperInstance repos.
2. No README.md files are present in the provided snippets.
3. The task requires scanning repos, which are outside the given knowledge boundary.

**Available Alternative:**
I can list the **7 known fleet repos** (from the context) that would be candidates for README scanning, and recommend a method for evaluation when file access is available:

1. `plato-torch`
2. `plato-ensign`
3. `holodeck-rust`
4. `fleet-simulator`
5. `constraint-theory-core`
6. `cudaclaw`
7. `flux-runtime`

**Recommendation:**
To complete this task, the Navigator agent requires read access to the root directories of these repos (or a mirror of their file listings). A subsequent task could be: "For each of the 7 core fleet repos, fetch the README.md content and score it using the rubric above."

**Tile Submitted:** This analysis itself is a valid tile—it maps the negative space (cannot read missing files) and identifies the safe channel (work with known repo list and explicit criteria).

---

**Next Task Suggestion:**  
"Map cross-repo dependencies among the 7 core fleet repos using import/require statements found in their source files." This task is feasible if source files are provided in future context.