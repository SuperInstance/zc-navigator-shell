# Cycle 409

# Cycle 409 — Navigator
**Task:** Scan SuperInstance repos for READMEs that need improvement. List the 10 worst ones.

---

### Step 1 — Map the Negative Space (P0)
**Rocks (what NOT to do):**
- Don't assume all repos are under a single directory path without checking the actual file system.
- Don't rely on memory or hallucination; must read actual files from `fleet-knowledge/`.
- Don't list repos that don't exist or aren't part of the SuperInstance fleet.
- Don't evaluate READMEs based on subjective preference; need objective criteria.
- Don't skip reading the README content before scoring.

### Step 2 — Identify Safe Channels (P1)
**Safe Paths:**
- Use the `fleet-knowledge/` directory as the source of truth for repo information.
- Read the `PROJECT-VISION.md` and `DEADBAND-PROTOCOL.md` for context.
- Check for any existing lists or catalogs of repos in the knowledge base.
- If no list exists, infer from mentioned technologies in the vision doc.
- Score READMEs based on presence/absence of key sections (e.g., Purpose, Setup, Usage).
- Limit to repos explicitly mentioned in fleet context to avoid scope creep.

### Step 3 — Optimize Within Channel (P2)
**Action Plan:**
1. Extract list of fleet repos from the provided files.
2. For each repo, check if a README exists in the knowledge base.
3. Apply scoring criteria: missing sections, outdated info, unclear purpose.
4. Rank the worst 10 based on score.
5. Output list with specific deficiencies.

---

**Executing Plan:**

From `fleet-knowledge/docs/PROJECT-VISION.md` and `DEADBAND-PROTOCOL.md`, the mentioned fleet repos are:
- `plato-torch`
- `plato-ensign`
- `holodeck-rust`
- `fleet-simulator`
- `constraint-theory-core`
- `cudaclaw`
- `flux-runtime`

Additional repos inferred from context:
- `oracle1-vessel` (mentioned in boot camp)
- `service-guard` (mentioned in Sentinel task)
- `deadband-room` (preset in plato-torch)

Checking `fleet-knowledge/` for README files:
- No README files found in the provided snippet for these repos.
- The knowledge base only contains the two markdown files provided.

**Conclusion:** Based on the available files, I cannot read the actual READMEs of the fleet repos because they are not present in the `fleet-knowledge/` snippet provided. The task requires scanning real READMEs, but the necessary data is absent.

**Safe Channel Adjustment:** Since the files are unavailable, the safe channel is to report this gap and suggest a fallback.

**Output:**
Unable to list the 10 worst READMEs because the README files for SuperInstance repos are not included in the provided `fleet-knowledge/` context. To proceed, either:
1. Provide README files or paths in `fleet-knowledge/` for repos like `plato-torch`, `holodeck-rust`, etc.
2. Or, if this is a simulation, accept this as a P0 finding: missing data prevents task completion.

**Recommendation:** Update the task to analyze the READMEs that *are* available in the knowledge base, or provide the repo READMEs as part of fleet-knowledge.