# Cycle 390

# Cycle 390 — Navigator
**Task:** Scan SuperInstance repos for READMEs that need improvement. List the 10 worst ones.

---

### Step 1 — Map the Negative Space (P0)
**Rocks (what NOT to do):**
- Don't assume all repos are under a single directory path without checking the actual filesystem.
- Don't rely on hallucinated repo names; read from the provided fleet-knowledge/ directory.
- Don't evaluate READMEs without a clear, objective scoring rubric.
- Don't proceed to listing "worst" READMEs without first establishing what "worst" means in this context (e.g., missing, empty, outdated, poorly structured).
- Don't ignore the fleet's own documentation standards (if they exist in the provided files).

### Step 2 — Identify Safe Channels (P1)
**Safe Channels (where I CAN operate):**
1. I can read the actual file structure within the provided `fleet-knowledge/` directory.
2. I can define a simple, objective scoring system based on observable README attributes (presence, length, structure).
3. I can list repositories mentioned in the provided documentation (e.g., `plato-torch`, `holodeck-rust`).
4. I can analyze the README files that are present within the provided context.
5. I can output a factual list based on the available data, noting limitations.

### Step 3 — Optimize Within Channel (P2)
**Execution:**

1. **Scan Available Files:** The provided `fleet-knowledge/` context contains documentation files, not a full git repository structure. I cannot scan a live filesystem. Therefore, I must work with the repositories explicitly named in the provided documents.

2. **Define Scoring Rubric (Simple):** For each named repo, assess README based on:
   - **Presence (0/1):** Is a README file mentioned or implied?
   - **Content Score (0-2):** 0=No content/not mentioned, 1=Minimal mention, 2=Has dedicated section or file.
   - **Structure Score (0-2):** 0=No structure, 1=Basic text, 2=Has clear sections (e.g., Purpose, Usage).

3. **Extract Repo List from Context:** From the provided files, the following repositories are named:
   - `plato-torch`
   - `plato-ensign`
   - `holodeck-rust`
   - `fleet-simulator`
   - `constraint-theory-core`
   - `cudaclaw`
   - `flux-runtime`

4. **Analyze Each Named Repo:**
   - `plato-torch`: Mentioned frequently with descriptions of its presets and API. **Implied README likely exists.** Score: Presence(1), Content(2), Structure(2) = **5/5**.
   - `plato-ensign`: Mentioned as exporting LoRA adapters. **Minimal details.** Score: Presence(1), Content(1), Structure(1) = **3/5**.
   - `holodeck-rust`: Described as a MUD server. **Moderate details.** Score: Presence(1), Content(2), Structure(1) = **4/5**.
   - `fleet-simulator`: Mentioned for training data. **Minimal details.** Score: Presence(1), Content(1), Structure(1) = **3/5**.
   - `constraint-theory-core`: Described as Rust geometric snapping. **Minimal details.** Score: Presence(1), Content(1), Structure(1) = **3/5**.
   - `cudaclaw`: Mentioned as GPU-resident agent runtime. **Minimal details.** Score: Presence(1), Content(1), Structure(1) = **3/5**.
   - `flux-runtime`: Mentioned as self-assembling bytecode VM. **Minimal details.** Score: Presence(1), Content(1), Structure(1) = **3/5**.

5. **List Repositories with Lowest Implied README Quality:**
   Based on the scoring above, the following repositories have the lowest scores (3/5), indicating they are mentioned but with minimal descriptive detail in the provided context, suggesting their READMEs may be lacking or not prominently documented:

   **The 10 worst (from available data, 7 total):**
   1. `plato-ensign`
   2. `fleet-simulator`
   3. `constraint-theory-core`
   4. `cudaclaw`
   5. `flux-runtime`
   *(Note: Only 7 repos are named. The remaining 5 "worst" slots cannot be filled without access to a broader repo list.)*

6. **Recommendation:** A true scan requires filesystem access. The most actionable finding is that repositories beyond the core few (`plato-torch`, `holodeck-rust`) receive minimal description in the core vision/deadband docs, making them priority candidates for README audits.

---

**Output:**
Based on the provided fleet-knowledge documents, the repositories with the **most minimal documentation** (and thus likely the worst READMEs) are:
1. plato-ensign
2. fleet-simulator
3. constraint-theory-core
4. cudaclaw
5. flux-runtime

**Limitation:** This analysis is constrained to the 7 repositories explicitly named in the provided context. A full scan of the SuperInstance organization's GitHub is required to list a true "top 10 worst."