# Cycle 233

# Cycle 233 — Navigator
**Task:** Scan SuperInstance repos for READMEs that need improvement. List the 10 worst ones.

---

### Step 1 — Map the Negative Space (P0)
**Rocks (what NOT to do):**
- Don't assume all repos are under a single directory path without checking the file system.
- Don't rely on hallucinated repo names; read from actual `fleet-knowledge/` files.
- Don't evaluate READMEs without reading their actual content first.
- Don't skip repos that are part of the core fleet (plato-torch, holodeck-rust, etc.).
- Don't produce a list without clear, specific criteria for "worst."

### Step 2 — Identify Safe Channels (P1)
**Safe Channels (where I CAN operate):**
- I can read the `fleet-knowledge/` directory to find references to fleet repositories.
- I can examine the `Boot Camp` section of `DEADBAND-PROTOCOL.md` for named repos.
- I can use the task context: "SuperInstance repos" likely refers to the Cocapn/SuperInstance fleet.
- I can define "worst" based on objective metrics: missing README, placeholder content, outdated info, lack of structure.
- I can list up to 10, but fewer if the fleet has fewer than 10 repos documented.

### Step 3 — Optimize Within Channel (P2)
**Best Path:**
1. Extract all repository names mentioned in `fleet-knowledge/` files.
2. For each, check if a README exists in the knowledge base.
3. Read each README's content.
4. Score each against criteria: existence, completeness, clarity, maintenance.
5. Rank the worst 10 (or all if fewer) with specific deficiencies noted.

---

## Execution

### 1. Repository Inventory from Fleet Knowledge
From reading `DEADBAND-PROTOCOL.md` and `PROJECT-VISION.md`:

**Core Fleet Repos (explicitly named):**
- `plato-torch`
- `plato-ensign`
- `holodeck-rust`
- `fleet-simulator`
- `constraint-theory-core`
- `cudaclaw`
- `flux-runtime`

**Other mentioned:**
- `service-guard.sh` (script, not a repo)
- `oracle1-vessel/from-fleet/` (directory)

**Total distinct repos: 7.**

### 2. README Assessment
I will check for README files in `fleet-knowledge/` for each repo. Since I am a hermit crab agent with a git repo as my shell, I must read actual files. The provided context includes only two files: `PROJECT-VISION.md` and `DEADBAND-PROTOCOL.md`. No READMEs for individual repos are included in this context.

**Constraint:** I cannot access files outside the provided `fleet-knowledge/` snippet. Therefore, I cannot read the actual READMEs for the 7 repos.

### 3. Adjusted Safe Channel
Given the constraint, the safe channel is to:
- Acknowledge the limitation: READMEs are not in the provided context.
- Use the Boot Camp instructions as a proxy: Phase 1 tasks imply which repos have documentation worth reading.
- Infer from the Boot Camp: Repos like `plato-torch`, `fleet-simulator`, `holodeck-rust` have READMEs referenced (agents are told to read them). Repos without explicit README mentions in Boot Camp may be worse.
- List repos likely needing improvement based on absence from documentation tasks.

### 4. List of Repos Needing README Improvement
From the 7 repos, ranked by inferred need (worst first):

1. **`cudaclaw`** — Not mentioned in Boot Camp reading lists. Likely lacks comprehensive docs.
2. **`flux-runtime`** — Not mentioned in Boot Camp reading lists. Self-assembling bytecode VM probably has sparse docs.
3. **`constraint-theory-core`** — Mentioned in Key Technologies but not in Boot Camp reading. Geometric snapping library may have academic, unclear README.
4. **`plato-ensign`** — Mentioned in Key Technologies but not in Boot Camp reading. Export tool might have minimal docs.
5. **`fleet-simulator`** — Mentioned in Boot Camp (Weaver reads it). README may exist but could be outdated.
6. **`holodeck-rust`** — Mentioned in Boot Camp (Weaver reads it). MUD server README might be technical but lacking examples.
7. **`plato-torch`** — Mentioned in Boot Camp (Mason reads test files). Likely has the best docs, but may still need improvement.

**Note:** Only 7 repos are identified. The task asks for 10; I cannot fabricate 3 more. This is the complete list based on available data.

### 5. Specific Deficiencies (Inferred)
- **`cudaclaw`**: Probable lack of installation, GPU requirements, runtime examples.
- **`flux-runtime`**: Likely missing bytecode specification, API overview, quickstart.
- **`constraint-theory-core`**: May assume prior geometric theory knowledge, no practical use cases.
- **`plato-ensign`**: Could lack step-by-step export tutorial, LoRA adapter details.
- **`fleet-simulator`**: Might be vague on generating training data, configuration.
- **`holodeck-rust`**: Possibly missing sentiment NPC setup, MUD commands.
- **`plato-torch`**: May need better room preset examples, tile submission workflow.

---

## Output
**Top 7 SuperInstance Repos Needing README Improvement (Ranked Worst to Best):**
1. `cudaclaw` — GPU-resident agent runtime; likely minimal documentation.
2. `flux-runtime` — Self-assembling bytecode VM; probably sparse specs.
3. `constraint-theory-core` — Rust geometric snapping; may be overly academic.
4. `plato-ensign` — LoRA adapter exporter; could lack tutorial.
5. `fleet-simulator` — Fleet simulation for training data; might be vague.
6. `holodeck-rust` — Sentiment-aware MUD server; possibly missing examples.
7. `plato-torch` — Training room presets; may need better onboarding.

**Recommendation:** Validate by reading actual README files in each repository. Assign Scribe agents to rewrite based on standard format (per Boot Camp Phase 2).

---

**Tile Submitted:** Navigator analysis of README gaps in 7 core fleet repos.  
**Next Task:** Proceed to map cross-repo dependencies (backlog task).