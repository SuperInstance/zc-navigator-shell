# Cycle 366

# Cycle 366 — Navigator
**Task:** Scan SuperInstance repos for READMEs that need improvement. List the 10 worst ones.

---

### Step 1 — Map the Negative Space (P0)
**Rocks (what NOT to do):**
- Don't assume all repos are under a single directory path without checking the actual file system.
- Don't rely on memory or hallucinated repo names; read from `fleet-knowledge/` or other provided files.
- Don't evaluate READMEs without a clear, objective scoring rubric.
- Don't list repos that don't exist in the provided context.
- Don't skip verifying that a README file actually exists before declaring it "bad."

### Step 2 — Identify Safe Channels (P1)
**Safe Channels (where I CAN operate):**
- I can read the `fleet-knowledge/` directory contents provided in the context.
- I can analyze the `Boot Camp` section of `DEADBAND-PROTOCOL.md` for repo names and agent roles.
- I can use the `Recent Work` file (`2026-04-20_1353_cycle364.md`) as a starting point, noting it was cut off.
- I can define a simple, objective scoring system for README quality based on observable attributes.
- I can list only repos explicitly mentioned or implied in the provided knowledge.

### Step 3 — Optimize Within Channel (P2)
**Action Plan:**
1. **Extract Repo List:** Parse the `Boot Camp` section for all mentioned repository names.
2. **Define Scoring Rubric:** Create a quick, 3-point scale based on common README flaws.
3. **Evaluate & Rank:** Score each repo's README based on the rubric and available context.
4. **List Top 10 Worst:** Present the repos with the lowest scores, noting the primary deficiency.

---

### Execution

#### 1. Repository List from Context
From the `Boot Camp` section and other mentions in `fleet-knowledge/`:
- `plato-torch`
- `plato-ensign`
- `holodeck-rust`
- `fleet-simulator`
- `constraint-theory-core`
- `cudaclaw`
- `flux-runtime`
- `service-guard.sh` (script, not a repo)
- `oracle1-vessel` (directory/vessel)
- `memory/` (directory)
- `fleet-knowledge/` (directory)

**Note:** The context does not provide a full filesystem listing. The above are the primary codebase names referenced. I will assume each corresponds to a repository.

#### 2. README Scoring Rubric (0-2 points, lower is worse)
- **Score 0 (Critical Deficiency):** No README file mentioned or implied; repository purpose is unclear from context.
- **Score 1 (Major Deficiency):** README exists but is mentioned as needing work (e.g., in the Scribe's boot camp task) or is described as non-standard.
- **Score 2 (Adequate):** README is referenced as a source of understanding (e.g., "Read the README") with no noted flaws.

#### 3. Evaluation & Ranking

| Repository | Score | Reason (Based on Context) |
|------------|-------|----------------------------|
| `service-guard.sh` | 0 | Not a repo; a script. No README implied. |
| `oracle1-vessel` | 0 | Directory/vessel; context mentions `from-fleet/` bottles, not a README. |
| `memory/` | 0 | Directory for logs/timelines; no README implied. |
| `fleet-knowledge/` | 0 | Documentation directory; contains the files I'm reading, but no repo README. |
| `constraint-theory-core` | 1 | Mentioned in "Key Technologies," but no indication its README has been reviewed or improved. |
| `cudaclaw` | 1 | Mentioned in "Key Technologies," no README quality status given. |
| `flux-runtime` | 1 | Mentioned in "Key Technologies," no README quality status given. |
| `plato-torch` | 2 | README is a source for understanding (Weaver's task: "Read plato-torch...READMEs"). |
| `plato-ensign` | 2 | Implied by `plato-torch` connection; likely has a README for understanding. |
| `holodeck-rust` | 2 | README is a source for understanding (Weaver's task: "Read holodeck-rust READMEs"). |
| `fleet-simulator` | 2 | README is a source for understanding (Weaver's task: "Read fleet-simulator...READMEs"). |

#### 4. The 10 Worst READMEs (or lacking READMEs)
Given the limited set of repos in context, here are the 10 lowest-scoring entries:

1. **`service-guard.sh`** (Score 0) – Script, no README.
2. **`oracle1-vessel`** (Score 0) – Vessel directory, no README.
3. **`memory/`** (Score 0) – Log directory, no README.
4. **`fleet-knowledge/`** (Score 0) – Docs directory, no repo README.
5. **`constraint-theory-core`** (Score 1) – README status unknown, likely needs work.
6. **`cudaclaw`** (Score 1) – README status unknown, likely needs work.
7. **`flux-runtime`** (Score 1) – README status unknown, likely needs work.
8. *[Insufficient repos for a full 10]* – The remaining repos (`plato-torch`, `plato-ensign`, `holodeck-rust`, `fleet-simulator`) scored 2, indicating they are referenced as sources of understanding and are likely in better shape.

**Observation:** The fleet knowledge provided does not contain enough distinct repository names to list 10 truly "worst" READMEs. The task may assume access to a broader `SuperInstance` GitHub organization or filesystem not included in the context. The most actionable finding is that the **three core tech repos (`constraint-theory-core`, `cudaclaw`, `flux-runtime`) have unknown README quality** and are prime candidates for the Scribe's improvement work per the Boot Camp.

---

### Tile Output
**Finding:** Based on the provided `fleet-knowledge` context, a full list of 10 worst READMEs cannot be generated. However, three key technology repositories (`constraint-theory-core`, `cudaclaw`, `flux-runtime`) are identified as having no README quality assessment and are likely priorities for documentation improvement, aligning with the Scribe's boot camp task to "Rewrite 3 fleet READMEs to standard format."

**Recommendation (P2 Optimization):** The Navigator's next task should involve scanning the actual filesystem or GitHub API (if available) to build a complete repository list before performing a README quality audit. The current context is insufficient for the requested scope.