# Cycle 215

# Cycle 215 — Navigator
**Task:** Scan SuperInstance repos for READMEs that need improvement. List the 10 worst ones.

---

### Step 1 — Map the Negative Space (P0)
**Rocks (what NOT to do):**
- Don't assume all repos are under a single directory path without checking the actual filesystem.
- Don't rely on memory or hallucination; read the actual files from `fleet-knowledge/`.
- Don't list repos that don't exist in the provided knowledge.
- Don't proceed to listing without first verifying README presence and quality.
- Don't ignore the Deadband Protocol: P0 (map rocks) must be completed before P1/P2.

### Step 2 — Identify Safe Channels (P1)
**Safe Channels (where I CAN operate):**
- I can read the provided `fleet-knowledge/` directory structure.
- I can analyze README files that are explicitly included in the knowledge dump.
- I can evaluate READMEs based on observable criteria: presence, length, structure, clarity.
- I can list repos from the knowledge that have poor or missing READMEs.
- I can base my analysis solely on the files provided in this session.

### Step 3 — Optimize Within Channel (P2)
**Best Path:**
1. Examine the `fleet-knowledge/` structure to identify repository references.
2. Check each referenced repo for a README file in the knowledge.
3. Assess README quality using a simple rubric: missing, very short (<100 words), unstructured, or unclear.
4. Rank the worst 10 based on these criteria.
5. Present the list with specific, factual reasons.

---

### Execution

**1. Survey `fleet-knowledge/` for Repo References**
From the provided files, I see references to these fleet repos:
- `plato-torch`
- `plato-ensign`
- `holodeck-rust`
- `fleet-simulator`
- `constraint-theory-core`
- `cudaclaw`
- `flux-runtime`

**2. Check for READMEs in Knowledge**
The knowledge includes:
- `PROJECT-VISION.md` (a vision doc, not a repo README)
- `DEADBAND-PROTOCOL.md` (a protocol doc)
- Boot camp instructions (no repo READMEs)
- No actual README files for the listed repos are provided in this knowledge dump.

**3. Assessment**
Since no README files are included in the provided `fleet-knowledge/` dump, I cannot evaluate their content or quality. The only files present are documentation and state files, not repository READMEs.

**4. Conclusion**
Based on the **actual files provided**, I cannot list "the 10 worst READMEs" because no READMEs are available to assess. The task requires reading files that are not present in the current context.

---

### Output: Navigator's Report

**Status:** Task blocked by missing data.
**Finding:** The `fleet-knowledge/` dump does not contain README files for SuperInstance repos. Therefore, I cannot scan or rank them.
**Recommendation:** To complete this task, provide README files from the repos (e.g., `plato-torch/README.md`, `holodeck-rust/README.md`) or access to the repository file trees. Alternatively, adjust the task to analyze the available documentation (`PROJECT-VISION.md`, `DEADBAND-PROTOCOL.md`) for clarity and structure.

**Next Action:** Await additional file context or task clarification. In the meantime, I can proceed to a backlog task that uses available data, such as mapping cross-repo dependencies based on the references in the provided docs.