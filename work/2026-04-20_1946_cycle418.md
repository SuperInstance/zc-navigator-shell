# Cycle 418

# Cycle 418 — Navigator
**Task:** Scan SuperInstance repos for READMEs that need improvement. List the 10 worst ones.

---

### Step 1 — Map the Negative Space (P0)
**Rocks (what NOT to do):**
- Don't assume all repos are under a single directory path without checking the actual file system.
- Don't rely on hallucinated repo names; read from the provided `fleet-knowledge/` context only.
- Don't evaluate READMEs based on subjective "badness"; use objective, observable criteria.
- Don't skip verifying file existence before attempting to read content.
- Don't produce a list without concrete examples from the actual files provided.

### Step 2 — Identify Safe Channels (P1)
**Safe Channels (where I CAN operate):**
- I can read the `fleet-knowledge/` directory structure from the context given.
- I can analyze the READMEs and documentation files explicitly mentioned in the provided context (e.g., `PROJECT-VISION.md`, `DEADBAND-PROTOCOL.md`).
- I can use the repo names and technologies listed in the "Key Technologies" and "Architecture" sections of the context as potential search targets.
- I can apply objective criteria: missing READMEs, placeholder text, outdated information, lack of structure, broken links, or unclear purpose.
- I can report on the READMEs I have direct access to within the provided context.

### Step 3 — Optimize Within Channel (P2)
**Best Path (execute the scan within constraints):**

1.  **Extract Repo/Project Names from Context:**
    From the "Fleet Context — Required Reading for All Agents" section, the following projects are explicitly named:
    - plato-torch
    - plato-ensign
    - holodeck-rust
    - fleet-simulator
    - constraint-theory-core
    - cudaclaw
    - flux-runtime

2.  **Check for READMEs in Provided Context:**
    The provided context includes two full documentation files:
    - `fleet-knowledge/docs/PROJECT-VISION.md`
    - `fleet-knowledge/docs/DEADBAND-PROTOCOL.md`
    These are high-quality, structured documents. They are not READMEs for the specific repos listed above.

3.  **Apply Objective "Needs Improvement" Criteria:**
    Since I cannot access the actual file systems for the listed repos, I must base my assessment on the context provided and the common patterns in open-source projects. A README "needs improvement" if it likely exhibits one or more of these traits (inferred from the boot camp's "Scribe" task to "rewrite 3 fleet READMEs to standard format"):
    - **Absent:** No README file exists.
    - **Skeletal:** Contains only a project name or a single line.
    - **Placeholder:** Uses text like "TODO," "Coming soon," or generic templates.
    - **Unstructured:** Lacks clear sections (Installation, Usage, API, Examples).
    - **Outdated:** Mentions old versions or deprecated dependencies.
    - **Unclear:** Fails to explain the project's purpose or how it fits into the fleet.

4.  **Generate the List Based on Available Evidence:**
    The "Boot Camp" section states that the **Scribe** agent's task is to "Rewrite 3 fleet READMEs to standard format." This is direct evidence that at least three fleet repos have READMEs that do not meet the standard and therefore need improvement.

    **Therefore, the 10 worst READMEs (a prioritized list based on the need implied by the Scribe's task and common project patterns) are likely from this set:**

    1.  **plato-torch** (High Priority - Core training system, likely complex. A poor README here would block many agents.)
    2.  **holodeck-rust** (High Priority - MUD server with unique "sentiment-aware NPCs." Requires clear setup instructions.)
    3.  **fleet-simulator** (High Priority - Critical for generating training data. Usage must be clearly documented.)
    4.  **cudaclaw** (Medium Priority - GPU-resident runtime. Likely has complex CUDA/GPU dependencies needing clear explanation.)
    5.  **flux-runtime** (Medium Priority - Self-assembling bytecode VM. Conceptual complexity demands excellent documentation.)
    6.  **plato-ensign** (Medium Priority - Exports LoRA adapters. Process needs to be clearly documented for users.)
    7.  **constraint-theory-core** (Medium Priority - Rust geometric snapping library. API must be well-documented for developers.)
    8.  *[A repo mentioned in context but not in the key tech list, e.g., a tool or utility repo]*
    9.  *[Another older or less-maintained fleet repo]*
    10. *[A repo with a README that is purely autogenerated or a copy from a boilerplate]*

    **Specific Finding:** The most concrete, actionable finding from the available context is that **there are at least 3 fleet READMEs requiring rewrite to a standard format**, as per the Scribe's Phase 2 task. The exact identities of these 3 are the highest-priority targets for improvement. The remaining 7 on the list above are educated inferences based on project criticality and the common tendency for early-stage or complex technical projects to have underdeveloped documentation.

### Recommendation (P2 Optimization)
To complete this task with factual accuracy, the next step requires **file system access** or a **list of repository contents** to:
1.  Confirm the existence and current state of README.md files in repos like `plato-torch`, `holodeck-rust`, etc.
2.  Apply the objective criteria directly to their content.
3.  Rank them based on specific deficiencies (e.g., "holodeck-rust/README.md is 2 lines long," "plato-torch/README.md has a broken 'Quick Start' link").

**Output for Tile Submission:** The primary, evidence-based conclusion is that the fleet has identified at least three READMEs in need of standardization. The improvement effort should begin by identifying those three specific repos through direct file inspection.