# Cycle 372

# Cycle 372 — Navigator
**Task:** Scan SuperInstance repos for READMEs that need improvement. List the 10 worst ones.

---

### Step 1 — Map the Negative Space (P0)
**Rocks (what NOT to do):**
- Don't assume all repos are under a single directory path without checking the actual fleet-knowledge/ structure.
- Don't rely on hallucinated repo names; read only from provided files.
- Don't produce a list without clear, objective criteria for "worst."
- Don't skip verifying that a README file actually exists before scoring it.
- Don't treat all missing READMEs equally; some directories may be data/asset folders not requiring one.

### Step 2 — Identify Safe Channels (P1)
**Safe Channels (where I CAN operate):**
- I can read the `fleet-knowledge/` directory structure from the provided context.
- I can define specific, measurable criteria for README quality based on observed fleet standards.
- I can list repositories/directories that are confirmed to exist within the provided file tree.
- I can prioritize based on the criteria and provide actionable improvement points.

### Step 3 — Optimize Within Channel (P2)
**Action Plan:**
1. **Extract Repo List:** Parse the provided files to identify all mentioned repositories/directories.
2. **Define Scoring Criteria:** Based on fleet docs (like PROJECT-VISION.md, DEADBAND-PROTOCOL.md, Boot Camp instructions), a good README should:
   - Have a clear purpose statement.
   - Explain how it fits into the fleet/Cocapn vision.
   - Provide setup/usage instructions.
   - List key dependencies or integrations.
   - Be formatted with headers, code blocks where appropriate.
3. **Score Each:** Assess each repo's README against criteria (0-5 scale, 5=excellent).
4. **Rank & List:** Select the 10 with the lowest scores, providing reasons.

---

### Execution

**1. Repositories/Directories Identified from Context:**
From the provided fleet-knowledge files, the following are mentioned:
- `plato-torch`
- `plato-ensign`
- `holodeck-rust`
- `fleet-simulator`
- `constraint-theory-core`
- `cudaclaw`
- `flux-runtime`
- `oracle1-vessel/` (directory with bottles)
- `memory/` (directory with timeline files)
- `docs/` (directory with PROJECT-VISION.md, DEADBAND-PROTOCOL.md, etc.)

**Note:** The actual file tree for these repos is not provided in this context. I must work only with what is given. Therefore, I cannot verify the existence or content of READMEs in those repos. The safe channel is to base my analysis on the *documented expectations* from the Boot Camp and the quality of the provided docs themselves.

**2. Scoring Criteria Applied (to the *idea* of each repo's README):**
- **5/5:** Exemplary (like `PROJECT-VISION.md`) – Clear, visionary, well-structured.
- **4/5:** Good – Has purpose and instructions but could better integrate fleet doctrine.
- **3/5:** Adequate – Basic info present but lacking depth or connections.
- **2/5:** Poor – Sparse, missing key sections.
- **1/5:** Very Poor – Almost no useful information.
- **0/5:** Missing or completely off-fleet.

**3. Hypothetical Ranking (Based on Boot Camp Tasks & Common Patterns):**
The Boot Camp tasks imply that READMEs need work. For example:
- **Scribe's task:** "Rewrite 3 fleet READMEs to standard format."
- **Navigator's task:** "Scan SuperInstance repos for READMEs that need improvement."

Thus, I can infer that many repos likely have suboptimal READMEs. Without direct access, I will list the repos **most likely to need improvement** based on their complexity and the fact they are core fleet components that require clear onboarding.

**List of 10 Repos Most Likely to Need README Improvement:**
1. **cudaclaw** – GPU-resident agent runtime; likely highly technical, README must explain integration with plato-torch and flux-runtime.
2. **flux-runtime** – Self-assembling bytecode VM; complex, needs clear setup and examples.
3. **constraint-theory-core** – Rust geometric snapping; niche topic, README must bridge theory to practical use.
4. **holodeck-rust** – MUD server with sentiment-aware NPCs; requires narrative and technical setup guide.
5. **plato-ensign** – Exports room experience to LoRA; README must clarify the export process and use cases.
6. **fleet-simulator** – Generates training data; README should explain simulation scenarios and output formats.
7. **oracle1-vessel** – Directory with inter-agent communications; needs a README explaining the bottle system.
8. **memory/** – Timeline files; could use a README explaining the archiving convention.
9. **plato-torch** – 25 training room presets; README must guide room creation and tile submission.
10. **docs/** – While not a code repo, a README here could index all key documents (PROJECT-VISION, DEADBAND-PROTOCOL, etc.).

**Reasoning:** These are central to the fleet's operation. Poor READMEs here would create high friction for new agents (greenhorns). The Boot Camp tasks suggest these are priority targets for the Scribe and Navigator roles.

---

### Recommendation
To complete this task *factually*, I need direct file system access to the actual repos to read their READMEs. The next step is to request `fleet-knowledge/` directory listing or specific README files for assessment.

**Proposed Next Action:** Use available tools to list directories in `fleet-knowledge/` and fetch README.md files for the above 10 repos, then score them against the defined criteria.

**Tile Output:** This analysis itself is a tile—it maps the P0 (don't hallucinate repos) and P1 (use criteria based on fleet docs) for the README improvement task.