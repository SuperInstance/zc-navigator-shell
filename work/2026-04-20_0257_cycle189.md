# Cycle 189

# Cycle 189 — Navigator
**Task:** Scan SuperInstance repos for READMEs that need improvement. List the 10 worst ones.

---

### Step 1 — Map the Negative Space (P0)
**Rocks (what NOT to do):**
- Don't assume all repos are under a single directory path without checking the actual file system.
- Don't rely on hallucinated repo names; read from actual `fleet-knowledge/` files.
- Don't rank READMEs without clear, objective criteria.
- Don't proceed to analysis without first verifying which repos exist and are accessible.

### Step 2 — Identify Safe Channels (P1)
**Safe Channels (where I CAN operate):**
1. Use the provided `fleet-knowledge/` directory as the source of truth.
2. Read existing documentation (e.g., `BOOT-CAMP.md`) to understand repo context.
3. Define clear criteria for "needs improvement" based on observable file attributes.
4. Limit analysis to repos explicitly mentioned in fleet knowledge or discoverable via file listing.

### Step 3 — Optimize Within Channel (P2)
**Execution Plan:**
1. **Discover Repos:** List all directories under `fleet-knowledge/` that represent or reference fleet repositories.
2. **Extract READMEs:** For each repo, locate its README file (or equivalent introductory documentation).
3. **Apply Criteria:** Score each README against objective metrics.
4. **Rank & List:** Sort by score and output the 10 worst.

---

## Execution

### 1. Discovered Repos & READMEs
Based on reading `fleet-knowledge/docs/BOOT-CAMP.md` and scanning the directory structure, the following repositories are referenced in the fleet context. I will examine their READMEs as they appear in the knowledge base.

**List of referenced repos (from BOOT-CAMP.md and other docs):**
- `plato-torch`
- `plato-ensign`
- `holodeck-rust`
- `fleet-simulator`
- `constraint-theory-core`
- `cudaclaw`
- `flux-runtime`
- `service-guard` (referenced, likely a script/service)
- `oracle1-vessel` (referenced as a directory)

**Note:** The `fleet-knowledge/` directory appears to be a curated snapshot, not a full git clone of each repo. Therefore, I will look for README files within this knowledge base that correspond to these repos.

### 2. Criteria for "Needs Improvement"
I define "worst" based on the following observable criteria (each criterion adds 1 point to a "deficiency score"; higher score = worse):

- **C1: Missing README** – No README.md or similar introductory file found.
- **C2: Empty/Skeleton README** – File exists but is extremely short (< 50 words) or contains only placeholder text (e.g., "# Project Name").
- **C3: No Purpose Statement** – README does not clearly state the project's purpose or core thesis in the first paragraph.
- **C4: No Usage/Getting Started** – Lacks any instructions for building, running, or using the project.
- **C5: No Context** – Does not explain how the project fits into the larger Cocapn fleet or SuperInstance vision.
- **C6: Poor Formatting** – Uses minimal Markdown, lacks headers, code blocks, or lists, making it hard to scan.

### 3. Analysis of Available READMEs

Scanning `fleet-knowledge/` for files named README.md or with introductory content:

1. **`plato-torch`** – README not found in knowledge base. (C1)
2. **`plato-ensign`** – README not found. (C1)
3. **`holodeck-rust`** – README not found. (C1)
4. **`fleet-simulator`** – README not found. (C1)
5. **`constraint-theory-core`** – README not found. (C1)
6. **`cudaclaw`** – README not found. (C1)
7. **`flux-runtime`** – README not found. (C1)
8. **`service-guard`** – Referenced in BOOT-CAMP.md; no README found. (C1)
9. **`oracle1-vessel`** – Directory exists (`fleet-knowledge/oracle1-vessel/`), contains `from-fleet/` but no README. (C1)
10. **SuperInstance / Cocapn** – Found `fleet-knowledge/docs/PROJECT-VISION.md` and `DEADBAND-PROTOCOL.md`. These are high-quality docs but are vision/protocol, not repo READMEs.

**Observation:** The `fleet-knowledge/` snapshot does not contain the actual README files for the individual code repositories. It contains overarching documentation (vision, protocol, boot camp) but not the per-repo introductory files.

### 4. Conclusion & Output

Given the constraints of the provided knowledge base, I cannot access the actual READMEs of the 10+ fleet repos. Therefore, I cannot rank them based on content.

However, applying the **P0 principle** (map negative space), the clear "rock" is that **repository READMEs are missing from the knowledge snapshot**. This is a critical gap for onboarding and understanding.

**Recommendation (P1 safe channel):** The fleet should ensure that each repository's README is captured in the `fleet-knowledge/` archive, or that agents have access to the live git repositories to read them directly.

**For the purpose of this task,** I will list the 10 referenced repos that **lack a README in the knowledge base**, which inherently makes them the "worst" for improvement priority:

1. `plato-torch`
2. `plato-ensign`
3. `holodeck-rust`
4. `fleet-simulator`
5. `constraint-theory-core`
6. `cudaclaw`
7. `flux-runtime`
8. `service-guard`
9. `oracle1-vessel`
10. (No tenth distinct repo found; other referenced names like "DeadbandRoom" are presets within `plato-torch`)

**Next Step (P2 optimization within channel):** If given access to the actual git repos, I would clone them and perform a detailed README analysis using the defined criteria.