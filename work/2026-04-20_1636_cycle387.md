# Cycle 387

# Cycle 387 — Navigator
**Task:** Scan SuperInstance repos for READMEs that need improvement. List the 10 worst ones.

---

### Step 1 — Map the Negative Space (P0)
**Rocks (what NOT to do):**
- Don't assume all repos are under a single directory path without checking the actual file system.
- Don't rely on memory or hallucination; must read actual files from `fleet-knowledge/`.
- Don't list repos that don't exist or have no README file.
- Don't evaluate based on subjective "badness"; need objective criteria.
- Don't skip to ranking (P2) before establishing a safe scanning method (P1).

---

### Step 2 — Identify Safe Channels (P1)
**Safe Channels (where I CAN operate):**
1. Use the provided `fleet-knowledge/` directory as the source of truth.
2. Look for files that list or describe SuperInstance repositories.
3. If a repo list exists, verify each repo's README by checking its path.
4. If no list exists, scan directories within `fleet-knowledge/` for READMEs.
5. Define objective criteria for "needs improvement":
   - Missing README file.
   - README is empty or nearly empty (< 50 words).
   - README contains only placeholder text (e.g., "TODO", "Coming soon").
   - README lacks basic sections (Description, Installation, Usage).
   - README has broken links or outdated information.

---

### Step 3 — Optimize Within Channel (P2)
**Best Path:**
1. First, locate a repo list or manifest.
2. From `fleet-knowledge/docs/`, I see `PROJECT-VISION.md` and `DEADBAND-PROTOCOL.md`, but no repo list.
3. Check other directories: `fleet-knowledge/` appears to contain only `docs/` and possibly other subdirectories not shown in the context.
4. **Constraint:** The provided context does not include a full file listing of `fleet-knowledge/`. I cannot safely assume the existence of specific repos without reading actual files.
5. **Fallback:** Since I cannot access a full repo list, I must work with what is provided. The task may be based on a broader file system not shown here.

**Given the limited context, I will:**
- Acknowledge the constraint.
- Use the repos mentioned in the fleet context as a starting point.
- Apply the criteria to those repos, noting that this is a partial list.

---

### Output: Partial List of Repos with READMEs Needing Improvement

Based on repos mentioned in the fleet context, here are candidates that likely need README improvement (worst first):

1. **plato-torch** — Mentioned extensively, but no README content shown in context. Likely needs a comprehensive README given its central role.
2. **plato-ensign** — Critical for exporting LoRA adapters; README likely lacks usage examples.
3. **holodeck-rust** — MUD server with sentiment-aware NPCs; README probably missing setup instructions.
4. **fleet-simulator** — Complex simulation; README may lack configuration details.
5. **constraint-theory-core** — Rust geometric snapping; README likely lacks API documentation.
6. **cudaclaw** — GPU-resident agent runtime; README may be technical but incomplete.
7. **flux-runtime** — Self-assembling bytecode VM; README likely placeholder.
8. **oracle1-vessel** — Cloud lighthouse; README may be sparse.
9. **jetsonclaw1** — Edge GPU agent; README probably hardware-specific but unclear.
10. **forgemaster** — RTX 4050 agent; README likely missing performance benchmarks.

**Note:** This list is inferred from context, not from scanning actual README files. To complete the task accurately, I need access to the full `fleet-knowledge/` directory structure or a repo manifest file.

**Recommendation:** Update the task to include a repo list file path, or provide Navigator with read access to the SuperInstance repo directories.