# Cycle 174

# Cycle 174 — Navigator
**Task:** Scan SuperInstance repos for READMEs that need improvement. List the 10 worst ones.

---

### Step 1 — Map the Negative Space (P0)
**Rocks (what NOT to do):**
- Don't assume all repos are under a single directory path without checking the actual file system.
- Don't rely on hallucinated repo names; read from the actual `fleet-knowledge/` structure.
- Don't evaluate READMEs without reading their actual content first.
- Don't ignore the context of the repo's purpose (e.g., a prototype vs. a core library).
- Don't rank based on superficial metrics like length alone; assess clarity, completeness, and usefulness.

### Step 2 — Identify Safe Channels (P1)
**Safe Channels (what I CAN do):**
- I can read the `fleet-knowledge/` directory to find references to repositories.
- I can examine the provided files for repo names and structures.
- I can assess READMEs based on concrete criteria: presence of a description, installation instructions, usage examples, and links to related projects.
- I can prioritize repos that are likely to be important (frequently mentioned in docs) but have poor documentation.
- I can produce a factual list based on the evidence available.

### Step 3 — Optimize Within Channel (P2)
**Action Plan:**
1. Extract all repository names mentioned in the provided `fleet-knowledge/` files.
2. For each repo, infer README quality based on mentions and descriptions in the docs.
3. Rank them by perceived documentation need (worst first).
4. List the top 10 with specific reasons.

---

**Execution:**

From the provided files, I identify the following repositories:
1. `plato-torch` (mentioned in Boot Camp, Key Technologies)
2. `plato-ensign` (Key Technologies)
3. `holodeck-rust` (Key Technologies)
4. `fleet-simulator` (Key Technologies)
5. `constraint-theory-core` (Key Technologies)
6. `cudaclaw` (Key Technologies)
7. `flux-runtime` (Key Technologies)
8. `oracle1-vessel` (mentioned in Boot Camp: Herald reads bottles here)
9. `service-guard.sh` (Boot Camp: Sentinel reads this)
10. `DeadbandRoom` (Boot Camp: Tinker reads this preset)

**Analysis:**
The `fleet-knowledge/` docs are high-level vision and protocol; they don't contain the actual READMEs for these repos. Therefore, I cannot directly read and score them. However, I can infer which ones are critical and likely to need better READMEs based on their importance and the lack of detailed documentation in the knowledge base.

**Inferred Ranking of README Need (Worst First):**
1. **cudaclaw** — GPU-resident agent runtime; complex, likely needs clear setup and API docs.
2. **flux-runtime** — Self-assembling bytecode VM; highly technical, requires detailed examples.
3. **constraint-theory-core** — Rust geometric snapping; niche topic, needs mathematical context.
4. **holodeck-rust** — MUD server with sentiment-aware NPCs; interdisciplinary, needs gameplay and tech docs.
5. **plato-ensign** — Exports room experience to LoRA; process-heavy, needs step-by-step guides.
6. **fleet-simulator** — Training data simulation; requires configuration and data format docs.
7. **plato-torch** — 25 training room presets; though mentioned often, may already have some docs.
8. **oracle1-vessel** — Cloud lighthouse agent; likely internal, but needs deployment notes.
9. **DeadbandRoom** — A preset in plato-torch; might be documented within that repo.
10. **service-guard.sh** — A script, not a repo; may need a README if standalone.

**Note:** This is an inference based on available context. To complete this task accurately, I would need direct access to the SuperInstance GitHub organization or the local clone of these repositories to read the actual README files. The next safe step is to request that access or use a tool to fetch repo metadata.

**Output — Top 10 Repos Needing README Improvement (Based on Inference):**
1. **cudaclaw**
2. **flux-runtime**
3. **constraint-theory-core**
4. **holodeck-rust**
5. **plato-ensign**
6. **fleet-simulator**
7. **plato-torch**
8. **oracle1-vessel**
9. **DeadbandRoom** (preset)
10. **service-guard.sh** (script)

**Recommendation:** Validate this list by reading the actual READMEs from the repos. If access is granted, I can proceed to Phase 2: Analyze & Document by rewriting the worst ones.