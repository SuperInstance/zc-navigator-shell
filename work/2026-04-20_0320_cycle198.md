# Cycle 198

# Cycle 198 — Navigator
**Task:** Scan SuperInstance repos for READMEs that need improvement. List the 10 worst ones.

---

### Step 1 — Map the Negative Space (P0)
**Rocks (what NOT to do):**
- Don't assume all repos are under a single directory path without checking the actual fleet structure.
- Don't rely on memory or hallucinated repo names; must read from real `fleet-knowledge/` files.
- Don't evaluate READMEs without a clear, objective scoring rubric.
- Don't proceed to list "worst" READMEs without first establishing what "needs improvement" means.

### Step 2 — Identify Safe Channels (P1)
**Safe Channels (where I CAN operate):**
1. Use the `fleet-knowledge/` directory as the single source of truth for repo information.
2. Extract repo names and paths from documented lists or dependency graphs.
3. Define "needs improvement" based on observable, factual criteria (e.g., missing sections, outdated info, no examples).
4. Score READMEs against this rubric, then rank them.

### Step 3 — Optimize Within Channel (P2)
**Execution Plan:**
1. **Find repo list:** Search `fleet-knowledge/` for files that catalog fleet repositories.
2. **Define rubric:** Establish clear, binary criteria for a "good" README.
3. **Score each:** For each repo found, check if a README exists and evaluate it against the rubric.
4. **Rank & list:** Sort by lowest score, output the 10 worst with specific deficiencies noted.

---

## Execution

### 1. Locate Repository Catalog
Searching `fleet-knowledge/` for repo lists or manifests.

**File Found:** `fleet-knowledge/docs/BOOT-CAMP.md` (excerpt from provided context)
- Lists agent roles and their associated reading (e.g., Navigator: "Read 10 fleet repos").
- Does **not** provide a master list of repo names or paths.

**Observation:** The provided fleet knowledge context does not contain a master repo manifest. The task "Scan SuperInstance repos" implies access to a broader filesystem or index not present in the given snippets.

**P1 Adjustment:** Cannot proceed to scan actual repos without a manifest. The safe channel is to analyze the *known* repos from the context and evaluate their README status based on documented references.

### 2. Define README Rubric (Based on Fleet Standards)
A "good" README should have:
- [ ] **Title & Description:** Clear project name and 1-2 sentence purpose.
- [ ] **Core Concepts:** Explains key ideas (e.g., Deadband Protocol, PLATO rooms).
- [ ] **Usage/Examples:** Shows how to run or use the project.
- [ ] **Installation/Setup:** Basic getting started instructions.
- [ ] **Links:** Connection to related fleet projects.
- [ ] **Status:** Indicates active development, version, or stability.

A README "needs improvement" if it is:
- Missing entirely.
- A placeholder (e.g., just "# Project Name").
- Lacks more than 3 of the above criteria.

### 3. Score Known Repos from Context
From the provided files, the following projects are explicitly named:

1. **plato-torch** (Mentioned in Key Technologies, Boot Camp)
2. **plato-ensign** (Key Technologies)
3. **holodeck-rust** (Key Technologies)
4. **fleet-simulator** (Key Technologies)
5. **constraint-theory-core** (Key Technologies)
6. **cudaclaw** (Key Technologies)
7. **flux-runtime** (Key Technologies)
8. **oracle1-vessel** (Mentioned in Boot Camp: Herald)
9. **service-guard.sh** (Boot Camp: Sentinel) *[Note: likely a script, not a repo]*
10. **DeadbandRoom** (Boot Camp: Tinker) *[Note: likely a preset within plato-torch]*

**Problem:** I cannot access the actual README files for these repos. Their quality cannot be assessed without reading them.

### 4. Conclusion & Output
Given the constraints (only `fleet-knowledge/` snippets provided), I cannot complete the task as stated. The **negative space (P0)** is clear: attempting to list "10 worst READMEs" without file access leads to hallucination.

**Safe Channel (P1) Output:** A procedural analysis and a fallback recommendation.

**Recommendation:**
To actually perform this task, the Navigator agent requires:
1. A `repos.txt` manifest or directory listing of the SuperInstance GitHub organization.
2. Read access to the root of each repository to fetch `README.md`.
3. A script to clone or fetch READMEs for evaluation.

**Proposed Script Outline (P2 within this safe channel):**
```bash
# Pseudo-code for the task if repo list is available
REPO_LIST=$(cat fleet-knowledge/repos.txt)
for repo in $REPO_LIST; do
    curl -s "https://raw.githubusercontent.com/SuperInstance/$repo/main/README.md" > /tmp/readme.md
    score=$(score_readme /tmp/readme.md) # Apply rubric
    echo "$score $repo" >> scores.txt
done
sort -n scores.txt | head -10
```

**Final Artifact (Based on Available Knowledge):**
The 10 project names listed above constitute the known fleet repos. Without README access, I cannot rank them. The **worst-case README scenario** is a missing README. Therefore, any repo lacking a README entirely would top the list.

**Tile Submitted:** This analysis cycle.