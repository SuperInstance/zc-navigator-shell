# Cycle 304

# Cycle 304 — Navigator
**Task:** Scan SuperInstance repos for READMEs that need improvement. List the 10 worst ones.

---

### Step 1 — Map the Negative Space (P0)
**Rocks (what NOT to do):**
- Don't assume all repos are under a single directory path without checking the file system.
- Don't rely on memory or hallucinated repo names; read from actual `fleet-knowledge/` files.
- Don't score READMEs based on subjective "badness"; need objective, verifiable criteria.
- Don't list repos that don't exist in the provided context.
- Don't skip reading the actual README content before assessment.

### Step 2 — Identify Safe Channels (P1)
**Safe Channels (where I CAN operate):**
- I can read the `fleet-knowledge/` directory structure provided in the context.
- I can extract repo names and README status from the `BOOT-CAMP.md` and other docs.
- I can define clear, objective criteria for "needs improvement" based on common README flaws.
- I can list only repos explicitly mentioned in the context to ensure factual accuracy.
- I can provide specific, actionable reasons for each repo's README deficiency.

### Step 3 — Optimize Within Channel (P2)
**Objective Criteria for "Worst" READMEs:**
1. **Missing:** No README file at all.
2. **Placeholder:** Contains only default text (e.g., "# Project Name").
3. **Sparse:** Less than 50 words of meaningful content.
4. **Outdated:** Mentions obsolete versions or broken links.
5. **Unclear:** Fails to explain the project's purpose or how to use it.

**Action:**
1. Scan the provided `fleet-knowledge/` context for all mentioned repos.
2. Assess each against the criteria above.
3. Rank the 10 most deficient.

---

### Scan Results from Fleet Knowledge Context

**Repos Mentioned (from docs):**
- `plato-torch`
- `plato-ensign`
- `holodeck-rust`
- `fleet-simulator`
- `constraint-theory-core`
- `cudaclaw`
- `flux-runtime`

**Additional Repos (from Boot Camp roles):**
- `service-guard.sh` (script, not a repo)
- `oracle1-vessel`
- GitHub API repos (unspecified names)
- `memory/` files (not a repo)

**Analysis:**
The context provides only repo names, not their README contents. I cannot read the actual README files because they are not included in the `fleet-knowledge/` snippet provided. Therefore, I cannot factually assess their quality or list the "10 worst ones."

**Safe Channel Adjustment:**
Since I lack access to the actual READMEs, I must adjust my task to what is possible given the constraints.

**Revised Task Output:**
Based solely on the context, I can identify repos that are **at risk** of poor READMEs because they are:
1. **New or experimental** (likely undocumented).
2. **Mentioned only in passing** (may lack dedicated docs).
3. **Complex projects** where documentation is often lagging.

**List of 7 Repos (from context) That May Need README Improvement:**
1. `plato-ensign` (export tool, may have sparse docs)
2. `cudaclaw` (GPU runtime, likely technical and under-documented)
3. `flux-runtime` (bytecode VM, complex subject)
4. `constraint-theory-core` (niche geometric library)
5. `holodeck-rust` (MUD server, may assume prior knowledge)
6. `fleet-simulator` (training data generator, may lack user guides)
7. `oracle1-vessel` (cloud component, implied from context)

**Recommendation:**
To complete the task as intended, I would need:
- Read access to the actual repo directories.
- Ability to read each README.md file.
- Use tools like `wc -l` for length, `grep` for placeholder patterns.

**Current Limitation:**
The `fleet-knowledge/` context provided does not include the file contents of these repos, only their names in documentation. Therefore, a factual "10 worst" list cannot be produced without hallucination.

**Next Action (P0):**
Do not guess. Report the constraint and suggest a path forward: request README file contents or directory listings for assessment.

---

**Tile Submitted:** Navigator identifies constraint—cannot assess READMEs without file access. Proposes safe channel: list repos at risk based on context, recommends obtaining actual README content for proper evaluation.