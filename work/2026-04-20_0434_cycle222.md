# Cycle 222

# Cycle 222 — Navigator
**Task:** Scan SuperInstance repos for READMEs that need improvement. List the 10 worst ones.

---

### Step 1 — Map the Negative Space (P0)
**Rocks (what NOT to do):**
- Don't assume all repos are under a single directory path without checking the actual fleet-knowledge/ structure.
- Don't rely on memory or hallucinated repo names; read only from provided files.
- Don't evaluate READMEs based on subjective "badness"; need objective criteria.
- Don't skip verifying file existence before listing.
- Don't proceed to listing without first establishing a clear, repeatable scoring method.

---

### Step 2 — Identify Safe Channels (P1)
**Safe Channels (where I CAN operate):**
1. I can read the `fleet-knowledge/` directory structure from the context provided.
2. I can infer README quality based on objective, observable metrics:
   - **Presence/Absence:** Does a README file exist?
   - **Length:** Is it extremely short (e.g., < 50 words) or empty?
   - **Structure:** Does it have standard sections (e.g., Description, Usage, Installation)?
   - **Clarity:** Is the purpose of the repo immediately clear from the first paragraph?
   - **Maintenance:** Are there obvious placeholders, TODOs, or outdated information?
3. I can list repos mentioned in the provided context (PROJECT-VISION.md, DEADBAND-PROTOCOL.md, Boot Camp instructions).
4. I can prioritize repos that are **core to the fleet's mission** (PLATO, training, simulation) as their READMEs have higher impact.

**Available Data (from context):**
- Core repos mentioned: `plato-torch`, `plato-ensign`, `holodeck-rust`, `fleet-simulator`, `constraint-theory-core`, `cudaclaw`, `flux-runtime`.
- Other mentioned components: `service-guard.sh`, `DeadbandRoom`, GitHub API, oracle1-vessel.
- The Boot Camp lists 12 agent specializations, implying at least those many domains/tools exist.

---

### Step 3 — Optimize Within Channel (P2)
**Action Plan:**
1. Extract all unique repository/project names from the provided context.
2. For each, assess README status based on the criteria above, using only information present in the context.
3. Rank them by "need for improvement" score (higher = worse).
4. Output the top 10.

**Analysis:**

From the provided files, I identify the following repositories/components:

1.  **plato-torch** (Core) – Mentioned with presets and self-training rooms. Likely has a README, but Boot Camp Phase 1 asks agents to "read" it, implying it exists and is substantive. **Low priority for improvement.**
2.  **plato-ensign** (Core) – Mentioned for exporting LoRA adapters. Likely has documentation. **Low priority.**
3.  **holodeck-rust** (Core) – MUD server with sentiment-aware NPCs. README likely exists. **Low priority.**
4.  **fleet-simulator** (Core) – Fleet simulation for training data. README likely exists. **Low priority.**
5.  **constraint-theory-core** (Core) – Rust geometric snapping. README likely exists. **Low priority.**
6.  **cudaclaw** (Core) – GPU-resident agent runtime. README likely exists. **Low priority.**
7.  **flux-runtime** (Core) – Self-assembling bytecode VM. README likely exists. **Low priority.**
8.  **service-guard.sh** (Script) – Not a repo, but a script. May lack a dedicated README. **High priority** for documentation.
9.  **DeadbandRoom** (Preset) – Part of `plato-torch`. May have inline documentation but not a standalone README. **Medium priority.**
10. **oracle1-vessel** (Directory) – Contains communication bottles (`from-fleet/`). Unclear if it has a README explaining its purpose. **High priority.**
11. **fleet-knowledge** (Directory) – This is the knowledge base itself. Has `docs/` with VISION and PROTOCOL. Its own top-level README status is unknown. **Medium priority.**
12. **The 12 Agent Specializations** (Navigator, Sentinel, Scribe, etc.) – These are roles, not repos. Their tools/scripts may be undocumented.

**Critical Gap:** The context does **not** contain a full file listing of `fleet-knowledge/`. I cannot see which repos actually have READMEs or their content. My assessment is based solely on mentions and logical inference.

**Top 10 Repos/Components Needing README Improvement (Based on Available Evidence):**

1.  **service-guard.sh** – High-impact operational script. Likely lacks a formal README explaining the 5 services and failure modes.
2.  **oracle1-vessel** – Critical communication hub. Needs a README explaining its structure and purpose.
3.  **fleet-knowledge** – The root knowledge base. Should have a README explaining its layout and contribution rules.
4.  **DeadbandRoom** – A key training preset. Should have a detailed README within `plato-torch` explaining its API and use.
5.  **Agent Tooling/Scrips** – Individual tools for Scribe, Curator, Mason, etc., are likely undocumented.
6.  **GitHub API integration scripts** – Mentioned for Curator. Likely need documentation.
7.  **TOOLS.md / API configs** – Mentioned for Alchemist. Should be clearly documented.
8.  **memory/ directory** – Used by Archivist. Could use a README on file naming and structure.
9.  **Boot Camp instructions** – Could be its own `BOOTCAMP.md` file for clarity.
10. **rabbit-trail research docs** – Mentioned for Scout. Likely scattered; need a central README/index.

**Recommendation:** The **P0 step for the fleet** is to generate a complete inventory of all repositories and key files within `fleet-knowledge/` before a meaningful "README improvement" audit can be performed. The current task is premature without that map.

**My Output (Constrained to Safe Channel):**
Based on the context provided, the components most likely to have missing or insufficient READMEs are operational scripts and directories (`service-guard.sh`, `oracle1-vessel`). To complete the task as stated, a script or process must first inventory the actual repository structure.

**Tile Submitted:** Analysis of README improvement task feasibility and prioritized list based on available context.