# Cycle 346

# Cycle 346 — Navigator
**Task:** Scan SuperInstance repos for READMEs that need improvement. List the 10 worst ones.

---

### Step 1 — Map the Negative Space (P0)
**Rocks (what NOT to do):**
- Don't assume all repos are under a single directory path without checking the actual file system.
- Don't rely on hallucinated repo names; read from the actual `fleet-knowledge/` context.
- Don't score READMEs based on subjective "badness"; use objective, observable criteria.
- Don't proceed to listing without first establishing a clear scanning method and criteria.

### Step 2 — Identify Safe Channels (P1)
**Safe Channels (where I CAN operate):**
1. Use the provided `fleet-knowledge/` directory as the source of truth for repo information.
2. Extract repo names and README paths from the actual files present (e.g., from `PROJECT-VISION.md`, `DEADBAND-PROTOCOL.md`, Boot Camp list).
3. Define objective criteria for "needs improvement" based on common README flaws: missing sections, outdated info, broken links, poor structure, lack of examples.
4. Scan only the repos explicitly mentioned in the fleet knowledge; ignore any not documented.

### Step 3 — Optimize Within Channel (P2)
**Optimal Path:**
1. **Extract Repo List:** From the Boot Camp section, I see 12 fleet repos mentioned:
   - `plato-torch`
   - `plato-ensign`
   - `holodeck-rust`
   - `fleet-simulator`
   - `constraint-theory-core`
   - `cudaclaw`
   - `flux-runtime`
   - `service-guard.sh` (script, not a repo)
   - `DeadbandRoom` (preset in plato-torch)
   - `oracle1-vessel/from-fleet/` (directory)
   - `memory/` files
   - GitHub API (external)
   
   Note: The task says "SuperInstance repos," but the provided context only lists these fleet repos. I'll treat "SuperInstance" as synonymous with the Cocapn fleet for this scan.

2. **Define Criteria for "Needs Improvement":** A README needs improvement if it exhibits one or more of:
   - **Missing:** No README file present.
   - **Skeleton:** Only placeholder text (e.g., "TODO," "# Project Name" with no content).
   - **Outdated:** Mentions old versions, deprecated APIs, or inactive projects.
   - **Unclear:** Lacks a clear purpose, installation, usage examples, or contribution guidelines.
   - **Broken:** Contains dead links or incorrect code snippets.

3. **Scan and Evaluate:** Since I cannot directly access the file system, I must rely on the context provided. The `fleet-knowledge/` docs give insights into some repos:
   - `plato-torch`: Mentioned with 25 presets, self-training rooms. Likely has a README.
   - `plato-ensign`: Exports room experience. Likely has a README.
   - `holodeck-rust`: MUD server. Likely has a README.
   - `fleet-simulator`: Fleet simulation. Likely has a README.
   - `constraint-theory-core`: Rust geometric snapping. Likely has a README.
   - `cudaclaw`: GPU-resident agent runtime. Likely has a README.
   - `flux-runtime`: Self-assembling bytecode VM. Likely has a README.
   - `service-guard.sh`: Not a repo; a script. No README.
   - `DeadbandRoom`: A preset within `plato-torch`, not a separate repo.
   - `oracle1-vessel/from-fleet/`: A directory, not a repo.
   - `memory/`: A directory, not a repo.
   - GitHub API: External, not a fleet repo.

   However, without actual README content, I cannot objectively score them. The Boot Camp task for **Scribe** is to "Read 5 READMEs from fleet repos. Score them." This implies READMEs exist and are scorable. But as Navigator, I lack access to those files here.

4. **Make a Factual List Based on Available Evidence:** Given the constraints, I can only list repos that are explicitly mentioned as having poor or missing documentation in the context. The `fleet-knowledge/` docs do not provide any README content or scores. Therefore, I cannot reliably list "the 10 worst ones" because:
   - Only ~7 repos are confirmed (the 7 listed above).
   - I have not read their READMEs.
   - No READMEs are criticized in the provided files.

**Output:**
Based on the **Deadband Protocol**, I must not guess or hallucinate. The safe channel is to report the factual limit of my knowledge.

**Result:** I cannot complete the task as stated because the necessary data (README files) is not accessible in the `fleet-knowledge/` context provided. To proceed, I would need:
- Direct file system access to the fleet repos.
- Or, README content included in the context.

**Recommendation (P2 Optimization within Constraint):** 
Instead, I can list the **7 fleet repos that likely have READMEs** and flag them as "requires manual inspection" for improvement. This is the best possible output given the constraints.

### List of Fleet Repos (Requiring README Inspection):
1. `plato-torch`
2. `plato-ensign`
3. `holodeck-rust`
4. `fleet-simulator`
5. `constraint-theory-core`
6. `cudaclaw`
7. `flux-runtime`

**Note:** This list is not a "worst 10" but all known repos. Without reading them, I cannot rank or identify flaws. The next step for the fleet would be to run a script that scans these repos' READMEs against the objective criteria above.

**Tile Submitted:** This analysis itself is a tile—documenting the constraint and method for README improvement scanning.

---

**Navigator Action:** Task blocked by data access; provided constrained output and method. Awaiting further instructions or file context.