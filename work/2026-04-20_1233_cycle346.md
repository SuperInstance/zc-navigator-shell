# Cycle 346

# Cycle 346 — Navigator
**Task:** Scan SuperInstance repos for READMEs that need improvement. List the 10 worst ones.

---

### Step 1 — Map the Negative Space (P0)
**Rocks (what NOT to do):**
- Don't assume all repos are under a single directory path without checking the actual file system.
- Don't rely on memory or hallucination; read the actual repo list from the provided fleet-knowledge.
- Don't evaluate READMEs based on subjective "badness"; define objective, measurable criteria.
- Don't skip repos that lack a README file entirely—they are candidates for the worst.
- Don't proceed to ranking without first scanning and scoring all repos.

### Step 2 — Identify Safe Channels (P1)
**Safe Channels (where I CAN operate):**
- I can read the actual file list from `fleet-knowledge/` to discover repo names and structures.
- I can define a scoring rubric for README quality based on observable, file-based criteria.
- I can examine each repo's README file (or lack thereof) and score it against the rubric.
- I can sort the scores to identify the 10 lowest-scoring repos.
- I can output a factual list with specific, cited reasons for each low score.

### Step 3 — Optimize Within Channel (P2)
**Optimal Path:**
1. **Acquire Repo List:** Read the `fleet-knowledge/` directory to find all repo references.
2. **Define Rubric:** Establish a simple, objective scoring system (0-5 points) based on:
   - **Existence (1 pt):** README.md file present.
   - **Length (1 pt):** More than 50 words of content (excluding headers, code blocks).
   - **Structure (1 pt):** Contains at least two of: Description, Installation, Usage, API, Examples.
   - **Clarity (1 pt):** Uses clear language and avoids unexplained jargon.
   - **Links (1 pt):** Contains at least one working link (to code, docs, or related repo).
3. **Scan & Score:** For each repo found, locate its README, evaluate against rubric, record score.
4. **Rank & List:** Sort by score ascending, take the 10 lowest, document each with specific deficiencies.
5. **Output:** Present the list with repo name, score, and concrete reasons for low score.

---

## Execution

### 1. Acquire Repo List
I will examine the `fleet-knowledge/` directory structure from the provided context. The files mention several key repos: `plato-torch`, `plato-ensign`, `holodeck-rust`, `fleet-simulator`, `constraint-theory-core`, `cudaclaw`, `flux-runtime`. The Boot Camp phase assignments also reference `service-guard.sh`, `DeadbandRoom`, GitHub API, `oracle1-vessel`, and various memory files. However, I need the actual list.

**Observation from context:** The provided files are snippets within `fleet-knowledge/docs/` and `fleet-knowledge/` root. I do not have a full directory listing. To proceed factually, I must work only with repos explicitly named in the provided text.

**List of repos explicitly named in the provided fleet-knowledge files:**
1. `plato-torch`
2. `plato-ensign`
3. `holodeck-rust`
4. `fleet-simulator`
5. `constraint-theory-core`
6. `cudaclaw`
7. `flux-runtime`

These seven are confirmed. I will also include `oracle1-vessel` as it is referenced as a directory. I will treat these eight as the initial scan set.

### 2. Define and Apply Rubric
For each repo, I will check for a README.md in its likely location (e.g., `fleet-knowledge/repo-name/README.md`). Since I cannot access files outside the provided snippets, I will base scores on the presence/absence of README content in the provided context.

**Scoring results based on provided context:**

- **`plato-torch`**: Mentioned in Boot Camp (Mason, Weaver tasks). README likely exists but content unknown. **Assumed score: 3** (exists, likely has structure, clarity uncertain).
- **`plato-ensign`**: Mentioned in Key Technologies. README unknown. **Assumed score: 2**.
- **`holodeck-rust`**: Mentioned in Key Technologies. README unknown. **Assumed score: 2**.
- **`fleet-simulator`**: Mentioned in Key Technologies. README unknown. **Assumed score: 2**.
- **`constraint-theory-core`**: Mentioned in Key Technologies. README unknown. **Assumed score: 2**.
- **`cudaclaw`**: Mentioned in Key Technologies. README unknown. **Assumed score: 2**.
- **`flux-runtime`**: Mentioned in Key Technologies. README unknown. **Assumed score: 2**.
- **`oracle1-vessel`**: Referenced as directory (`oracle1-vessel/from-fleet/`). README unknown. **Assumed score: 1** (may exist, but less likely).

**This approach is flawed**—it relies on assumptions, not actual file reads. This violates P0: I am hitting the rock of hallucination. I must adjust.

### 3. Corrective Action (P0 Revisit)
**Rock identified:** I do not have access to the actual README files for these repos within the provided context. Proceeding with assumed scores is not factual.

**Safe channel:** I can only evaluate READMEs that are **provided in the context**. The only README-like content provided is:
- `fleet-knowledge/docs/PROJECT-VISION.md` (not a repo README)
- `fleet-knowledge/docs/DEADBAND-PROTOCOL.md` (not a repo README)
- The Boot Camp instructions (not READMEs)

Therefore, **I cannot complete the task as stated** because the necessary files are not provided. However, I can output a factual statement about this limitation and provide a method for when the files are available.

### 4. Adjusted Output (Factual)
**Finding:** Based on the provided `fleet-knowledge` context, I cannot scan READMEs for SuperInstance repos because the actual repo directories and their README files are not included in the provided snippets. The task requires access to the file system beyond the given documents.

**Recommendation:** To complete this task, the agent needs:
1. A full directory listing of `fleet-knowledge/` showing repo folders.
2. Read access to each repo's `README.md` file.

**Procedural Template for When Files Are Available:**
```
README Quality Rubric (0-5):
- Existence: README.md present? (1 pt)
- Length: >50 words of prose? (1 pt)
- Structure: Contains ≥2 of Description, Installation, Usage, API, Examples? (1 pt)
- Clarity: Language clear, jargon explained? (1 pt)
- Links: Contains ≥1 working link? (1 pt)

Scan each repo, score, rank ascending. The 10 lowest scores are "worst".
```

**List of Repos Mentioned in Context (for future scanning):**
1. plato-torch
2. plato-ensign
3. holodeck-rust
4. fleet-simulator
5. constraint-theory-core
6. cudaclaw
7. flux-runtime
8. oracle1-vessel

**Conclusion:** The task cannot be completed with the provided data. The next action should be to request the necessary file access or proceed to a different backlog task.

---

**Tile Summary:** Navigator attempted task "Scan SuperInstance repos for READMEs that need improvement" but hit a P0 rock: insufficient file access. Mapped the negative space (no README files provided), identified safe channel (define rubric, list mentioned repos), and optimized by providing a procedural template and factual limitation statement.