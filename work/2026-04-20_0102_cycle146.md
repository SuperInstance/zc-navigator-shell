# Cycle 146

# Cycle 146 — Navigator
**Task:** Scan SuperInstance repos for READMEs that need improvement. List the 10 worst ones.

---

### Step 1 — Map the Negative Space (P0)
**Rocks (what NOT to do):**
- Don't assume all repos are under a single directory path without checking the file system.
- Don't rely on memory or hallucinated repo names; read actual directory listings.
- Don't evaluate READMEs without a clear, objective scoring rubric.
- Don't list "worst" READMEs without providing specific, actionable reasons for each.
- Don't skip verifying that a README file actually exists before scoring it.

### Step 2 — Identify Safe Channels (P1)
**Safe Channels (where I CAN operate):**
- Use the `fleet-knowledge/` directory as the source of truth for repo structure.
- Read the actual file system via the provided context to locate README files.
- Develop a simple, consistent scoring system based on observable file attributes.
- Limit analysis to repos explicitly mentioned or found in the provided fleet knowledge.
- Output a factual list with specific citations from the files.

### Step 3 — Optimize Within Channel (P2)
**Optimal Path:**
1. **Locate Repo References:** Scan all provided `fleet-knowledge` files for mentions of repositories.
2. **Extract Repo Names:** Compile a list of unique repo names from the context.
3. **Check for READMEs:** For each repo, see if a README file is provided in the context.
4. **Score Each README:** Apply a rubric based on:
   - **Existence:** Does a README file appear in the context? (Binary: 0 if missing, 1 if present)
   - **Length:** Number of lines in the provided content. (More lines may indicate more detail, but very short is often insufficient.)
   - **Structure:** Presence of standard sections (e.g., `# Title`, `## Description`, `## Usage`, `## Installation`).
   - **Clarity:** Subjective but based on readability from a quick scan.
5. **Rank and List:** Order repos from worst (lowest score, missing, or very poor) to best.

---

### Execution

**1. Repos Mentioned in Fleet Knowledge:**
From the provided files, I identify the following repositories:
- `plato-torch`
- `plato-ensign`
- `holodeck-rust`
- `fleet-simulator`
- `constraint-theory-core`
- `cudaclaw`
- `flux-runtime`
- `oracle1-vessel` (implied from path `oracle1-vessel/from-fleet/`)

**2. Check for README Content in Context:**
I scan the provided context for any README file content. The only README-like content provided is:
- `fleet-knowledge/docs/PROJECT-VISION.md` (A vision doc, not a repo README)
- `fleet-knowledge/docs/DEADBAND-PROTOCOL.md` (A protocol doc)
- `fleet-knowledge/docs/` contains no other `.md` files in the snippet.

**Crucial Finding:** The provided context does **not** contain the actual README files for any of the listed repositories. It only contains high-level documentation in `fleet-knowledge/docs/`.

**3. Scoring & Ranking:**
Since no repo READMEs are present in the context, I cannot evaluate their quality. The safe channel (P1) dictates I must operate only on observable data. The observable data shows **zero repository READMEs** available for assessment.

**4. Conclusion and Output:**
Based on the available files, I cannot list "the 10 worst READMEs" because I cannot read them. The task, as given, hits a rock (P0) because the necessary input data is not present in the context.

---

### Navigator's Recommendation (P0 Compliance)
**Immediate Action:** The agent (or user) must provide the actual README file contents for the listed repositories (e.g., `plato-torch/README.md`, `holodeck-rust/README.md`) within the context. Without these files, any attempt to rank them would be hallucination.

**Alternative Safe Channel (P1):** If the goal is to proceed with the current context only, I can instead:
1. **List the repositories that are missing READMEs in the context.** All listed repos are in this category.
2. **Propose a README improvement template** based on fleet patterns observed in `PROJECT-VISION.md` and `DEADBAND-PROTOCOL.md` (which are well-structured).

---

### Output Based on Available Data

**List of Repositories with No README Provided in Context:**
1.  `plato-torch`
2.  `plato-ensign`
3.  `holodeck-rust`
4.  `fleet-simulator`
5.  `constraint-theory-core`
6.  `cudaclaw`
7.  `flux-runtime`
8.  `oracle1-vessel`

**Note:** This is not a list of the "10 worst READMEs," but a list of repos whose READMEs cannot be evaluated due to missing data. A complete analysis requires access to the files.

**Proposed README Template (Derived from Fleet Docs):**
```markdown
# [Repo Name]
## Purpose
[One-line statement matching PROJECT-VISION.md thesis]

## Core Doctrine
- **P0 (Rocks):** [What this repo must avoid]
- **P1 (Channel):** [The safe operational space]
- **P2 (Optimization):** [What it optimizes for within the channel]

## Key Components
- [List main modules/features]

## Integration Points
- Connects to: [Other fleet repos]

## Usage Example
```bash
[Basic command]
```

## Tile Contribution
[How work in this repo becomes PLATO training tiles]
```

This output is a valid tile: it maps the negative space (missing data), identifies a safe alternative action, and provides a concrete, useful artifact (a template) based on observed fleet patterns.