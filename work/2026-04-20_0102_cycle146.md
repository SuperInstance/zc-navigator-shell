# Cycle 146

# Cycle 146 — Navigator
**Task:** Scan SuperInstance repos for READMEs that need improvement. List the 10 worst ones.

---

### Step 1 — Map the Negative Space (P0)
**Rocks (what NOT to do):**
- Don't assume all repos are under a single directory path without checking the actual file system.
- Don't rely on memory or hallucination; read the actual files from `fleet-knowledge/`.
- Don't produce a list without clear, objective criteria for "worst."
- Don't skip verifying each repo's README exists and is accessible.

### Step 2 — Identify Safe Channels (P1)
**Safe Channels (where I CAN operate):**
- Use the `fleet-knowledge/` directory as the source of truth.
- Scan for repository references in documentation and memory files.
- Define "worst" based on observable metrics: missing README, placeholder content, outdated information, poor formatting, or lack of examples.
- Limit scope to repositories explicitly mentioned in the provided fleet knowledge to avoid speculation.

### Step 3 — Optimize Within Channel (P2)
**Optimal Path:**
1. Extract all repository names from the provided `fleet-knowledge/` files.
2. For each repo, check if a README file exists in the knowledge base.
3. Evaluate each README against defined criteria.
4. Rank and list the 10 with the most severe issues.

---

### Execution

**Repositories Identified from Fleet Knowledge:**
- plato-torch
- plato-ensign
- holodeck-rust
- fleet-simulator
- constraint-theory-core
- cudaclaw
- flux-runtime
- oracle1-vessel (implied from context)
- jetsonclaw1 (implied from context)
- forgemaster (implied from context)

**Criteria for "Worst" README:**
1. **Missing:** No README file found.
2. **Placeholder:** Contains only template text (e.g., "# Project Name").
3. **Outdated:** Mentions obsolete versions or deprecated tools.
4. **Sparse:** Lacks installation, usage, or contribution guidelines.
5. **Unformatted:** Poor Markdown structure, no sections.

**Analysis of Available READMEs in `fleet-knowledge/`:**
- `PROJECT-VISION.md` and `DEADBAND-PROTOCOL.md` are present but are project-wide docs, not repo-specific READMEs.
- No repo-specific README files are provided in the given knowledge snippet.

**Finding:** Based on the provided `fleet-knowledge/` files, **no repository READMEs are available for evaluation**. The task cannot be completed with the current data.

---

### Adjusted Output
Given the constraint of reading only actual files, I cannot list "10 worst READMEs" because none are present in the provided context. Instead, I will:

**P0 Revised:** Do not fabricate READMEs or scores.
**P1 Revised:** Report the factual absence and suggest a next step.
**P2 Revised:** Propose a method for when READMEs are available.

**Recommendation:**
To complete this task, the Navigator requires access to the actual repository directories (e.g., `plato-torch/README.md`). A script could be run to scan all fleet repos, score READMEs using the criteria above, and output a ranked list. Until then, the task is blocked on data availability.

**Current Status:** Task incomplete due to insufficient data in `fleet-knowledge/`. No READMEs to evaluate.

---

**Tile Submitted:** Factual report on README availability.
**Next Action:** If granted access to repo file trees, proceed with scanning and ranking.