# Cycle 211

# Cycle 211 — Navigator
**Task:** Scan SuperInstance repos for READMEs that need improvement. List the 10 worst ones.

---

### Step 1 — Map the Negative Space (P0)
**Rocks (what NOT to do):**
- Don't assume all repos are under a single directory path without checking the actual file system.
- Don't rely on memory or hallucinated repo names; read the actual `fleet-knowledge/` directory.
- Don't evaluate READMEs without a clear, objective scoring rubric.
- Don't list more than 10 repos if fewer than 10 exist.
- Don't proceed to scoring (P2) without first establishing a method to locate and read the repos (P1).

### Step 2 — Identify Safe Channels (P1)
**Safe Channels (where I CAN operate):**
1. I have access to the `fleet-knowledge/` directory via the provided context.
2. I can read the file `fleet-knowledge/docs/BOOT-CAMP.md` to understand the expected structure.
3. I can search for repository references within the knowledge base to infer likely repo names/paths.
4. I can define a simple, objective scoring rubric based on observable README characteristics.
5. I can output a list based only on repos I can actually verify from the context.

**Action:** Read the available files to locate repository information.

**Findings:**
- From `BOOT-CAMP.md`: Repos mentioned include `plato-torch`, `plato-ensign`, `holodeck-rust`, `fleet-simulator`, `constraint-theory-core`, `cudaclaw`, `flux-runtime`.
- From `PROJECT-VISION.md` and `DEADBAND-PROTOCOL.md`: No additional repo names.
- The context provides no direct file listing or directory tree for `fleet-knowledge/`. I cannot verify the existence or contents of READMEs for these repos from the given snippets alone.

**Constraint:** I lack the necessary file system access to scan actual repository directories. The task as stated cannot be completed factually without that data.

### Step 3 — Optimize Within Channel (P2)
**Revised Action:** Given the constraint, I will:
1. Propose a clear, actionable scoring rubric for README quality.
2. List the 7 repos named in the boot camp as candidates for scanning.
3. Recommend the next step to obtain the missing data.

**README Scoring Rubric (Proposed):**
- **Score 0 (Missing):** No README file present.
- **Score 1 (Skeleton):** Only a title or one-line description.
- **Score 2 (Basic):** Has sections (e.g., Description, Usage) but lacks examples, installation, or API details.
- **Score 3 (Good):** Contains installation, usage examples, and basic API documentation.
- **Score 4 (Excellent):** Comprehensive: problem statement, architecture, examples, contribution guidelines, and links to related projects.

**Candidate Repositories (from boot camp text):**
1. `plato-torch`
2. `plato-ensign`
3. `holodeck-rust`
4. `fleet-simulator`
5. `constraint-theory-core`
6. `cudaclaw`
7. `flux-runtime`

**Recommendation:** To complete the task, the agent (or a fleet process) needs to:
- Run a script to locate these repos in the filesystem.
- For each, check for a README.md (or similar) and score it against the rubric.
- Sort by score (lowest = worst) and return the top 10.

**Output:** Given the current context, I cannot list the 10 worst READMEs. I can only provide the framework to do so once the data is available. The safe channel is to report this limitation and propose a concrete method.

**Tile Submitted:** Navigator_211_README_Scan_Method.