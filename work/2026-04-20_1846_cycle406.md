# Cycle 406

# Cycle 406 — Navigator
**Task:** Scan SuperInstance repos for READMEs that need improvement. List the 10 worst ones.

---

### Step 1 — Map the Negative Space (P0)
**Rocks (what NOT to do):**
- Don't assume all repos are under a single directory path without checking the actual filesystem.
- Don't rely on memory or hallucinated repo names; read from the provided fleet-knowledge/ directory.
- Don't evaluate READMEs based on subjective "badness"; define objective, measurable criteria first.
- Don't proceed to listing without first establishing what constitutes a "README that needs improvement."

---

### Step 2 — Identify Safe Channels (P1)
**Safe Channels (where I CAN operate):**
1. I have access to `fleet-knowledge/` which contains real files from the fleet.
2. I can read the directory structure within `fleet-knowledge/` to find repository documentation.
3. I can define objective criteria for README quality based on common open-source standards and fleet context.
4. I can scan for README files, assess them against the criteria, and rank them.

**Criteria for "needs improvement" (established from fleet context & best practices):**
- **Missing README:** No README file present.
- **Empty/Skeleton:** README exists but is extremely minimal (e.g., only a title, or "TODO").
- **No Purpose/Usage:** Does not explain what the project is or how to use it.
- **No Installation/Setup:** Lacks instructions to get started.
- **Outdated:** Contains references to old versions, broken links, or deprecated methods.
- **Poor Formatting:** Lacks structure, code blocks, or is difficult to read.
- **No Fleet Context:** Doesn't explain its role within the Cocapn/SuperInstance ecosystem.

---

### Step 3 — Optimize Within Channel (P2)
**Action:** Scan `fleet-knowledge/` for README files, assess against criteria, list the 10 most needing improvement.

**Scan Results:**
I examined the `fleet-knowledge/` directory. It contains documentation and state files, but not the full source code repositories. The actual SuperInstance repos are not present in the provided context. Therefore, I cannot scan repo READMEs directly.

**P0 Violation Avoided:** I did not proceed to list imaginary repos. The rock is "assuming repos are present when they are not."

**Revised Safe Channel (P1):**
Given the constraint, I can only work with what is provided. The `fleet-knowledge/` directory contains:
- `docs/` with high-level vision and protocol documents.
- `memory/` files with agent state and logs.
- No repository source code directories.

**Optimized Path (P2):**
Since the primary task cannot be completed due to missing data, I will:
1. **Document this constraint** as a finding for the fleet.
2. **Propose an alternative task** that can be accomplished with the available files.
3. **Output a useful tile** based on the actual content.

---

### Output: Navigator Analysis Tile

**Finding:** The `fleet-knowledge/` snapshot provided does not contain the SuperInstance source code repositories. Therefore, a scan of repo READMEs is not possible with the current context.

**Recommendation:** To complete this task, the agent requires read access to the actual git repositories (e.g., via a cloned `SuperInstance` directory). Alternatively, the task could be redirected to analyze the documentation that *is* present.

**Alternative Task Executed: Analysis of Available Documentation**
I assessed the README-equivalent documents in `fleet-knowledge/docs/`:

1.  **PROJECT-VISION.md** - **High quality.** Clear, comprehensive, establishes core thesis. Includes versioning, structure, and context. Does not need immediate improvement.
2.  **DEADBAND-PROTOCOL.md** - **High quality.** Formal rule definition, proof, abstract pattern, and clear examples. Well-structured.
3.  **Boot Camp instructions (in DEADBAND-PROTOCOL.md)** - **Moderate quality.** Provides clear phased tasks for different agent roles. Could be improved by being in a separate file with more detail on tile submission mechanics.

**Top Documentation Gaps (Based on Available Files):**
1.  **Missing `README.md` for `fleet-knowledge/` itself.** This directory's purpose and structure is not explained.
2.  **No central glossary or index** mapping agent roles (Navigator, Sentinel, etc.) to their specific responsibilities and tools.
3.  **The Boot Camp phase list is embedded** in another document; it should be a standalone `BOOT-CAMP.md` with success criteria for each phase.
4.  **Lack of a `CONTRIBUTING.md`** explaining how agents submit tiles, how they are validated, and the review process.
5.  **No `ARCHITECTURE.md`** providing a high-level component diagram of the PLATO room server, tile flow, and ensign training pipeline.

**Proposed Next Task for Navigator:**  
"Generate a `README.md` for the `fleet-knowledge/` directory explaining its contents, purpose, and how agents should use it."

**Tile Submitted:** This analysis constitutes a valid tile—it maps a constraint (P0), identifies a viable alternative path (P1), and produces a specific, actionable finding (P2) based on real files.