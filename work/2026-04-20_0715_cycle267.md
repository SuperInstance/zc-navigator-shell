# Cycle 267

# Cycle 267 — Navigator
**Task:** Scan SuperInstance repos for READMEs that need improvement. List the 10 worst ones.

---

### Step 1 — Map the Negative Space (P0)
**Rocks (what NOT to do):**
- Don't assume all repos are under a single directory path without checking the actual fleet-knowledge/ structure.
- Don't rely on memory or hallucination; read actual files.
- Don't list repos that don't exist in the provided knowledge.
- Don't proceed to scoring without first establishing clear, objective criteria for "worst."
- Don't ignore the fleet's own documentation standards if they exist.

### Step 2 — Identify Safe Channels (P1)
**Safe Channels (where I CAN operate):**
1. I have access to the `fleet-knowledge/` directory context provided.
2. I can read the `BOOT-CAMP.md` file to understand the Scribe's role and README scoring criteria.
3. I can search for README files within the provided knowledge snippets.
4. I can define a simple, objective scoring system based on observable file attributes (presence, length, structure).
5. I can list only repositories mentioned or implied in the provided files.

### Step 3 — Optimize Within Channel (P2)
**Action Plan:**
1. **Locate README criteria:** Read `BOOT-CAMP.md` for Scribe's instructions.
2. **Gather repo list:** Extract all repository names from the provided knowledge.
3. **Assess READMEs:** For each repo, check if a README is present in the context. If present, evaluate based on simple heuristics (e.g., has a # Title, has some structure).
4. **Score & Rank:** Apply a scoring system. Lower score = worse.
5. **Output:** List the 10 repos with the lowest scores, with reasons.

---

### Execution

**1. Criteria from Boot Camp (Scribe Role):**
From `BOOT-CAMP.md`:
> "**Scribe**: Read 5 READMEs from fleet repos. Score them."
> "**Scribe**: Rewrite 3 fleet READMEs to standard format."

This implies a "standard format" exists, but it is not defined in the provided snippets. Therefore, I will use a minimal, objective baseline: a README should exist and contain basic structure (a primary heading and some descriptive text).

**2. Repository List from Fleet Knowledge:**
From the provided files, the following repositories are explicitly named:
- `plato-torch`
- `plato-ensign`
- `holodeck-rust`
- `fleet-simulator`
- `constraint-theory-core`
- `cudaclaw`
- `flux-runtime`

The `PROJECT-VISION.md` also mentions the overarching project: `Cocapn/SuperInstance`.

**3. Assessment & Scoring:**
I will search the provided context for README content related to each repo.

**Scoring System (0-3):**
- **3 (Good):** README content is provided in context and has clear structure (e.g., `# Title`, sections).
- **2 (Adequate):** README content is provided but is minimal or fragmented.
- **1 (Poor):** Repository is mentioned, but no README content is provided in context.
- **0 (Missing):** No repository mention or README found.

**Findings:**

*   `plato-torch`: Mentioned multiple times. No README content provided in snippets. **Score: 1**
*   `plato-ensign`: Mentioned. No README content provided. **Score: 1**
*   `holodeck-rust`: Mentioned. No README content provided. **Score: 1**
*   `fleet-simulator`: Mentioned. No README content provided. **Score: 1**
*   `constraint-theory-core`: Mentioned. No README content provided. **Score: 1**
*   `cudaclaw`: Mentioned. No README content provided. **Score: 1**
*   `flux-runtime`: Mentioned. No README content provided. **Score: 1**
*   `Cocapn/SuperInstance`: Has extensive `PROJECT-VISION.md` and `DEADBAND-PROTOCOL.md`. These serve as high-level docs, but a traditional `README.md` is not provided in snippets. **Score: 2** (Adequate, due to other core docs).

**Note:** The provided `fleet-knowledge/` context contains documentation files (`PROJECT-VISION.md`, `DEADBAND-PROTOCOL.md`, `BOOT-CAMP.md`) but does not contain the actual README files for the individual code repositories. Therefore, I cannot assess their quality beyond noting their absence from the given context.

**4. The 10 Worst READMEs (Based on Available Data):**
Given the limited set of 8 identified repos and the scoring, all code repos score `1` ("Poor" - mentioned but no README provided). The `Cocapn/SuperInstance` project scores `2`.

**List (Prioritized by score, then alphabetically):**
1.  `plato-torch` - Score 1. No README content in context.
2.  `plato-ensign` - Score 1. No README content in context.
3.  `holodeck-rust` - Score 1. No README content in context.
4.  `fleet-simulator` - Score 1. No README content in context.
5.  `constraint-theory-core` - Score 1. No README content in context.
6.  `cudaclaw` - Score 1. No README content in context.
7.  `flux-runtime` - Score 1. No README content in context.
8.  *(Only 7 code repos identified. The remaining 2 slots in a "top 10" cannot be filled with factual data from the provided context.)*

**Conclusion:**
Based **strictly on the provided `fleet-knowledge/` context**, the READMEs for all specific code repositories (`plato-torch`, `plato-ensign`, `holodeck-rust`, `fleet-simulator`, `constraint-theory-core`, `cudaclaw`, `flux-runtime`) are the most in need of improvement, as no content for them is present to evaluate. A proper scan would require filesystem access to the actual repositories, which is not provided in this session's context.

**P0 Adherence Check:** I did not assume the existence of unmentioned repos. I worked only with the data provided. The negative space was "missing README content in context," and I operated within that constraint.

---
**Tile Submitted:** Analysis of README availability in provided fleet-knowledge context.
**Next Task Recommendation:** To complete this task accurately, the Navigator requires direct filesystem access to the `SuperInstance` GitHub organization or local clone to read the actual README.md files.