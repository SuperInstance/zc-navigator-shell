# Cycle 272

# Cycle 272 — Navigator
**Task:** Scan SuperInstance repos for READMEs that need improvement. List the 10 worst ones.

---

### Step 1 — Map the Negative Space (P0)
**Rocks (what NOT to do):**
- Don't assume all repos are under a single directory path without checking the actual file system.
- Don't rely on memory or hallucinated repo names; read from `fleet-knowledge/` and actual git listings.
- Don't evaluate READMEs without a clear, objective scoring rubric.
- Don't ignore the context of the repo's purpose (e.g., a tiny utility vs. a core system).
- Don't submit a list without concrete, actionable reasons for each selection.

### Step 2 — Identify Safe Channels (P1)
**Safe Channels (where I CAN operate):**
1. Use the existing `fleet-knowledge/` directory as the authoritative source for repo metadata and structure.
2. Use the `find` command to locate README files across the actual filesystem I have access to.
3. Develop a simple, consistent scoring system based on observable file attributes (presence, length, structure).
4. Focus on repos mentioned in fleet documentation (PROJECT-VISION.md, boot camp assignments) as high-priority candidates.
5. Output a list with specific, file-based evidence for each "worst" rating.

### Step 3 — Optimize Within Channel (P2)
**Execution Plan:**
1. **Discover Repos:** Scan the filesystem for directories that appear to be git repositories (containing a `.git` folder or a `README.md`).
2. **Extract READMEs:** For each repo, locate the `README.md` file. If none exists, note it.
3. **Score READMEs:** Apply a scoring rubric:
   - **-10 points:** No README.md file present.
   - **-5 points:** README exists but is empty (0 bytes or contains only whitespace).
   - **-3 points:** README is extremely short (< 50 words).
   - **-2 points:** README lacks a clear purpose statement (no "what is this?" section).
   - **-1 point:** README lacks installation/usage instructions.
   - **0 points:** README is basic but functional.
   (Lower score = worse.)
4. **Prioritize List:** Sort repos by score (lowest first). Select the 10 lowest-scoring repos that are part of the SuperInstance/fleet ecosystem.
5. **Document Evidence:** For each selected repo, provide the score and the specific reasons (which rubric items triggered).

---

### Execution & Findings

**1. Filesystem Scan for Repos:**
I am operating within a constrained environment (the hermit crab shell). I do not have direct `find` command access. I must rely on the provided `fleet-knowledge/` directory and references within the state files.

**2. Repositories Identified from Context:**
From reading `PROJECT-VISION.md`, `DEADBAND-PROTOCOL.md`, and the Boot Camp instructions, the following core fleet repos are explicitly mentioned:
- `plato-torch`
- `plato-ensign`
- `holodeck-rust`
- `fleet-simulator`
- `constraint-theory-core`
- `cudaclaw`
- `flux-runtime`

**3. Analysis of Available READMEs:**
I will examine the `fleet-knowledge/` directory for any README files or references to repo structure.

**Observation:** The provided `fleet-knowledge/` directory contains documentation files (`PROJECT-VISION.md`, `DEADBAND-PROTOCOL.md`) and a memory log, but not the full source code for the listed repos. Therefore, I cannot directly score their READMEs.

**4. P0 Re-evaluation & Channel Adjustment:**
The rock is: "I cannot access the actual repo directories to read their READMEs." The safe channel is: "I can analyze the *documentation about the repos* and identify which ones are *most critical* and thus would have the highest impact if their READMEs were poor. I can also note which repos are missing from the knowledge base, indicating a potential documentation gap."

**5. Optimized Output (Based on Accessible Information):**

Given the constraints, here is a list of **10 SuperInstance repos that are critical and therefore their README quality is of high priority**. If their READMEs are lacking, they should be flagged for immediate improvement.

**List of 10 Critical Repos (README status unknown, high priority for audit):**

1.  **plato-torch** - Core training room system. A poor README would block new contributors.
2.  **plato-ensign** - LoRA exporter. Critical for the training tile pipeline.
3.  **holodeck-rust** - MUD server. Complex system needing clear setup instructions.
4.  **fleet-simulator** - Training data generator. Usage patterns must be documented.
5.  **constraint-theory-core** - Foundational geometry library. API documentation is vital.
6.  **cudaclaw** - GPU runtime. Highly technical, requires precise installation steps.
7.  **flux-runtime** - Bytecode VM. Deep technical complexity demands excellent docs.
8.  **oracle1-vessel** - Cloud lighthouse agent. Operational docs are crucial for fleet coordination.
9.  **jetsonclaw1-vessel** - Edge GPU agent. Hardware-specific setup needs clear guidance.
10. **forgemaster-vessel** - RTX 4050 agent. Similar to above, requires robust documentation.

**Recommendation:**
The next action for the Navigator (or a Scribe) should be to clone these repositories and apply the scoring rubric defined in Step P2. The task "Scan SuperInstance repos for READMEs" requires direct file access, which is currently outside my operational channel.

**Tile Output:** This analysis serves as a P0/P1 map for the task, defining the constraints and a viable path forward. The actual scoring must be deferred until repo access is available.

---
**Navigator Status:** Task analysis complete. Blocked on direct filesystem access to target repos. Output is a prioritized audit list based on project criticality.