# Cycle 302

# Cycle 302 — Navigator
**Task:** Scan SuperInstance repos for READMEs that need improvement. List the 10 worst ones.

---

### Step 1 — Map the Negative Space (P0)
**Rocks (what NOT to do):**
- Don't assume all repos are under a single directory path without checking the actual file system.
- Don't rely on memory or hallucinated repo names; read only from provided `fleet-knowledge/` files.
- Don't submit a list without concrete, file-based evidence for each entry.
- Don't ignore the context that "worst" likely means missing, empty, or severely outdated READMEs.
- Don't proceed to listing (P2) without first establishing a method to find and evaluate READMEs (P1).

### Step 2 — Identify Safe Channels (P1)
**Safe Channels (where I CAN operate):**
1. Use the `fleet-knowledge/` directory as the sole source of truth.
2. Search for references to repository structures, READMEs, or documentation within the provided files.
3. Define "worst" based on observable criteria: absence, placeholder text (e.g., "TODO"), or explicit mentions of poor quality.
4. If direct repo scans aren't possible, infer from agent tasks and logs (e.g., Scribe's duty to rewrite READMEs).
5. Output must be factual, citing specific file lines where evidence is found.

### Step 3 — Optimize Within Channel (P2)
**Action:** Analyze provided files for README quality signals.

**Findings:**
From `fleet-knowledge/docs/BOOT-CAMP.md`:
- The **Scribe** agent's Phase 2 task is to "Rewrite 3 fleet READMEs to standard format." This implies at least three READMEs currently need improvement.

From `fleet-knowledge/docs/PROJECT-VISION.md`:
- The document is a vision statement, not a README. It is complete and high-quality, so not a candidate.

From `fleet-knowledge/docs/DEADBAND-PROTOCOL.md`:
- This is a well-structured protocol document. Not a README, and not in need of improvement.

**Inference:** The fleet has multiple repositories (implied by "10 fleet repos" in Navigator's Phase 1 reading list). However, the provided `fleet-knowledge/` snippet does not contain actual repository files or READMEs to evaluate. Therefore, I cannot generate a specific list of "10 worst READMEs" from the given data.

**Optimized Output Given Constraints:**
Based solely on the provided files, I can identify **1 concrete example** of a README needing improvement, inferred from the boot camp task:

1. **Unspecified Fleet Repositories (at least 3)** – Referenced in Scribe's task to "Rewrite 3 fleet READMEs." Location: `BOOT-CAMP.md`, Phase 2 description. Evidence: Direct task assignment implies existing READMEs are substandard.

**Recommendation:** To complete this task accurately, the Navigator requires access to the actual repository file system (e.g., a listing of repo directories and their README.md files) or a knowledge file that catalogs them. The current context is insufficient to list 10 specific repos.

**Tile Submitted:** Analysis of README improvement task based on boot camp documentation.