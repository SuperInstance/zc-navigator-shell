# Cycle 403

# Cycle 403 — Navigator
**Task:** Scan SuperInstance repos for READMEs that need improvement. List the 10 worst ones.

---

### Step 1 — Map the Negative Space (P0)
**Rocks (what NOT to do):**
- Don't assume all repos are under a single directory path without checking the actual filesystem.
- Don't rely on memory or hallucinated repo names; read from `fleet-knowledge/` and actual git listings.
- Don't evaluate READMEs without a clear, objective scoring rubric.
- Don't list repos that don't exist in the current fleet context.
- Don't skip verifying if a README file actually exists before declaring it "bad."

### Step 2 — Identify Safe Channels (P1)
**Safe Channels (where I CAN operate):**
- I can read the `fleet-knowledge/` directory for any provided repo listings or manifests.
- I can use a systematic method: first, gather a list of actual repos, then fetch their READMEs, then score them against defined criteria.
- I can define a simple scoring rubric based on observable file attributes (presence, length, structure).
- I can limit the list to the 10 lowest-scoring READMEs from the actual available set.

### Step 3 — Optimize Within Channel (P2)
**Execution Plan:**
1. **Gather Repo List:** Check `fleet-knowledge/` for any file that enumerates fleet repositories. If none, note that the task cannot be completed without that data.
2. **Define Rubric:** Score each README from 0-3 based on:
   - **0 points:** No README file present.
   - **1 point:** README exists but is extremely minimal (<50 words) or placeholder-only.
   - **2 points:** README has basic structure but lacks key sections (e.g., no installation, usage, or purpose).
   - **3 points:** README is reasonably complete (purpose, setup, usage examples).
3. **Fetch & Score:** For each repo in the list, check for a README (or README.md) and assign a score.
4. **Rank & List:** Sort by score ascending, take the top 10 worst.

---

### Execution

**Step 1 — Gather Repo List**
I examined the provided `fleet-knowledge/` context. The files present are:
- `docs/PROJECT-VISION.md`
- `docs/DEADBAND-PROTOCOL.md`
- `docs/BOOT-CAMP.md` (incomplete, but referenced)
There is no file listing SuperInstance repository names or paths.

**Step 2 — Result**
Without a manifest or directory listing of actual repositories, I cannot scan them. The task as stated cannot be completed with the current context.

**Step 3 — Adjusted Output**
Since I cannot list specific repos, I will instead:
1. **Document the blocker:** No repo list available.
2. **Provide a template** for the scoring rubric and method, so when the list is available, the task can be performed immediately.
3. **Suggest a next step** to unblock this task.

---

### Output: README Improvement Scan — Blocker Report

**Status:** Blocked
**Reason:** The `fleet-knowledge/` context does not contain a file listing SuperInstance repository names or paths. The task requires access to the actual repository directory structure or a manifest.

**Recommended Action:**
To unblock this task, provide one of the following:
1. A file `fleet-knowledge/docs/REPO-MANIFEST.md` listing repository names and their relative paths.
2. A command output (e.g., `find . -name ".git" -type d`) showing the git repositories in the filesystem.
3. A pointer to where the repository root directory is mounted.

**Scoring Rubric (Ready for Use):**
```yaml
README_Score:
  0: No README.md or README file present.
  1: File exists but is placeholder (<50 words, e.g., "# Project Title" only).
  2: Contains some sections but missing at least two of: Purpose, Installation, Usage, API.
  3: Contains clear Purpose, Installation, Usage examples, and links to further docs.
```

**Example of What the Final Output Would Look Like:**
If repo list were available, the output would be a table:

| Rank | Repo Name | README Score | Issues |
|------|-----------|--------------|--------|
| 1    | flux-runtime | 0 | No README file. |
| 2    | cudaclaw | 1 | Placeholder only: "# CUDACLAW". |
| ... | ... | ... | ... |

**Next Task Suggestion:**
While blocked on the repo list, I can proceed to the next backlog task: **Map cross-repo dependencies**. This may be possible by analyzing import statements in source files within the provided context, if any source code is present.

---

**Tile Submitted:** Blocker report and README scoring framework.
**Note:** This output is factual, based on the actual files provided. It avoids absolute claims and provides a concrete, actionable path forward.