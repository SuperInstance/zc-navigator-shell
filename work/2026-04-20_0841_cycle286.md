# Cycle 286

# Cycle 286 — Navigator
**Task:** Scan SuperInstance repos for READMEs that need improvement. List the 10 worst ones.

---

### Step 1 — Map the Negative Space (P0)
**Rocks (what NOT to do):**
- Don't assume all repos are under a single directory path without checking the actual file system.
- Don't rely on memory or hallucinated repo names; read from `fleet-knowledge/` and actual git listings.
- Don't evaluate READMEs without a clear, objective scoring rubric.
- Don't submit a list without providing actionable, specific reasons for each selection.
- Don't skip verifying the current state of a README (e.g., it might have been updated after last scan).

### Step 2 — Identify Safe Channels (P1)
**Safe Channels (where I CAN operate):**
1. Use the existing `fleet-knowledge/` directory as the primary source of truth for repo context.
2. If possible, access a real file listing or git command output to identify SuperInstance repos.
3. Develop a simple, repeatable scoring system based on observable README attributes.
4. Focus on repos mentioned in fleet documentation (PROJECT-VISION.md, boot camp tasks) as high-priority candidates.
5. Provide clear citations from files or observed states to justify each "worst" designation.

### Step 3 — Optimize Within Channel (P2)
**Execution Plan:**
1. **Source Identification:** From the provided `fleet-knowledge/` docs, extract all mentioned repository names.
2. **Scoring Rubric:** Define criteria for a "bad" README:
   - **Missing:** No README file.
   - **Skeleton:** Only a title or placeholder text.
   - **Outdated:** References old versions or broken links.
   - **Non-Informative:** Lacks setup, usage, or purpose.
   - **Non-Standard:** Doesn't follow fleet formatting hints.
3. **Evaluate:** Score each identified repo against the rubric.
4. **Rank & List:** Select the 10 with the lowest scores, providing specific reasons.

---

### Execution & Findings

**Step 1 — Source Identification**
From the provided fleet-knowledge files, the following repositories are explicitly mentioned:
- `plato-torch`
- `plato-ensign`
- `holodeck-rust`
- `fleet-simulator`
- `constraint-theory-core`
- `cudaclaw`
- `flux-runtime`

Additional context from the boot camp task list suggests other repos exist (e.g., for Navigator to map "top 20 fleet repos"), but they are not named here. My safe channel is to work only with the explicitly named repos.

**Step 2 — Apply Scoring Rubric**
I cannot access the actual file system to read these READMEs. Therefore, my only safe inference is based on the **absence of information** in the provided context. A README that is not described, quoted, or its quality discussed in the foundational docs is potentially a candidate for being "non-informative" or lacking a strong standard.

However, the **PROJECT-VISION.md** file is truncated mid-sentence ("Scale Through Inh..."). The **BOOT-CAMP.md** file is also truncated ("Phase 3: Build & Test (Cycles 16-30)\nAgents produce code and integrations.\n\n**Navigator**: Fi"). This is a critical P0 rock: **I am working with incomplete source data**.

**Step 3 — Optimized Output Given Constraints**

Given the constraints (incomplete source files, no filesystem access), the most factual and useful output I can produce is a **methodology and a conditional list**.

**Methodology for a Future, Full Scan:**
1.  Clone the `SuperInstance` GitHub organization or relevant directory.
2.  Run a script to inventory all repos and the presence/size of a README.md.
3.  Apply the scoring rubric above via manual review or simple heuristics (word count, presence of key sections like "## Installation").
4.  Generate a ranked list.

**Top Candidates for README Improvement (Based on Fleet Context Priority):**
The following repos are **critical to the core thesis** (PLATO rooms, training, agents) and therefore their documentation quality is paramount. If their READMEs are lacking, they should be the highest priority to fix.

1.  **plato-torch** - Core training room system. If its README doesn't explain the 25 presets and self-training, it's a major gap.
2.  **plato-ensign** - Exports room experience to LoRA. Must clearly explain the "tile to adapter" pipeline.
3.  **holodeck-rust** - MUD server with sentiment NPCs. Requires clear setup and API docs for interaction.
4.  **fleet-simulator** - Generates training data. Needs examples of simulation scenarios and output formats.
5.  **cudaclaw** - GPU-resident agent runtime. Must have hardware/software requirements and quickstart.
6.  **flux-runtime** - Self-assembling bytecode VM. Requires deep technical explanation.
7.  **constraint-theory-core** - Geometric snapping library. Needs API docs and visual examples.
8.  **(Repo mentioned in Navigator backlog)** - A repo containing "dead code" likely has an outdated/unmaintained README.
9.  **(Repo with "duplicate implementations")** - Such a repo's README probably doesn't clarify its unique purpose vs. others.
10. **(Repo with "unused dependencies")** - Its README likely doesn't accurately reflect its current build state.

**Conclusion:**
My primary P0 finding is that the task cannot be completed definitively without access to the actual repository files. The most valuable tile I can submit is this **protocol for performing the scan** and the **priority list based on architectural importance**. The next Navigator should use this method when granted shell access to the git repos.

**Tile Submitted:** Protocol for README audit and priority ranking based on fleet architecture.