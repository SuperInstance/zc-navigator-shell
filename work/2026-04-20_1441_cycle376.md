# Cycle 376

# Cycle 376 — Navigator
**Task:** Scan SuperInstance repos for READMEs that need improvement. List the 10 worst ones.

---

### Step 1 — Map the Negative Space (P0)
**Rocks (what NOT to do):**
- Don't assume all repos are under a single directory path without checking the actual file system.
- Don't rely on memory or hallucination; read actual files from `fleet-knowledge/`.
- Don't produce a list without clear, objective criteria for "worst."
- Don't skip verifying the existence and content of each README file.
- Don't ignore the fleet's established README format standards if they exist.

### Step 2 — Identify Safe Channels (P1)
**Safe Channels (where I CAN operate):**
- I can read the provided `fleet-knowledge/` directory structure.
- I can define "worst" based on observable metrics: missing README, empty README, placeholder text (e.g., "TODO"), or severe lack of structure.
- I can list up to 10 repos that meet these criteria, based on available data.
- I can note that my scope is limited to the files and references provided in the current context.

### Step 3 — Optimize Within Channel (P2)
**Action:** Scan the provided `fleet-knowledge/` context for repository references and evaluate their README status.

**Findings:**
Based on the provided `fleet-knowledge/docs/` files, I can identify referenced repositories. However, the actual README files for those repos are not provided in the context. Therefore, I cannot directly read or score them. I must operate within the safe channel of what is known.

**Inferred List of Repositories (from context):**
1. `plato-torch`
2. `plato-ensign`
3. `holodeck-rust`
4. `fleet-simulator`
5. `constraint-theory-core`
6. `cudaclaw`
7. `flux-runtime`
8. `oracle1-vessel` (implied from path)
9. `fleet-knowledge` (this repo itself)
10. `superinstance` (referenced in vision)

**Assessment:**
Without access to the actual README files, I cannot determine which are "worst." The safe output is to state the limitation and propose a method.

**Output:**
The task requires scanning READMEs, but the current context (`fleet-knowledge/docs/`) does not contain the README files for the listed repositories. To complete this task accurately, I would need:
- File system access to the SuperInstance repos, or
- The README files included in the context.

**Proposed Method for Future Execution:**
1. **Acquire READMEs:** Access the repo directories or receive the files.
2. **Define Criteria:** Score each README based on:
   - Existence (0 or 1)
   - Length (e.g., >50 words)
   - Structure (presence of sections like Overview, Usage, Installation)
   - Currency (mentions recent updates)
   - Clarity (subjective but can use placeholder detection).
3. **Rank:** Sort by lowest score, list top 10.

**Current Action (Within Safe Channel):**
Since I cannot read the missing READMEs, I will document this constraint and note that the task cannot be completed without further data. This output itself is a valid tile—a lesson in constraint recognition.

**Tile Submitted:** Constraint acknowledgment for Navigator task 376.