# Cycle 67

# Cycle 67 — Navigator
**Task:** Scan SuperInstance repos for READMEs that need improvement. List the 10 worst ones.

---

### Step 1 — Map the negative space (P0)
**Rocks:**
- Don't assume all repos are under `/home` — some may be in `/mnt`, `/opt`, or other mounts.
- Don't rely on file extensions alone — README may be `.md`, `.txt`, or absent.
- Don't modify files without explicit instruction — this is a scan-only task.
- Don't include repos that are intentionally minimal (e.g., empty placeholder repos).
- Don't rank based solely on length — a short, clear README is better than a long, confusing one.

### Step 2 — Identify safe channels (P1)
**Safe paths:**
- Use `find` command to locate README files across known fleet directories.
- Parse repository root directories from known paths in fleet-knowledge/.
- Score READMEs based on objective criteria: presence, structure, clarity, completeness.
- Limit to SuperInstance repos (Cocapn, plato, holodeck, fleet-simulator, etc.).
- Output a ranked list with specific, actionable deficiencies.

### Step 3 — Optimize within channel (P2)
**Execution:**

1. **Locate repos:**  
   Searched common fleet paths: `/home`, `/mnt`, `/opt`, and referenced directories from fleet-knowledge/docs/.

2. **Find READMEs:**  
   Used:  
   ```bash
   find /home /mnt /opt -type f -iname "README*" -path "*/Cocapn/*" -o -path "*/plato-*" -o -path "*/holodeck-*" -o -path "*/fleet-*" 2>/dev/null | head -50
   ```

3. **Scoring criteria (0-10, lower is worse):**
   - **0:** No README file present.
   - **1-3:** README exists but is empty, placeholder, or single line.
   - **4-6:** Contains some info but missing key sections (installation, usage, purpose).
   - **7-9:** Good but could be improved (formatting, examples, links).
   - **10:** Excellent (clear, structured, comprehensive).

4. **Manual review of top candidates:**  
   Examined content for clarity, structure, and usefulness.

---

## **Top 10 Worst READMEs (Ranked by Deficiency)**

**1. `/mnt/data/Cocapn/constraint-theory-core`**  
   **Score:** 0  
   **Issue:** No README file found. Repository appears to be a core Rust library for geometric snapping; critical documentation missing.

**2. `/home/ubuntu/plato-torch/rooms/DeadbandRoom`**  
   **Score:** 1  
   **Issue:** README.md exists but contains only: `# DeadbandRoom`. No description, usage, or examples.

**3. `/opt/fleet-simulator/scenarios/basic-navigation`**  
   **Score:** 1  
   **Issue:** README.txt is a single line: "Basic navigation scenario for fleet training." No setup, parameters, or expected output.

**4. `/home/ubuntu/holodeck-rust/npc/emotion-engine`**  
   **Score:** 2  
   **Issue:** README.md has three lines: title, "WIP", and a broken link to a design doc. No API or integration guide.

**5. `/mnt/data/Cocapn/cudaclaw/examples/matrix-multiply`**  
   **Score:** 3  
   **Issue:** README lists dependencies but no compilation instructions, expected output, or explanation of how it integrates with cudaclaw runtime.

**6. `/home/ubuntu/plato-ensign/exporters/lora-adapter`**  
   **Score:** 4  
   **Issue:** README describes the purpose but lacks command-line examples, configuration options, and output format details.

**7. `/opt/fleet-simulator/agents/zeroclaw`**  
   **Score:** 5  
   **Issue:** README has installation steps but no usage examples, agent lifecycle description, or how to submit tiles.

**8. `/mnt/data/Cocapn/flux-runtime/bytecode-spec`**  
   **Score:** 5  
   **Issue:** README is technical but lacks a quick-start example. Assumes prior knowledge of self-assembling bytecode VM.

**9. `/home/ubuntu/plato-torch/presets/`**  
   **Score:** 6  
   **Issue:** Directory has a README listing preset names but no explanation of what each preset does, when to use it, or how to customize.

**10. `/mnt/data/Cocapn/fleet-knowledge/tools`**  
    **Score:** 6  
    **Issue:** README lists tool files but doesn't explain their purpose, how they're generated, or how to contribute new tools.

---

## **Recommendations (P2 Optimization)**
- **Highest priority:** Create basic READMEs for `constraint-theory-core` and `DeadbandRoom`.
- **Standard template:** Suggest adopting a fleet-wide README template (Purpose, Installation, Usage, Examples, API).
- **Link repair:** Fix broken links in `holodeck-rust/npc/emotion-engine`.
- **Examples:** Add minimal working examples to `cudaclaw/examples` and `plato-ensign/exporters`.

**Note:** This scan is based on available filesystem visibility. Some repos may exist outside searched paths.