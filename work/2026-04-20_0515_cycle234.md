# Cycle 234

# Cycle 234 — Navigator
**Task:** Scan SuperInstance repos for READMEs that need improvement. List the 10 worst ones.

---

### Step 1 — Map the Negative Space (P0)
**Rocks (what NOT to do):**
- Don't assume all repos are under a single directory path without checking the filesystem.
- Don't rely on memory or hallucinated repo names; read from actual fleet-knowledge/ files.
- Don't evaluate READMEs without reading their actual content first.
- Don't list repos that don't exist in the provided context.
- Don't skip the P1 safe channel identification.

### Step 2 — Identify Safe Channels (P1)
**Safe Channels (where I CAN operate):**
- I can read the `fleet-knowledge/` directory structure from the provided context.
- I can analyze the READMEs of repos mentioned in the fleet-knowledge files (e.g., `PROJECT-VISION.md`, `DEADBAND-PROTOCOL.md`, Boot Camp list).
- I can use the Boot Camp's list of key technologies as a source of repo names.
- I can assess README quality based on observable metrics: presence, length, clarity, structure, examples.
- I can produce a factual list based only on repos explicitly named in the context.

### Step 3 — Optimize Within Channel (P2)
**Action:** Read the provided fleet-knowledge files to extract repo names, then evaluate their READMEs.

**Findings from Context:**
The provided files name several key fleet repositories:
1.  `plato-torch`
2.  `plato-ensign`
3.  `holodeck-rust`
4.  `fleet-simulator`
5.  `constraint-theory-core`
6.  `cudaclaw`
7.  `flux-runtime`

**Analysis:**
The task asks for the "10 worst ones," but the provided context only lists **7 specific repos**. I cannot invent 3 more. Therefore, I will evaluate the READMEs for these 7 based on the information available.

**Assessment of READMEs (Based on Provided Context):**
*   **`plato-torch`**: Mentioned as having "25 training room presets" and "self-training rooms." A README likely exists and may be detailed. **Status: Likely Good.**
*   **`plato-ensign`**: Described as exporting "room experience into LoRA adapters." A README explaining this process would be crucial. **Status: Needs Evaluation.**
*   **`holodeck-rust`**: Described as a "MUD server with sentiment-aware NPCs." This is a complex project requiring a good README. **Status: Needs Evaluation.**
*   **`fleet-simulator`**: "Fleet simulation for training data." A README defining inputs/outputs is essential. **Status: Needs Evaluation.**
*   **`constraint-theory-core`**: "Rust geometric snapping." Technical library; README needs examples. **Status: Needs Evaluation.**
*   **`cudaclaw`**: "GPU-resident agent runtime." Niche, performance-critical; README must be clear. **Status: Needs Evaluation.**
*   **`flux-runtime`**: "Self-assembling bytecode VM." Highly technical; README quality is paramount. **Status: Needs Evaluation.**

**Conclusion & Output:**
I cannot list 10 repos. Based on the **7 repos explicitly named in the fleet-knowledge files**, the ones most likely to need README improvement are those with complex, technical, or novel concepts that require clear documentation for contributors. Without access to the actual README files, I cannot definitively rank them.

**Factual Output:**
The 7 key fleet repos identified in the context are: `plato-torch`, `plato-ensign`, `holodeck-rust`, `fleet-simulator`, `constraint-theory-core`, `cudaclaw`, and `flux-runtime`. To complete the task of identifying the worst READMEs, I would need read access to these repositories' root directories to assess their `README.md` files for presence, completeness, and clarity. The most critical repos for README quality are likely the novel runtime systems (`cudaclaw`, `flux-runtime`) and the core libraries (`constraint-theory-core`, `holodeck-rust`).

**Tile Submitted:** Analysis of documented fleet repos and README evaluation prerequisites.