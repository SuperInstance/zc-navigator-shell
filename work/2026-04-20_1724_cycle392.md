# Cycle 392

# Cycle 392 — Navigator
**Task:** Scan SuperInstance repos for READMEs that need improvement. List the 10 worst ones.

---

### Step 1 — Map the Negative Space (P0)
**Rocks (what NOT to do):**
- Don't assume all repos are under a single directory path without checking the file system.
- Don't rely on memory or hallucinated repo names; read from actual fleet-knowledge/ files.
- Don't rank READMEs without a clear, objective scoring rubric.
- Don't proceed to scanning without first verifying the method to locate SuperInstance repos.

### Step 2 — Identify Safe Channels (P1)
**Safe Channels (where I CAN operate):**
1. Use the `fleet-knowledge/` directory as the source of truth for repo information.
2. Look for files that list or categorize fleet repositories (e.g., documentation, task outputs from other agents).
3. If a direct list isn't found, infer repo names from context in docs like BOOT-CAMP.md which reference specific repos (e.g., plato-torch, holodeck-rust).
4. For each identified repo, check for a README.md file.
5. Apply a consistent scoring criteria based on observable attributes (presence, length, structure, clarity).

### Step 3 — Optimize Within Channel (P2)
**Action Plan:**
1. **Locate repo list:** Search `fleet-knowledge/` for any file containing "repo" or listing project names.
2. **Extract repo names:** From found references, compile a candidate list.
3. **Check for READMEs:** For each candidate, see if a README.md exists in the knowledge base.
4. **Score READMEs:** Using a simple rubric (e.g., 0=missing, 1=placeholder, 2=minimal, 3=adequate, 4=good).
5. **Rank:** Sort by score ascending, list the 10 lowest-scoring.

**Rubric (0-4 scale):**
- **0:** No README file found.
- **1:** README exists but is extremely minimal (e.g., only a title, or "TODO").
- **2:** README has some content but lacks structure, key sections (Install, Use, API), or is unclear.
- **3:** README has basic structure and useful information but could be more comprehensive or polished.
- **4:** README is well-structured, clear, complete with examples, and follows fleet standards.

---

### Execution & Findings

**1. Locate repo list:**
- Searched `fleet-knowledge/` (as provided in context). The only files shown are `PROJECT-VISION.md`, `DEADBAND-PROTOCOL.md`, and a snippet of `BOOT-CAMP.md`.
- The `BOOT-CAMP.md` snippet mentions several repos: `plato-torch`, `plato-ensign`, `holodeck-rust`, `fleet-simulator`, `constraint-theory-core`, `cudaclaw`, `flux-runtime`.
- No comprehensive repo list file is present in the provided context.

**2. Extract repo names:**
From the context, I identify these **7 candidate SuperInstance repos**:
1. plato-torch
2. plato-ensign
3. holodeck-rust
4. fleet-simulator
5. constraint-theory-core
6. cudaclaw
7. flux-runtime

**3. Check for READMEs:**
The provided fleet knowledge does not contain the actual README files for these repos. I cannot read them to score. This is a **blocking rock**.

**4. P0 Re-evaluation:**
The negative space includes: attempting to score READMEs without access to them. The safe channel is to acknowledge the limitation and propose a next step based on available data.

**5. Adjusted Action:**
Since README content is not in context, I will:
- List the 7 repos identified.
- State that without README access, scoring cannot be performed.
- Recommend the next action: either the task requires pulling actual repo files, or the Navigator should be given a directory scan capability.

---

### Output: The 7 Identified SuperInstance Repos (README Status Unknown)

Based on fleet knowledge references, these are key SuperInstance repos:
1. **plato-torch** – Referenced in Boot Camp (Phase 1 reading).
2. **plato-ensign** – Mentioned in PROJECT-VISION as exporting LoRA adapters.
3. **holodeck-rust** – MUD server with sentiment-aware NPCs.
4. **fleet-simulator** – Generates training data.
5. **constraint-theory-core** – Rust geometric snapping.
6. **cudaclaw** – GPU-resident agent runtime.
7. **flux-runtime** – Self-assembling bytecode VM.

**Assessment:** Cannot list "10 worst READMEs" because:
- Only 7 repos are identified from context.
- Their README files are not present in `fleet-knowledge/` provided.
- Scoring requires reading the actual README content.

**Recommendation (Next Task):**
To complete this task, the Navigator needs either:
- A file system scan of the actual SuperInstance repository directories, or
- A dump of README.md files into `fleet-knowledge/` for analysis.

**Tile Submitted:** This analysis itself is a tile—documenting the constraint and mapping the available data.

---

**Navigator Status:** Task blocked by data access. Ready for next instruction or data provision.