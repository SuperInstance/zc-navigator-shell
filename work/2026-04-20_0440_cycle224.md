# Cycle 224

# Cycle 224 — Navigator
**Task:** Scan SuperInstance repos for READMEs that need improvement. List the 10 worst ones.

---

### Step 1 — Map the Negative Space (P0)
**Rocks (what NOT to do):**
- Don't assume all repos are under a single directory path without checking the actual fleet-knowledge structure.
- Don't rely on memory or hallucination; must read actual files.
- Don't evaluate READMEs without a clear, objective scoring rubric.
- Don't list repos that don't exist in the provided knowledge.
- Don't proceed to scoring (P2) without first establishing a method to find and read the READMEs (P1).

---

### Step 2 — Identify Safe Channels (P1)
**Safe Channels (where I CAN operate):**
1. The provided context includes `fleet-knowledge/docs/` with two files: `PROJECT-VISION.md` and `DEADBAND-PROTOCOL.md`.
2. The `Boot Camp` section lists 12 agent roles and their Phase 1 reading assignments, which reference several fleet repos (e.g., `plato-torch`, `fleet-simulator`, `holodeck-rust`, `constraint-theory-core`, `cudaclaw`, `flux-runtime`).
3. My task is to "Scan SuperInstance repos for READMEs." The term "SuperInstance" appears in `PROJECT-VISION.md` as "Cocapn/SuperInstance."
4. **Constraint:** The current context does not contain the actual README files for the listed repos. I only have the two documentation files and the boot camp instructions.
5. **Safe Action:** Since I cannot access the READMEs directly, I must operate within the channel of what is known. I can:
   - Acknowledge the limitation based on provided files.
   - Propose a method for how I *would* score READMEs if they were accessible.
   - List the repos *mentioned* in the context that likely have READMEs, based on the boot camp reading assignments.
   - Flag this as a data availability issue for the task.

---

### Step 3 — Optimize Within Channel (P2)
**Proposed README Scoring Rubric (if files were available):**
I would score each README from 0-10 based on:
- **Clarity (0-3):** Is the purpose immediately clear?
- **Completeness (0-3):** Does it cover setup, usage, and API?
- **Structure (0-2):** Is it well-organized with sections?
- **Links (0-2):** Does it link to related docs and code?

**List of Repos Mentioned in Context (Potential README Candidates):**
Based on the boot camp Phase 1 reading assignments for various agents:
1. `plato-torch`
2. `plato-ensign`
3. `holodeck-rust`
4. `fleet-simulator`
5. `constraint-theory-core`
6. `cudaclaw`
7. `flux-runtime`
8. `service-guard.sh` (script, but may have a README in its repo)
9. `DeadbandRoom` (preset within `plato-torch`)
10. GitHub API-related repos (implied by Curator task)

**Task Output Given Constraints:**
I cannot list the "10 worst ones" because the README files are not present in the provided `fleet-knowledge` context. To complete this task accurately, I would need direct file system access to the repositories listed above.

**Recommended Next Action:**
To proceed, I require either:
- A directory listing of the SuperInstance repos, or
- Direct content of the README.md files for the repos listed.

Given the current data, the most factual output is to report the limitation and list the repos I would inspect based on the context provided.

**Final Artifact for This Cycle:**
```
# README Analysis Blocked — Data Not in Context

## Repos Identified for Future Scanning (from fleet-knowledge context):
1. plato-torch
2. plato-ensign
3. holodeck-rust
4. fleet-simulator
5. constraint-theory-core
6. cudaclaw
7. flux-runtime
8. service-guard.sh (associated repo)
9. DeadbandRoom (component of plato-torch)
10. GitHub taxonomy repos (implied)

## Status: Awaiting README file access to apply scoring rubric.
```

---
**Tile Submitted:** Cycle 224 — Navigator — README scan constraint acknowledgment.