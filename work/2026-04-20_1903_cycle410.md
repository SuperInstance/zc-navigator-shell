# Cycle 410

# Cycle 410 — Navigator
**Task:** Scan SuperInstance repos for READMEs that need improvement. List the 10 worst ones.

---

### Step 1 — Map the Negative Space (P0)
**Rocks (what NOT to do):**
- Don't assume all repos are under a single directory path without checking the actual file system.
- Don't rely on memory or hallucination; read actual files from `fleet-knowledge/`.
- Don't list repos that don't exist or have no README.
- Don't evaluate READMEs without a clear, objective scoring rubric.
- Don't skip the P1 step of finding a safe channel before optimization (P2).

---

### Step 2 — Identify Safe Channels (P1)
**Safe Channels (where I CAN operate):**
1. Use the `fleet-knowledge/` directory as the source of truth for repo information.
2. Look for existing documentation that catalogs fleet repos (e.g., `docs/`, `memory/`).
3. Define a simple, repeatable scoring system for README quality based on observable file attributes.
4. Only list repos that are confirmed to exist and have a README file.
5. Proceed to scoring and ranking only after establishing the channel.

**Action:** Search `fleet-knowledge/` for any file that lists or references SuperInstance repositories.

**Findings:**
- `fleet-knowledge/docs/PROJECT-VISION.md` mentions "PLATO rooms" and "fleet repos" but no explicit list.
- `fleet-knowledge/docs/DEADBAND-PROTOCOL.md` is a protocol doc, not a repo list.
- `fleet-knowledge/docs/BOOT-CAMP.md` (referenced in context) lists agent roles and tasks but not repos.
- No `memory/` files provided in this context.
- No other `.md` files in the provided context contain a repo catalog.

**Constraint:** The provided fleet knowledge context does not contain a definitive list of SuperInstance repositories. Proceeding without this map would violate P0 (operating in unknown negative space).

**Decision:** Since the safe channel (a verified repo list) is not found in the provided context, I cannot complete the task as stated without risking P0 violation. However, I can define a **methodology** for the task that can be executed once the repo list is available, which is a safe P1 output.

---

### Step 3 — Optimize Within Channel (P2)
**Optimized Action:** Define a clear, objective README scoring rubric and process for when repo data is available.

**README Scoring Rubric (0–10 scale, lower is worse):**
1. **Existence (0 or 3 points):** 0 if no README file, 3 if present.
2. **Length (0–3 points):** 0 if empty or <50 words, 1 if 50–200 words, 2 if 200–500, 3 if >500.
3. **Structure (0–2 points):** 0 if plain text/no headers, 1 if basic headers, 2 if clear sections (e.g., Overview, Install, Use).
4. **Clarity (0–2 points):** 0 if unclear or jargon-heavy, 1 if understandable, 2 if concise and beginner-friendly.

**Process:**
1. Obtain a verified list of SuperInstance repo names/paths.
2. For each repo, check for `README.md` (or similar).
3. Score each README using the rubric.
4. Sort by total score (ascending) and list the 10 lowest scores.

**Example of a "Worst" README (hypothetical):**
- **Repo:** `example-repo`
- **Score:** 1 (Exists=3, Length=0, Structure=0, Clarity=0)
- **Reason:** README is present but contains only "TODO: write docs."

**Output for Current Cycle:**
Since a repo list is not in context, I cannot produce the requested list. Instead, I provide the **scoring rubric and process** as a tile. This is a safe, factual output that advances the task indirectly by establishing a repeatable method.

---

**Tile Submitted:**
```
# README Evaluation Rubric for SuperInstance Repos
## Cycle 410 — Navigator
### Purpose: Objective scoring to identify READMEs needing improvement.

**Scoring (0–10):**
- Existence: 0 (none) or 3 (present)
- Length: 0 (<50 words), 1 (50–200), 2 (200–500), 3 (>500)
- Structure: 0 (no headers), 1 (basic headers), 2 (clear sections)
- Clarity: 0 (unclear), 1 (understandable), 2 (concise)

**Process:**
1. Input: verified list of repo paths.
2. For each repo, locate README.md.
3. Apply rubric, sum scores.
4. Output: top 10 lowest scores (worst READMEs).

**Note:** This rubric is derived from common documentation best practices and is designed to be automated (e.g., via script). It avoids subjective terms like "good" or "bad" in favor of observable attributes.
```

**Next Step:** To complete the original task, the fleet must provide a source for the SuperInstance repo list (e.g., a `repos.md` file, GitHub API response, or directory listing). I can then execute the rubric and produce the ranked list.