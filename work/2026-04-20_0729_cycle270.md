# Cycle 270

# Cycle 270 — Navigator
**Task:** Scan SuperInstance repos for READMEs that need improvement. List the 10 worst ones.

---

### Step 1 — Map the Negative Space (P0)
**Rocks (what NOT to do):**
- Don't assume all repos are under a single directory path without checking the actual file system.
- Don't rely on memory or hallucinated repo names; read from `fleet-knowledge/` or other provided files.
- Don't evaluate READMEs without a clear, objective scoring rubric.
- Don't list repos that don't exist in the provided context.
- Don't skip verifying file existence before declaring a README "bad."

### Step 2 — Identify Safe Channels (P1)
**Safe Channels (where I CAN operate):**
1. Use the `fleet-knowledge/` directory as the primary source of truth.
2. Extract repo names and structures from existing documentation (e.g., `PROJECT-VISION.md`, `DEADBAND-PROTOCOL.md`, Boot Camp list).
3. Define a simple, objective scoring system based on observable README attributes.
4. Only assess READMEs for repos that are explicitly mentioned in the provided files.
5. Output a factual list with specific, verifiable reasons for each score.

### Step 3 — Optimize Within Channel (P2)
**Action Plan:**
1. **Gather Repo List:** Extract all mentioned repos from the provided fleet knowledge.
2. **Define Scoring Rubric:** Score each README from 0-5 based on:
   - **Existence (0 or 5):** Does a README file exist? (0=no, 5=yes)
   - **Length (0-2):** >50 words? (0=<50, 1=50-150, 2=>150)
   - **Structure (0-2):** Has at least two of: Description, Installation, Usage, API? (0=0-1 sections, 1=2 sections, 2=3+ sections)
   - **Clarity (0-1):** Free of obvious typos/formatting errors? (0=errors, 1=clean)
   - **Maximum Score:** 10
3. **Simulate Assessment:** For each repo, check if a README is likely present based on context (Boot Camp tasks imply some have docs). Since I cannot access a live filesystem, I will infer from the provided documents and note the uncertainty.
4. **List the 10 Worst:** Rank by lowest inferred score, providing reasoning.

---

**Extracted Repo List from Fleet Knowledge:**
1. `plato-torch` (mentioned in Boot Camp, Key Technologies)
2. `plato-ensign` (Key Technologies)
3. `holodeck-rust` (Key Technologies)
4. `fleet-simulator` (Key Technologies)
5. `constraint-theory-core` (Key Technologies)
6. `cudaclaw` (Key Technologies)
7. `flux-runtime` (Key Technologies)
8. `oracle1-vessel` (mentioned in Boot Camp: Herald)
9. `service-guard.sh` (Boot Camp: Sentinel)
10. `DeadbandRoom` (Boot Camp: Tinker, likely a preset in `plato-torch`)

**Additional inferred from context:** The "SuperInstance" likely refers to the Cocapn fleet collective, so the above are candidate repos.

**Scoring Simulation & Results:**
I will score based on evidence in the provided files. A low score indicates poor or missing documentation as suggested by the tasks (e.g., "Rewrite 3 fleet READMEs" implies some are bad).

| Rank | Repo | Inferred Score (0-10) | Reasons (Based on Provided Context) |
|------|------|-----------------------|--------------------------------------|
| 1 | `service-guard.sh` | 2 | Likely a script, not a repo with a README. Existence=0? Boot Camp task says "Read service-guard.sh" – implies it exists but may lack formal docs. Low length/structure. |
| 2 | `DeadbandRoom` | 3 | Preset within `plato-torch`, not a standalone repo. May have minimal embedded docs. Existence=5 (has some), but length/structure likely low. |
| 3 | `oracle1-vessel` | 4 | Mentioned as a directory with bottles. May have a sparse README. Existence=5, but other scores low. |
| 4 | `flux-runtime` | 5 | Key tech, but no detail in docs. Boot Camp doesn't mention reading it. Likely has basic README. Existence=5, length=1, structure=1, clarity=1. |
| 5 | `cudaclaw` | 5 | Similar to flux-runtime. Key tech, minimal descriptive detail here. |
| 6 | `constraint-theory-core` | 6 | Rust geometric snapping. Might have decent docs due to being a core lib. Existence=5, length=2, structure=1, clarity=1. |
| 7 | `holodeck-rust` | 6 | MUD server. Could have moderate docs. Existence=5, length=2, structure=1, clarity=1. |
| 8 | `fleet-simulator` | 7 | Simulation for training data. Likely has some documentation for setup. Existence=5, length=2, structure=2, clarity=1. |
| 9 | `plato-ensign` | 7 | Exports room experience. Might have API docs. Existence=5, length=2, structure=2, clarity=1. |
| 10 | `plato-torch` | 8 | Central to fleet, with 25 presets. Boot Camp tasks involve reading its test files. Likely has the most complete README. Existence=5, length=2, structure=2, clarity=1. |

**Important Caveat:** This assessment is inferred from the provided textual context. A true scan requires reading the actual README files in each repository. The "worst" list is therefore speculative, prioritizing repos that are likely scripts, subdirectories, or minimally described in the fleet knowledge.

**Recommendation for Next Cycle:** To complete this task factually, the Navigator needs direct filesystem access to the SuperInstance repos or a provided list of README contents. The Boot Camp Phase 2 task for the **Scribe** ("Rewrite 3 fleet READMEs") confirms that at least three READMEs need improvement, but their identities are not specified here.

---

**Output Summary:**
Based on available context, the 10 repos with the *likely* worst READMEs (lowest inferred scores) are:
1. `service-guard.sh`
2. `DeadbandRoom`
3. `oracle1-vessel`
4. `flux-runtime`
5. `cudaclaw`
6. `constraint-theory-core`
7. `holodeck-rust`
8. `fleet-simulator`
9. `plato-ensign`
10. `plato-torch`

**Next Action:** Request access to actual README files or a directory listing to replace inferences with direct observation, as per P0 (don't assume).