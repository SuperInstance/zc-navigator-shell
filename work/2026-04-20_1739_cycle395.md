# Cycle 395

# Cycle 395 — Navigator
**Task:** Scan SuperInstance repos for READMEs that need improvement. List the 10 worst ones.

---

### Step 1 — Map the Negative Space (P0)
**Rocks (what NOT to do):**
- Don't assume all repos are under a single directory path without checking the actual file structure.
- Don't rely on hallucinated repo names; read from the actual `fleet-knowledge/` files.
- Don't produce a list without concrete, file-based evidence for each entry.
- Don't ignore the "READMEs that need improvement" criteria; a missing README is the worst case, but a poor one also qualifies.

### Step 2 — Identify Safe Channels (P1)
**Safe Channels (where I CAN operate):**
- I can read the `fleet-knowledge/` directory to find references to actual repositories.
- I can look for patterns in existing documentation (like `PROJECT-VISION.md`, `DEADBAND-PROTOCOL.md`, `BOOT-CAMP.md`) that mention repo names.
- I can infer README quality based on fleet standards: presence, structure, clarity, and completeness.
- I can list repos where READMEs are absent, minimal, outdated, or non-informative as "worst."

### Step 3 — Optimize Within Channel (P2)
**Best Path:**
1. Extract all repository names mentioned in the provided fleet knowledge files.
2. For each, assess README status based on available data.
3. Rank by severity of deficiency (missing > empty > poor).
4. Output the top 10 with specific reasons.

---

## Execution

**Repositories Identified from Fleet Knowledge:**
From the provided files, I see explicit mentions of:
- `plato-torch`
- `plato-ensign`
- `holodeck-rust`
- `fleet-simulator`
- `constraint-theory-core`
- `cudaclaw`
- `flux-runtime`

Additional repos inferred from context (e.g., `oracle1-vessel`, `service-guard.sh` as part of a repo) but not explicitly named in the snippets.

**Assessment:**
Since I cannot directly access the file system beyond the provided snippets, I must base my assessment on the evidence within `fleet-knowledge/`. The `BOOT-CAMP.md` file (partial) lists tasks for various agents that imply these repos exist and may have READMEs of varying quality. However, without the actual README files, I cannot definitively rank them.

**P0 Violation Risk:** Proceeding to rank without direct evidence would be a P0 violation (hallucinating). The safe channel is to report the limitation and propose a concrete next step.

---

## Output

**Finding:** Based solely on the provided `fleet-knowledge/` snippets, I cannot list the "10 worst READMEs" because the README files themselves are not present in the context. To complete this task factually, I would need either:
1. Direct access to the repository file structures, or
2. The README content for the mentioned repos included in the context.

**Proposed Next Step (P1 Safe Channel):**
If the goal is to identify READMEs needing improvement, a viable approach is:
1. Use a script to clone all SuperInstance repos (from a master list).
2. Parse each for a README file (README.md, README.rst, etc.).
3. Score each README based on objective metrics: file size, presence of sections (Installation, Usage, API), links, code examples.
4. Rank by score and list the bottom 10.

**Current, Evidence-Based List (Based on Mentions):**
The following repos are confirmed to exist in the fleet. Without their READMEs, they are *candidates* for improvement, but cannot be ranked:

1. `plato-torch`
2. `plato-ensign`
3. `holodeck-rust`
4. `fleet-simulator`
5. `constraint-theory-core`
6. `cudaclaw`
7. `flux-runtime`

**Recommendation:** To proceed, please provide the README content for these repos, or grant filesystem access to the `fleet-knowledge/` directory containing repository data. This aligns with the Navigator role: mapping actual codebase structure, not assuming.

---
**Tile Submitted:** Analysis of README assessment limitation and proposed method.
**Status:** P1 complete — identified safe channel for task execution given constraints.