# Cycle 399

# Cycle 399 — Navigator
**Task:** Scan SuperInstance repos for READMEs that need improvement. List the 10 worst ones.

---

### Step 1 — Map the Negative Space (P0)
**Rocks (what NOT to do):**
- Don't assume all repos are under a single directory path without checking the actual file system.
- Don't rely on memory or hallucinated repo names; read from the actual `fleet-knowledge/` directory.
- Don't evaluate READMEs based on subjective "badness"; define objective, measurable criteria.
- Don't skip repos that are part of the SuperInstance project but might be nested or have alternate naming.
- Don't produce a list without providing specific, actionable reasons for each entry.

### Step 2 — Identify Safe Channels (P1)
**Safe Channels (where I CAN operate):**
1. I can read the directory structure from the provided `fleet-knowledge/` context.
2. I can define clear, objective criteria for a "README that needs improvement."
3. I can examine the actual content of README files that exist within the known repos.
4. I can limit my search to repositories explicitly mentioned in the fleet knowledge (e.g., `plato-torch`, `holodeck-rust`).
5. I can produce a ranked list based on the defined criteria, with concrete evidence from the files.

### Step 3 — Optimize Within Channel (P2)
**Best Path:**
1. **Define Criteria:** Establish objective metrics for README quality.
2. **Inventory Repos:** List all repositories mentioned in the `fleet-knowledge/` files.
3. **Examine READMEs:** Check for the existence and content of a README in each repo.
4. **Score & Rank:** Apply criteria to score each README, then rank the lowest scores.
5. **Present Findings:** List the 10 worst with specific deficiencies and suggestions.

---

## Execution

### 1. Defined README Improvement Criteria
A README "needs improvement" if it exhibits one or more of the following:
- **Absent:** No README file detected.
- **Skeletal:** Extremely short (e.g., only a title or one line).
- **Outdated:** Contains references to old versions, deprecated APIs, or incorrect project status.
- **Unclear:** Lacks a clear description of the project's purpose, how to install it, or how to use it.
- **Non-Standard:** Missing standard sections (e.g., Installation, Usage, API, Contributing).

### 2. Inventory of SuperInstance Repos from Fleet Knowledge
From the provided files, the following repositories are part of the Cocapn/SuperInstance fleet:
1. `plato-torch`
2. `plato-ensign`
3. `holodeck-rust`
4. `fleet-simulator`
5. `constraint-theory-core`
6. `cudaclaw`
7. `flux-runtime`

*(Note: The `fleet-knowledge/` directory provided does not contain the full file trees for these repos, only documentation snippets. Therefore, I cannot directly read their READMEs. This is a **P0 Rock**—I cannot proceed by hallucinating content. I must operate within the safe channel of what is actually provided.)*

### 3. Adjusted Safe Channel (P1 Re-evaluation)
Given the constraint, the safe channel is:
- I can only analyze READMEs or documentation files that are **explicitly present within the provided `fleet-knowledge/` context**.
- I can identify which repos are mentioned but lack any provided README content, flagging them as "unknown status."
- I can analyze the quality of the documentation files that *are* provided (e.g., `PROJECT-VISION.md`, `DEADBAND-PROTOCOL.md`) as proxies for project documentation standards.

### 4. Analysis of Provided Documentation
The `fleet-knowledge/` directory provided contains:
- `docs/PROJECT-VISION.md`
- `docs/DEADBAND-PROTOCOL.md`
- `docs/` (implied, but no other files shown)
- `BOOT-CAMP.md` (in the context, but not as a separate file)
- `STATE.md` (in the context)

**Findings:**
- `PROJECT-VISION.md` is comprehensive, clear, and well-structured.
- `DEADBAND-PROTOCOL.md` is precise, formal, and includes examples.
- No actual repository README files are provided in the context.

### 5. Conclusion and Output
Since I cannot access the actual repository READMEs, I cannot complete the task as originally scoped without hitting a P0 rock (hallucination). However, following the Deadband Protocol, I can report the following within the safe channel:

**List of SuperInstance Repos with README Status Unknown (Due to Lack of Data):**
1. `plato-torch` - README not provided in context.
2. `plato-ensign` - README not provided in context.
3. `holodeck-rust` - README not provided in context.
4. `fleet-simulator` - README not provided in context.
5. `constraint-theory-core` - README not provided in context.
6. `cudaclaw` - README not provided in context.
7. `flux-runtime` - README not provided in context.

**Recommendation:**
To accurately "Scan SuperInstance repos for READMEs that need improvement," the agent requires read access to the actual repository directories or their mirrored contents within the `fleet-knowledge/` file tree. The provided context only includes high-level vision and protocol documents, not the repository files themselves.

**Actionable Insight:**
The fleet's documentation strategy, as evidenced by the provided `PROJECT-VISION.md` and `DEADBAND-PROTOCOL.md`, is strong at the high-level conceptual layer. A potential "improvement" would be to ensure this quality and completeness propagates down to every individual repository's README, which currently cannot be verified.

**Final Output (Within Constraints):**
Based solely on the provided files, I cannot list "the 10 worst ones." I can only state that the README status for all seven core fleet repositories is **unknown** due to absence of data. The task cannot be completed without hitting a P0 violation. The safe output is this diagnostic report.

---

**Tile Submitted:** Diagnostic report on README analysis constraint.
**Note for Fleet:** To enable this task, include repository README files in the `fleet-knowledge/` context or grant filesystem access to the repos.