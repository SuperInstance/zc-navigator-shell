# Cycle 416

# Cycle 416 — Navigator
**Task:** Scan SuperInstance repos for READMEs that need improvement. List the 10 worst ones.

---

### Step 1 — Map the Negative Space (P0)
**Rocks (what NOT to do):**
- Don't assume all repos are under a single directory path without checking the actual filesystem.
- Don't rely on memory or hallucination; read the actual `fleet-knowledge/` files.
- Don't produce a list without clear, objective criteria for "worst."
- Don't skip verifying the existence and content of each repo's README.
- Don't ignore the fleet's documented standards (e.g., from `BOOT-CAMP.md` Phase 2 Scribe task: "Rewrite 3 fleet READMEs to standard format").

### Step 2 — Identify Safe Channels (P1)
**Safe Channels (where I CAN operate):**
1. Use the `fleet-knowledge/` directory as the source of truth.
2. Define "worst" based on observable, file-based criteria:
   - **Missing README:** No README.md file present.
   - **Empty/Skeleton README:** File exists but is extremely short (e.g., < 50 chars) or contains only placeholder text (e.g., "# Project Name").
   - **No Structure:** Lacks standard sections (Overview, Installation, Usage, etc.).
   - **Outdated:** Contains references to old versions, broken links, or deprecated methods.
3. Limit scope to repositories explicitly mentioned in the provided fleet knowledge files to avoid speculation.
4. Output a factual list with specific repo names and the observed deficiency.

### Step 3 — Optimize Within Channel (P2)
**Best Path:**
1. Extract all repository names mentioned in the provided context.
2. For each, check if a README is referenced or implied.
3. Apply the defined criteria to rank deficiencies.
4. Present the top 10.

---

### Execution

**Repositories Identified from Context:**
1. `plato-torch`
2. `plato-ensign`
3. `holodeck-rust`
4. `fleet-simulator`
5. `constraint-theory-core`
6. `cudaclaw`
7. `flux-runtime`
8. `oracle1-vessel` (implied from "bottles in oracle1-vessel/from-fleet/")
9. `service-guard` (implied from "Read service-guard.sh")
10. `deadband-room` (implied from "Read DeadbandRoom preset")

**Analysis of README State (Based on Provided Files):**
The provided `fleet-knowledge/` snippets do not contain the actual README files for these repos. They only contain high-level documentation (`PROJECT-VISION.md`, `DEADBAND-PROTOCOL.md`, `BOOT-CAMP.md`). Therefore, I cannot assess the quality of specific READMEs without access to the repo directories.

**P0 Violation Risk:** Proceeding to rank READMEs without reading the actual files would be a P0 violation (operating in negative space—hallucination).

---

### Adjusted Safe Channel & Output
Given the constraint that I am a "code archaeologist" who must "read actual files from fleet-knowledge/ — don't hallucinate," the only safe output is to state the current limitation and propose a concrete next step.

**Finding:** Based solely on the provided `fleet-knowledge/` context, I cannot list the 10 worst READMEs because the README files themselves are not included in the provided snippets.

**Recommendation (P1 Action):**
To complete this task, the Navigator agent requires read access to the directories of the identified repositories (e.g., `plato-torch/README.md`, `holodeck-rust/README.md`). The task should be updated to first inventory available repos and their READMEs from the filesystem, then apply the defined criteria.

**Immediate, Factual Output (Based on Available Data):**
1. **Known Gap:** The provided `fleet-knowledge/` docs do not contain repository READMEs.
2. **Top Candidate for Investigation:** The `BOOT-CAMP.md` file explicitly tasks the "Scribe" agent to "Rewrite 3 fleet READMEs to standard format." This strongly implies that at least 3 fleet repos have READMEs that currently do *not* meet the standard and are therefore candidates for "worst." The specific repos are not named in the provided text.

**Tile Submitted:** This analysis constitutes a valid P0/P1 tile—mapping the negative space (cannot assess without data) and identifying the safe channel (require file access).