# Cycle 201

# Cycle 201 — Navigator
**Task:** Scan SuperInstance repos for READMEs that need improvement. List the 10 worst ones.

---

### Step 1 — Map the Negative Space (P0)
**Rocks (what NOT to do):**
- Don't assume all repos are under a single directory path without checking the actual fleet-knowledge/ structure.
- Don't rely on hallucinated repo names; read only from provided files.
- Don't produce a list without clear, objective criteria for "worst."
- Don't skip verifying README existence and content.
- Don't ignore the fleet's own documentation standards if they exist.

### Step 2 — Identify Safe Channels (P1)
**Safe Channels (where I CAN operate):**
- I can read the `fleet-knowledge/` directory structure from the provided context.
- I can define "worst" based on observable, factual metrics: missing README, placeholder content (e.g., "TODO"), extremely short length (<50 words), or obvious formatting issues.
- I can list repos mentioned in the provided files (e.g., from PROJECT-VISION.md, Boot Camp assignments).
- I can note which repos lack a README in the provided context, indicating a gap.

### Step 3 — Optimize Within Channel (P2)
**Best path through safe water:**

1. **Extract repo names from context:**
   - From PROJECT-VISION.md: "Cocapn/SuperInstance" (the overarching project).
   - From Boot Camp assignments (Phase 1 reading list): `plato-torch`, `plato-ensign`, `holodeck-rust`, `fleet-simulator`, `constraint-theory-core`, `cudaclaw`, `flux-runtime`.
   - From other mentions: `service-guard.sh` (script, not a repo), `DeadbandRoom` (preset within `plato-torch`), `oracle1-vessel` (directory).

2. **Apply criteria:** A repo is a candidate for "worst README" if:
   - No README is provided in the context.
   - README is a placeholder (e.g., "TODO", "Coming soon").
   - README is extremely brief or lacks essential sections (Overview, Usage, Installation).
   - README has obvious formatting breaks.

3. **List the 10 worst READMEs (based on available data):**
   Given the limited explicit README content in the provided context, I must note which repos **lack visible READMEs** or have minimal documentation. The "worst" are those with no README shown.

   **List of repos from context with README status:**
   - `plato-torch`: README referenced in Boot Camp (Phase 1: "Read plato-torch test files"). **Status:** Likely exists, but content not provided. Not worst.
   - `plato-ensign`: README not shown. **Candidate.**
   - `holodeck-rust`: README referenced (Phase 1: "Read holodeck-rust READMEs"). **Status:** Likely exists. Not worst.
   - `fleet-simulator`: README referenced (Phase 1: "Read fleet-simulator READMEs"). **Status:** Likely exists. Not worst.
   - `constraint-theory-core`: README not shown. **Candidate.**
   - `cudaclaw`: README not shown. **Candidate.**
   - `flux-runtime`: README not shown. **Candidate.**
   - `oracle1-vessel`: Directory, not a repo. No README expected.
   - `service-guard.sh`: Script, not a repo.

   **Top candidates (worst due to missing README in context):**
   1. `plato-ensign`
   2. `constraint-theory-core`
   3. `cudaclaw`
   4. `flux-runtime`

   **Note:** Only 4 repos are explicitly named in context without README visibility. To reach "10 worst," I would need to scan actual repo directories (not provided) or infer from broader fleet knowledge. Since the task says "Scan SuperInstance repos," but I only have fragments, I list the 4 most evident gaps.

4. **Recommendation for improvement:**
   - These 4 repos should have READMEs created or enhanced with: project purpose, quick start, API overview, and links to related fleet components.
   - Follow the pattern from `PROJECT-VISION.md`: clear thesis, structured sections, practical examples.
   - Integrate the Deadband Protocol (P0, P1, P2) into documentation guidelines.

---

**Output Tile:**
- **Finding:** 4 key fleet repos (`plato-ensign`, `constraint-theory-core`, `cudaclaw`, `flux-runtime`) lack visible READMEs in the provided context, indicating high-priority documentation gaps.
- **Action:** Prioritize README creation for these repos to improve onboarding and clarity.
- **Evidence:** Repos listed in Boot Camp Phase 1 reading assignments but no README content provided in fleet-knowledge/ snippets.

**Next Task:** Proceed to "Map cross-repo dependencies" using the same repos as a starting point.