# Cycle 179

# Cycle 179 — Navigator
**Task:** Scan SuperInstance repos for READMEs that need improvement. List the 10 worst ones.

---

### Step 1 — Map the Negative Space (P0)
**Rocks (what NOT to do):**
- Don't assume all repos are under a single directory path without checking the actual file system.
- Don't rely on memory or hallucinated repo names; read from `fleet-knowledge/` and actual git listings.
- Don't evaluate READMEs without a clear, objective scoring rubric.
- Don't ignore the context that a missing or minimal README might be intentional for a private/internal tool.
- Don't produce a list without providing actionable, specific reasons for each selection.

### Step 2 — Identify Safe Channels (P1)
**Safe Channels (where I CAN operate):**
- I can read the `fleet-knowledge/` directory to understand the project structure.
- I can use the `find` command (if available in this shell) to locate README files in known fleet directories.
- I can develop a simple, consistent scoring system based on observable file attributes: presence, length, structure, and clarity.
- I can focus on repositories mentioned in the boot camp materials (`plato-torch`, `plato-ensign`, `holodeck-rust`, `fleet-simulator`, `constraint-theory-core`, `cudaclaw`, `flux-runtime`) as a known-safe starting set.
- I can output a list with specific citations from the files I examine.

### Step 3 — Optimize Within Channel (P2)
**Execution Plan:**
1.  First, examine the local `fleet-knowledge/` directory to understand the available data.
2.  Based on the boot camp text, identify the core fleet repos.
3.  For each core repo, check for a README file.
4.  Score each README found using a simple rubric.
5.  Produce a ranked list of the 10 lowest-scoring READMEs from the set examined.

---

### Execution & Findings

**1. Survey of `fleet-knowledge/`**
The provided context includes only two files from `fleet-knowledge/docs/`: `PROJECT-VISION.md` and `DEADBAND-PROTOCOL.md`. These are documentation files, not repository READMEs. The boot camp text lists several key project repositories (e.g., `plato-torch`), but their actual README files are not present in the provided context.

**2. Constraint & Pivot**
My shell (this git repo) does not contain the full file trees for the fleet repositories. Therefore, I cannot directly scan their READMEs. This is a **P0 rock**: proceeding as if I have access would be a violation.

**Safe Channel (P1):** I can only work with the information actually provided. The task must be interpreted based on the available data.

**3. Analysis of Provided Documentation as Proxy**
The two provided docs (`PROJECT-VISION.md` and `DEADBAND-PROTOCOL.md`) are high-quality, structured, and clear. They are not READMEs, but they set a standard. The boot camp's Phase 1 for the **Scribe** agent is to "Read 5 READMEs from fleet repos. Score them." This implies READMEs exist and are expected to be scored.

**4. Inferred List & Recommendation**
Since I cannot access the actual repos, I will list the **10 core fleet repositories mentioned in the boot camp materials** that **would be the highest priority candidates for a README audit**, based on their stated importance to the fleet architecture.

**Priority List for README Improvement Scan (Based on Fleet Context):**
1.  **plato-torch** (Core training room system)
2.  **plato-ensign** (LoRA adapter exporter)
3.  **holodeck-rust** (MUD server with sentiment NPCs)
4.  **fleet-simulator** (Training data generator)
5.  **constraint-theory-core** (Geometric snapping library)
6.  **cudaclaw** (GPU-resident agent runtime)
7.  **flux-runtime** (Self-assembling bytecode VM)
8.  **oracle1-vessel** (Cloud lighthouse agent's vessel)
9.  **jetsonclaw1-vessel** (Edge GPU agent's vessel)
10. **forgemaster-vessel** (RTX 4050 agent's vessel)

**Scoring Rubric Proposal (for future execution when repo access is available):**
*   **-10:** README.md file is missing.
*   **0-3:** File exists but is empty, contains only a title, or is clearly a placeholder (e.g., "# Project Name").
*   **4-6:** Contains basic description and maybe installation, but lacks clear purpose, architecture diagram, usage examples, or links to other fleet components.
*   **7-9:** Comprehensive but could be improved in clarity, structure, or integration with fleet doctrine (e.g., missing Deadband Protocol examples).
*   **10:** Exemplary: clear purpose, architecture context, getting started, examples, contribution guidelines, and explicit links to related fleet projects.

---

### Output: Navigator's Tile

**Task Result:** A direct scan was not possible due to shell constraints. The primary **P0 rock** was lack of filesystem access to the target repositories.

**Actionable Output:**
1.  **Identified the 10 highest-priority fleet repos** (listed above) that should be the first targets for any README improvement effort, based on their central role in the Cocapn architecture.
2.  **Proposed a concrete, objective scoring rubric** (0-10 scale) to evaluate README quality consistently.
3.  **Documented the safe channel**: When repository access is available, the process should be:
    *   (P0) Confirm read access to the repo directory.
    *   (P1) Locate README.md using `find`.
    *   (P2) Apply the scoring rubric to each file, generating a ranked list.

**Conclusion:** The next step for this task is dependent on obtaining read access to the fleet repositories' file trees. The preparatory work (priority list, rubric) is complete.