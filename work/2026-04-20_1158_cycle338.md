# Cycle 338

# Cycle 338 — Navigator
**Task:** Scan SuperInstance repos for READMEs that need improvement. List the 10 worst ones.

---

### Step 1 — Map the Negative Space (P0)
**Rocks (what NOT to do):**
- Don't assume all repos are under a single directory path without checking the file system.
- Don't rely on memory or hallucinated repo names; read actual files from `fleet-knowledge/`.
- Don't evaluate READMEs without a clear, objective scoring rubric.
- Don't proceed to list "worst" READMEs without first establishing what "worst" means in this context (missing, empty, outdated, uninformative).
- Don't ignore the fleet's own documentation standards if they exist.

### Step 2 — Identify Safe Channels (P1)
**Safe Channels (where I CAN operate):**
1. Use the `fleet-knowledge/` directory as the source of truth for repo information.
2. Look for existing documentation on README standards (e.g., in `docs/`).
3. Scan for files that list or categorize repos (e.g., `BOOT-CAMP.md` mentions repos like `plato-torch`, `fleet-simulator`).
4. Define a simple, factual scoring criteria based on observable file attributes:
   - **Existence:** Does `README.md` exist in the repo root?
   - **Length:** Is it empty or extremely short (<50 words)?
   - **Content:** Does it explain the repo's purpose, how to use it, and how it fits into the fleet?
   - **Freshness:** Is there a date or version indicating recent updates?
5. Limit analysis to repos explicitly mentioned in the provided fleet knowledge to avoid speculation.

### Step 3 — Optimize Within Channel (P2)
**Best Path:**
1. **Extract repo list from fleet knowledge.** From the provided files:
   - `plato-torch` (mentioned in Boot Camp Phase 1)
   - `plato-ensign`
   - `holodeck-rust`
   - `fleet-simulator`
   - `constraint-theory-core`
   - `cudaclaw`
   - `flux-runtime`
   - `oracle1-vessel` (implied from `Herald` task)
   - `service-guard.sh` (script, but referenced as a "service")
   - `rabbit-trail` research docs (likely a repo or directory)
   *Note: This is a limited set from context. A full scan would require filesystem access.*

2. **Apply scoring criteria.** For each repo, assess README status based on available data:
   - `plato-torch`: Has a `README.md`? Unknown from context. Boot Camp references "DeadbandRoom preset" and "test files," suggesting some documentation exists.
   - `plato-ensign`: No README details provided.
   - `holodeck-rust`: README likely exists (mentioned in Phase 1 reading).
   - `fleet-simulator`: README likely exists (mentioned in Phase 1 reading).
   - `constraint-theory-core`: No README details.
   - `cudaclaw`: No README details.
   - `flux-runtime`: No README details.
   - `oracle1-vessel`: No README details.
   - `service-guard.sh`: Not a repo; a script. Likely has inline comments.
   - `rabbit-trail`: Possibly a docs directory, not a code repo.

3. **Identify gaps.** Without filesystem access to actual READMEs, I cannot objectively rank them. The biggest "rock" is missing data.

4. **Produce a useful output given constraints.** Instead of a false-ranked list, I will:
   - State the limitation clearly.
   - Propose a method for the task if given filesystem access.
   - List repos from fleet knowledge that *likely* need README attention based on absence from detailed discussion.

---

## Output: README Improvement Assessment

**Status:** Cannot complete task as specified due to lack of filesystem access to repo READMEs. The provided `fleet-knowledge/` snippets do not contain the actual README files for scoring.

**Proposed Method for Future Cycle:**
1. Clone or navigate the SuperInstance GitHub organization.
2. Run a script to collect all `README.md` files.
3. Score each using rubric:
   - **Score 0:** No README.
   - **Score 1:** README exists but is empty or placeholder (e.g., "TODO").
   - **Score 2:** README is short (<50 words) or lacks purpose/usage.
   - **Score 3:** README is adequate but outdated or missing links.
   - **Score 4:** README is clear, comprehensive, and current.
4. Sort by score ascending to identify the 10 worst.

**Repos from Fleet Knowledge That May Need README Attention:**
*(Based on being mentioned without evidence of existing README content in provided docs)*
1. `plato-ensign`
2. `constraint-theory-core`
3. `cudaclaw`
4. `flux-runtime`
5. `oracle1-vessel`
6. `rabbit-trail` (if a repo)
7. `fleet-simulator` (if README is brief)
8. `holodeck-rust` (if README is brief)
9. `plato-torch` (if README is brief)
10. `service-guard` (if considered a repo)

**Recommendation:** This task requires Phase 3 (Build & Test) tools—specifically, filesystem access or API calls to GitHub. As a Navigator in Phase 4, I can map the *process* for the task, but execution depends on environment capabilities.

**Tile Submitted:** Analysis of README assessment constraints and proposed method.